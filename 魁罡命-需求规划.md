# 八字排盘 —— "魁罡命"渲染功能 · 需求规划文档

> **文档版本**: v1.0  
> **创建日期**: 2026-06-29  
> **用途**: 直接投喂给 AI 编程工具（Zcode / Cursor 等），作为功能开发的完整技术需求文档  
> **定位**: 在已有八字排盘系统中，为命主增加"魁罡"命格的检测与可视化渲染

---

## 一、功能概述

在现有八字排盘结果中，新增 **"魁罡"（Kui Gang）** 命格的检测逻辑与前端渲染。魁罡属于**吉神·权势类**，主杀伐权柄、刚毅果断，但也有严格的成格与破格条件。需要在以下场景中检测并展示：

1. **命局日柱检测** — 判断命主日柱是否为魁罡四日之一（核心判定）
2. **四柱重逢检测** — 判断年/月/时柱是否再现魁罡干支（叠逢加分）
3. **成格/破格判定** — 结合身旺衰、财官刑煞判断魁罡是否真正成格
4. **大运/流年遇魁罡** — 判断行运中是否逢魁罡之地

---

## 二、文献依据与理论出处

### 2.1 核心典籍出处

| 典籍 | 原文摘录 | 说明 |
|------|---------|------|
| **《三命通会》** | "魁罡四日最为先，叠叠重逢掌大权。" "壬辰庚戌与庚辰，戊戌魁罡四座神。不见财官刑煞并，身行旺地贵无伦。" | 确立了魁罡四日的核心定义和成格条件 |
| **《渊海子平》** | "魁罡重叠是贵人，天元健旺喜临身。" "魁罡性严有操持，运行身旺发福，一见财官祸患立至。" | 阐明身旺为喜、财官为忌的核心规则 |
| **《星平会海》** | 魁罡格者若遇"将星"则易极端化，或功成名就或身陷囹圄 | 强化魁罡的双重性（吉凶并存） |
| **《壶中子》** | "辰为天罡，戌为河魁，乃阴阳绝灭之地" | 天文意象：辰戌为阴阳绝灭之处 |

### 2.2 天文与哲学原理

> "辰为天罡，戌为河魁，乃阴阳绝灭之地。辰中有乙木，为春天余气；戌中有辛金，为秋天余气。辰为水库，戌为火库，二者相遇则天冲地击。"

- **辰（天罡）**: 龙位，水库，藏戊土/乙木/癸水
- **戌（河魁）**: 犬位，火库，藏戊土/辛金/丁火
- **庚金** 坐辰/戌：金土相生，锐利而厚重
- **戊土** 坐戌：土气叠加，厚重而决断
- **壬水** 坐辰：水入库中，深藏不露

---

## 三、推算逻辑（核心算法）

### 3.1 魁罡四日定义（硬编码常量）

```javascript
// 魁罡四日 — 日柱干支组合（唯一判定标准）
const KUIGANG_DAYS = [
  { stem: '庚', branch: '辰', name: '庚辰', type: '罡' },  // 庚金坐辰土（天罡）
  { stem: '壬', branch: '辰', name: '壬辰', type: '罡' },  // 壬水坐辰土（天罡）
  { stem: '庚', branch: '戌', name: '庚戌', type: '魁' },  // 庚金坐戌土（河魁）
  { stem: '戊', branch: '戌', name: '戊戌', type: '魁' },  // 戊土坐戌土（河魁）
];
```

### 3.2 核心查法：日柱判定

**规则极其简单** — 仅检查日柱天干地支组合是否为上述四日之一：

```javascript
/**
 * 检测日柱是否为魁罡
 * @param {string} dayStem  - 日干（甲~癸）
 * @param {string} dayBranch - 日支（子~亥）
 * @returns {{ hit: boolean, type: string, name: string }}
 */
function detectKuiGang(dayStem, dayBranch) {
  const dayGZ = dayStem + dayBranch;
  const match = KUIGANG_DAYS.find(k => k.name === dayGZ);
  if (match) {
    return { hit: true, type: match.type, name: match.name };
  }
  return { hit: false, type: '', name: '' };
}
```

### 3.3 四柱重逢检测（叠逢）

"魁罡四柱曰多同，贵气朝来在此中" — 检查年/月/时柱是否也出现魁罡干支：

```javascript
/**
 * 检测四柱中魁罡出现次数
 * @param {Object} fourPillars - { year, month, day, hour } 各含 { stem, branch }
 * @returns {{ count: number, positions: string[], isStacked: boolean }}
 */
function detectKuiGangOverlap(fourPillars) {
  const positions = [];
  const pillars = ['year', 'month', 'day', 'hour'];

  for (const pos of pillars) {
    const gz = fourPillars[pos].stem + fourPillars[pos].branch;
    if (KUIGANG_DAYS.some(k => k.name === gz)) {
      positions.push(pos);
    }
  }

  return {
    count: positions.length,
    positions,
    isStacked: positions.length >= 2  // 两柱及以上为"叠逢"
  };
}
```

### 3.4 成格与破格判定（关键！）

魁罡并非日柱命中就一定吉，需要结合命局整体分析：

```
┌─────────────────────────────────────────────────┐
│              魁罡成格/破格判定流程                  │
└──────────────────────┬──────────────────────────┘
                       │
              日柱是否魁罡四日？
              ┌────────┴────────┐
              ▼ 否              ▼ 是
         不入魁罡格        进入成格/破格分析
                              │
                    ┌─────────┴─────────┐
                    ▼                   ▼
              检查身旺衰          检查财官刑煞
                    │                   │
           ┌───────┴───────┐    ┌───────┴───────┐
           ▼               ▼    ▼               ▼
        身旺            身弱  无财官刑煞     有财官刑煞
        (加分)          (减分)  (成格)        (破格)
                              │               │
                              ▼               ▼
                         ✅ 魁罡成格      ⚠️ 魁罡破格
                         大贵之象         凶象加重
```

#### 身旺判定（简化版）

```javascript
/**
 * 简化判定日主是否身旺
 * 需结合月令、通根、生扶等因素
 * 此处给出判定接口，具体逻辑复用已有的 wuxing.js 模块
 */
function isShenWang(chart) {
  // 复用已有的 core/wuxing.js 中的身旺判定逻辑
  // 返回 { shenWang: boolean, score: number }
}
```

#### 财官刑煞检测

```javascript
/**
 * 检测命局中是否有魁罡忌讳的财官刑煞
 * @returns {{ hasWealth: boolean, hasOfficer: boolean, hasPunishment: boolean, hasSha: boolean, details: string[] }}
 */
function checkKuiGangTaboo(chart) {
  const result = {
    hasWealth: false,     // 见正偏财
    hasOfficer: false,    // 见正官/七杀
    hasPunishment: false, // 见刑冲
    hasSha: false,        // 见煞（如羊刃等）
    details: []
  };

  // 庚辰/庚戌日：忌见正官（丙丁火克庚金为官）
  // 戊戌日：忌见正偏财（壬癸水为财）
  // 壬辰日：忌见正偏财（丙丁火为财）

  // 具体判定逻辑：
  const dayStem = chart.fourPillars.day.stem;

  // 检查其余三柱的十神关系
  // 复用 core/wuxing.js 的十神计算
  // 若出现"正财""偏财""正官""七杀"则标记

  // 检查地支刑冲
  // 辰戌冲：若四柱中有辰又有戌且非魁罡日柱本身 → 刑冲
  // 其他三刑：寅巳申、丑未戌等

  return result;
}
```

### 3.5 综合评级

```javascript
/**
 * 魁罡综合评级
 * @returns {{ level: number, grade: string, description: string }}
 *   level: 0-5（0=不入格, 1=破格, 2=弱格, 3=普通, 4=成格, 5=叠逢大贵）
 */
function evaluateKuiGang(chart) {
  const detection = detectKuiGang(chart.fourPillars.day.stem, chart.fourPillars.day.branch);
  if (!detection.hit) {
    return { level: 0, grade: '无', description: '日柱非魁罡四日，不入魁罡格' };
  }

  const overlap = detectKuiGangOverlap(chart.fourPillars);
  const taboo = checkKuiGangTaboo(chart);
  const shenWang = isShenWang(chart);

  // 叠逢 + 身旺 + 无财官刑煞 → 大贵
  if (overlap.isStacked && shenWang && !taboo.hasWealth && !taboo.hasOfficer && !taboo.hasPunishment) {
    return { level: 5, grade: '魁罡叠逢·大贵', description: '叠叠相逢掌大权，身行旺地贵无伦' };
  }

  // 单逢 + 身旺 + 无财官刑煞 → 成格
  if (shenWang && !taboo.hasWealth && !taboo.hasOfficer && !taboo.hasPunishment) {
    return { level: 4, grade: '魁罡成格·贵', description: '不见财官刑煞并，身行旺地贵无伦' };
  }

  // 单逢 + 身旺 + 有轻微忌讳 → 普通
  if (shenWang && (!taboo.hasWealth || !taboo.hasOfficer)) {
    return { level: 3, grade: '魁罡入命·平', description: '魁罡入命，身旺尚可，略有波折' };
  }

  // 身弱 或 见财官 → 弱格
  if (!shenWang || taboo.hasWealth || taboo.hasOfficer) {
    return { level: 2, grade: '魁罡受制·弱', description: '身弱逢魁罡，或见财官，贵气受损' };
  }

  // 身弱 + 刑冲 + 煞并 → 破格
  if (!shenWang && taboo.hasPunishment && (taboo.hasWealth || taboo.hasOfficer)) {
    return { level: 1, grade: '魁罡破格·凶', description: '倘有刑冲兼破坏，一生彻骨受笞鞭' };
  }

  return { level: 3, grade: '魁罡入命·平', description: '魁罡入命，需结合大运流年综合论断' };
}
```

### 3.6 大运/流年遇魁罡

```javascript
/**
 * 检测大运/流年是否逢魁罡
 * 注意：运逢魁罡与命带魁罡含义不同
 */
function checkKuiGangInYun(dayunBranch, dayunStem) {
  const gz = dayunStem + dayunBranch;
  const match = KUIGANG_DAYS.find(k => k.name === gz);

  if (match) {
    return {
      hit: true,
      type: match.type,
      name: match.name,
      interpretation: '运逢魁罡，此运主权势变动，需谨慎行事'
    };
  }

  // 仅地支逢辰/戌也算逢魁罡位（非严格魁罡，但有影响）
  if (dayunBranch === '辰' || dayunBranch === '戌') {
    return {
      hit: true,
      type: '位',
      name: dayunBranch,
      interpretation: '运逢辰/戌魁罡之位，此运有权力变动之象'
    };
  }

  return { hit: false };
}
```

---

## 四、数据结构设计

### 4.1 检测结果数据模型

```typescript
interface KuiGangResult {
  /** 是否命中魁罡 */
  hasShensha: boolean;

  /** 魁罡类型: '罡'(辰) | '魁'(戌) | ''(未命中) */
  type: '罡' | '魁' | '';

  /** 魁罡日柱名称 */
  dayPillar: string;  // '庚辰' | '壬辰' | '庚戌' | '戊戌' | ''

  /** 四柱重逢信息 */
  overlap: {
    count: number;           // 魁罡出现次数 (0-4)
    positions: string[];     // 出现位置 ['day', 'year', ...]
    isStacked: boolean;      // 是否叠逢 (>=2)
  };

  /** 成格/破格评级 */
  evaluation: {
    level: number;           // 0-5
    grade: string;           // 等级名称
    description: string;     // 描述
  };

  /** 忌讳检测 */
  taboo: {
    hasWealth: boolean;      // 见财星
    hasOfficer: boolean;     // 见官星
    hasPunishment: boolean;  // 见刑冲
    hasSha: boolean;         // 见煞
    details: string[];       // 具体忌讳项说明
  };

  /** 身旺判定 */
  shenWang: {
    isWang: boolean;
    score: number;           // 身旺分数
    explanation: string;
  };

  /** 性格特质分析（基于魁罡类型） */
  personality: {
    traits: string[];        // ['刚毅果断', '权威过人', '疾恶如仇', ...]
    career: string;          // 事业倾向
    relationship: string;    // 婚姻感情倾向
    health: string;          // 健康倾向
  };

  /** 综合解读文案 */
  interpretation: {
    summary: string;         // 一句话概括
    detail: string;          // 详细解读
    suggestion: string;      // 建议
    genderNote: string;      // 性别差异说明
    tabooWarning: string;    // 忌讳警告（若有）
  };
}
```

### 4.2 大运/流年中的魁罡运

```typescript
interface KuiGangYunResult {
  /** 大运/流年干支 */
  stem: string;
  branch: string;

  /** 触发类型: '严格魁罡'(四日之一) | '魁罡位'(仅辰/戌) | '无' */
  type: 'strict' | 'position' | 'none';

  /** 是否与命局形成叠加 */
  isStackedWithMing: boolean;

  /** 解读文案 */
  interpretation: string;
}
```

---

## 五、前端渲染方案

### 5.1 命局日柱中的魁罡标注

在四柱排盘结果中，日柱位置高亮显示魁罡标记：

```
┌─────────────────────────────────────────┐
│              八字命局                      │
├──────┬──────┬──────┬──────┤
│  年柱  │  月柱  │  日柱  │  时柱  │
│  甲辰  │  丁丑  │  庚辰  │  丙子  │
│        │        │  ⚔️    │        │
│        │        │ [魁罡] │        │
└──────┴──────┴──────┴──────┘

神煞标注:
  ⚔️ 魁罡 · 庚辰（天罡） — 日柱庚辰入魁罡格
  叠逢: 无 | 评级: 魁罡成格·贵
```

### 5.2 魁罡详情卡片

在神煞信息区域展示独立的魁罡详情卡片：

```
┌──────────────────────────────────────────────┐
│  ⚔️ 魁罡命格分析                              │
├──────────────────────────────────────────────┤
│                                              │
│  类型: 天罡 · 庚辰日                           │
│  评级: ★★★★☆ 魁罡成格·贵                     │
│  身旺: ✅ 是（日主庚金得月令生扶）              │
│                                              │
│  ┌─ 成格条件 ─────────────────────────┐      │
│  │ ✅ 日柱庚辰 — 魁罡四日之一          │      │
│  │ ✅ 身旺 — 日主得令得地              │      │
│  │ ✅ 无财星透干 — 命局不见正偏财      │      │
│  │ ✅ 无官杀混杂 — 不见正官七杀并临    │      │
│  │ ✅ 无刑冲破坏 — 地支无辰戌冲        │      │
│  └──────────────────────────────────────┘      │
│                                              │
│  📖 解读:                                     │
│  魁罡成格，主人性格刚毅果断，行事雷厉风行。    │
│  具有领导才能，善于决策，权威过人。             │
│  身旺无破，运行旺地则百福骈臻。                │
│                                              │
│  💼 事业: 适合管理、司法、军警等权威领域       │
│  💑 感情: 性格刚强，需注意刚柔并济            │
│  🏥 健康: 体质强健，注意心脑血管              │
│                                              │
│  ⚠️ 忌讳:                                     │
│  忌见财官刑煞并临，一见财官祸患立至。          │
│  行运遇财官流年需谨慎。                       │
│                                              │
│  📚 出处: 《三命通会》《渊海子平》             │
└──────────────────────────────────────────────┘
```

### 5.3 视觉样式规范

```css
/* 魁罡标记 — 金/铜色系，象征权柄 */
.kuigang-badge {
  background: linear-gradient(135deg, #b8860b, #daa520);
  color: #fff;
  border: 1px solid #8b6914;
  border-radius: 4px;
  padding: 2px 8px;
  font-size: 12px;
  font-weight: bold;
  box-shadow: 0 0 6px rgba(218, 165, 32, 0.4);
  letter-spacing: 0.05em;
}

/* 魁罡叠逢 — 更醒目的金色光晕 */
.kuigang-stacked-badge {
  background: linear-gradient(135deg, #ffd700, #daa520);
  color: #3c2415;
  border: 2px solid #b8860b;
  border-radius: 4px;
  padding: 2px 10px;
  font-size: 13px;
  font-weight: bold;
  box-shadow: 0 0 12px rgba(255, 215, 0, 0.5);
  letter-spacing: 0.08em;
}

/* 魁罡破格 — 暗沉色 */
.kuigang-broken-badge {
  background: linear-gradient(135deg, #696969, #808080);
  color: #d3d3d3;
  border: 1px solid #555;
  border-radius: 4px;
  padding: 2px 8px;
  font-size: 12px;
  font-weight: bold;
}

/* 魁罡详情卡片 */
.kuigang-card {
  background: var(--bg-card);
  border: 1px solid var(--border-gold);
  border-left: 4px solid #daa520;
  border-radius: var(--radius-md);
  padding: 16px;
  margin: 12px 0;
}

/* 评级星标 */
.kuigang-stars {
  color: #ffd700;
  letter-spacing: 2px;
}
```

### 5.4 大运时间线中的魁罡标记

```
大运时间线:
  5岁   15岁   25岁   35岁   45岁   55岁
  ├──────┼──────┼──────┼──────┼──────┤
  丁卯    戊辰    己巳    庚午    辛未    壬申
                                    　     　
         　　   　　   　　   　　   　　

  ─── 若大运干支为庚辰/庚戌/戊戌/壬辰 ───

  5岁   15岁   25岁   35岁   45岁   55岁
  ├──────┼──────┼──────┼──────┼──────┤
  丙寅    丁卯    戊辰    己巳    庚辰    辛未
                               ⚔️
                            [魁罡运]
```

---

## 六、解读文案模板

### 6.1 命局解读文案

```javascript
const KUIGANG_INTERPRETATIONS = {
  // ─── 天罡（辰）───
  gengchen: {
    name: '庚辰日·天罡',
    summary: '庚辰日生人，命带天罡魁罡，刚毅果断，权威过人。',
    detail: `庚金坐辰土，金土相生，兼具水库的谋略与金气的锐利。
      主人性格刚直不阿，做事雷厉风行，善于决策，具有天生的领导才能。
      历史名将岳飞即为庚辰日生人，以果敢刚毅著称。
      身旺者运行旺地，百福骈臻；身弱或见财官，则贵气受损。`,
    traits: ['刚毅果断', '权威过人', '疾恶如仇', '善于决策', '领导才能'],
    career: '适合从事管理、司法、军警、监察等需要权威与决断力的领域',
    relationship: '性格刚强，在感情中易显强势，需学会刚柔并济',
    health: '体质强健，但需注意心脑血管健康，避免情绪波动'
  },

  renchen: {
    name: '壬辰日·天罡',
    summary: '壬辰日生人，命带天罡魁罡，深藏不露，内有乾坤。',
    detail: `壬水坐辰土，水入库中，象征深藏不露的权威力量。
      主人外表沉稳，内心有大志向，善于隐忍蓄势。
      壬骑龙背格若成，主大贵之象。
      忌见财星（火），见则魁罡受制，贵气减损。`,
    traits: ['深藏不露', '沉稳内敛', '蓄势待发', '志向远大', '善于隐忍'],
    career: '适合战略规划、研究、金融等需要深谋远虑的领域',
    relationship: '外表沉稳但内心强势，需注意沟通方式',
    health: '注意肾脏、泌尿系统健康'
  },

  // ─── 河魁（戌）───
  gengxu: {
    name: '庚戌日·河魁',
    summary: '庚戌日生人，命带河魁魁罡，火库爆发，果敢魄力。',
    detail: `庚金坐戌土，火库之中金气受炼，体现火库爆发的果敢魄力。
      主人行事果断，敢作敢当，有开拓进取之精神。
      庚戌日魁罡怕见正官（丙丁火），官星明现则祸患立至。
      身旺行运吉地，主掌权柄，威震四方。`,
    traits: ['果敢魄力', '敢作敢当', '开拓进取', '雷厉风行', '不畏艰难'],
    career: '适合创业、军事、执法等需要魄力和决断的领域',
    relationship: '性格强势，容易与伴侣产生摩擦，需培养包容心',
    health: '注意肺部、呼吸系统健康'
  },

  wuxu: {
    name: '戊戌日·河魁',
    summary: '戊戌日生人，命带河魁魁罡，火库决断，土性厚重。',
    detail: `戊土坐戌土，土气叠加，代表火库的决断与土性的厚重。
      主人性格沉稳厚重，但一旦决断则雷厉风行，不可动摇。
      戊戌日魁罡杀气最强，古诀云"戊戌庚辰杀最强"。
      忌见财星（水），见则破格。武则天即为戊戌日生人。`,
    traits: ['沉稳厚重', '决断果敢', '杀伐果断', '意志坚定', '权威感强'],
    career: '适合高层管理、政治、法律等需要权威与决断的领域',
    relationship: '过于强势，婚姻中需学会退让与包容',
    health: '注意脾胃消化系统健康'
  }
};
```

### 6.2 性别差异文案

```javascript
const KUIGANG_GENDER_NOTES = {
  male: {
    summary: '男命带魁罡，多能掌权立威，事业有成。',
    detail: '男命魁罡主刚毅果断，有领导才能，事业上容易取得成就。但需注意避免因强势性格引发人际冲突和家庭矛盾。',
    suggestion: '修身养性，刚柔并济，方能长久。'
  },
  female: {
    summary: '女命带魁罡，性格刚强于常人，需特别注意感情经营。',
    detail: '女命魁罡主人心性过于刚强，在婚姻感情中容易表现出强势的一面。古书有"女带魁罡，家败人散"之说，虽显极端，但反映了婚姻风险。不过命理是趋势不是定数，通过后天修养完全可以改善。',
    suggestion: '学会以柔克刚，婚姻中多一份包容与理解。'
  }
};
```

### 6.3 破格/弱格警告文案

```javascript
const KUIGANG_WARNINGS = {
  broken: {
    title: '⚠️ 魁罡破格',
    message: '命局财官刑煞并临，魁罡破格。古诀云"倘有刑冲兼破坏，一生彻骨受笞鞭"。需格外谨慎，遵纪守法，修身养性。',
    level: 'danger'
  },
  weak: {
    title: '⚠️ 魁罡受制',
    message: '日主身弱而逢魁罡，或命局见财官透干，魁罡贵气受损。身弱不能驾驭魁罡之刚猛，反受其害。',
    level: 'warning'
  },
  wealthTaboo: {
    title: '⚠️ 魁罡忌财',
    message: '命局见财星透干，犯魁罡之忌。戊戌日、壬辰日尤忌财星。古诀云"戊戌庚辰怕官显，戊戌壬辰畏财连"。',
    level: 'warning'
  },
  officerTaboo: {
    title: '⚠️ 魁罡忌官',
    message: '命局见正官/七杀透干，犯魁罡之忌。庚辰日、庚戌日尤忌官星。魁罡见官，祸患立至。',
    level: 'warning'
  }
};
```

---

## 七、完整算法流程图

```
┌─────────────────────────────────────────────────┐
│              输入: 八字排盘数据                     │
│   fourPillars + gender + wuxing/shenWang data    │
└──────────────────────┬──────────────────────────┘
                       │
          ┌────────────┴────────────┐
          ▼                         ▼
  ┌───────────────┐       ┌───────────────────┐
  │ 第一步:        │       │ 第二步:            │
  │ 日柱判定       │       │ 四柱重逢检测       │
  │               │       │                   │
  │ 日柱是否为     │       │ 年/月/时柱是否     │
  │ 庚辰/壬辰/    │       │ 也是魁罡四日之一   │
  │ 庚戌/戊戌？   │       │                   │
  └───────┬───────┘       └───────┬───────────┘
          │                       │
     ┌────┴────┐                  │
     ▼ 否      ▼ 是               │
  不入格   进入分析 ◄──────────────┘
              │
  ┌───────────┴───────────┐
  ▼                       ▼
┌───────────────┐  ┌───────────────┐
│ 第三步:        │  │ 第四步:        │
│ 身旺判定       │  │ 财官刑煞检测   │
│               │  │               │
│ 复用 wuxing.js │  │ 正偏财透干？   │
│ 判定日主旺衰   │  │ 正官七杀透干？ │
│               │  │ 地支刑冲？     │
└───────┬───────┘  └───────┬───────┘
        │                   │
        └─────────┬─────────┘
                  ▼
        ┌───────────────────┐
        │ 第五步: 综合评级    │
        │                   │
        │ 叠逢+身旺+无忌 → 5│
        │ 身旺+无忌     → 4│
        │ 身旺+轻微忌   → 3│
        │ 身弱或见财官   → 2│
        │ 身弱+刑冲+煞  → 1│
        └───────┬───────────┘
                ▼
        ┌───────────────────┐
        │ 第六步: 生成文案    │
        │                   │
        │ · 按四日类型选模板 │
        │ · 按性别调整语气   │
        │ · 按评级决定正/负面│
        │ · 附加忌讳警告     │
        └───────┬───────────┘
                ▼
        ┌───────────────────┐
        │ 第七步: 前端渲染    │
        │                   │
        │ · 日柱魁罡标记     │
        │ · 魁罡详情卡片     │
        │ · 评级星标         │
        │ · 成格条件清单     │
        │ · 忌讳警告         │
        └───────────────────┘
```

---

## 八、测试用例

### 8.1 标准测试用例

```javascript
// 用例1: 庚辰日 — 标准魁罡
{
  input: {
    fourPillars: { year: '甲辰', month: '丁丑', day: '庚辰', hour: '丙子' },
    gender: 'male'
  },
  expected: {
    hit: true,
    type: '罡',
    name: '庚辰',
    overlap: { count: 1, positions: ['day'], isStacked: false }
  }
}

// 用例2: 壬辰日 — 天罡
{
  input: {
    fourPillars: { year: '壬辰', month: '壬寅', day: '壬辰', hour: '乙巳' },
    gender: 'female'
  },
  expected: {
    hit: true,
    type: '罡',
    name: '壬辰',
    overlap: { count: 3, positions: ['year', 'month', 'day'], isStacked: true }
    // 注: 壬辰年月日三柱叠逢，但月柱壬寅不是魁罡（壬寅≠壬辰）
    // 修正: year=壬辰(命中), month=壬寅(未中), day=壬辰(命中)
    // overlap: { count: 2, positions: ['year', 'day'], isStacked: true }
  }
}

// 用例3: 戊戌日 — 河魁（杀气最强）
{
  input: {
    fourPillars: { year: '戊戌', month: '甲寅', day: '戊戌', hour: '丁巳' },
    gender: 'female'
  },
  expected: {
    hit: true,
    type: '魁',
    name: '戊戌',
    overlap: { count: 2, positions: ['year', 'day'], isStacked: true }
  }
}

// 用例4: 庚戌日 — 河魁
{
  input: {
    fourPillars: { year: '辛亥', month: '戊戌', day: '庚戌', hour: '丁亥' },
    gender: 'male'
  },
  expected: {
    hit: true,
    type: '魁',
    name: '庚戌',
    overlap: { count: 2, positions: ['month', 'day'], isStacked: true }
  }
}

// 用例5: 非魁罡日 — 不入格
{
  input: {
    fourPillars: { year: '甲子', month: '乙丑', day: '丙寅', hour: '丁卯' },
    gender: 'male'
  },
  expected: {
    hit: false,
    type: '',
    name: '',
    overlap: { count: 0, positions: [], isStacked: false }
  }
}

// 用例6: 大运遇魁罡
{
  input: {
    dayunStem: '庚',
    dayunBranch: '辰'
  },
  expected: {
    hit: true,
    type: 'strict',
    name: '庚辰'
  }
}

// 用例7: 大运仅逢辰位
{
  input: {
    dayunStem: '甲',
    dayunBranch: '辰'
  },
  expected: {
    hit: true,
    type: 'position',
    name: '辰'
  }
}
```

### 8.2 边界测试用例

```javascript
// 边界1: 四柱全部为魁罡日（极端叠逢）
// 边界2: 庚辰日 + 月柱丙火（正官透干）→ 破格
// 边界3: 戊戌日 + 时柱壬水（财星透干）→ 破格
// 边界4: 庚辰日 + 辰戌冲（年柱戌）→ 刑冲
// 边界5: 壬辰日 + 天乙贵人同柱 → 可化解部分凶象
// 边界6: 日柱庚辰但非庚辰（如庚辰年 ≠ 日柱）→ 不入格
```

---

## 九、与项目架构的集成方案

### 9.1 文件修改清单

```
core/shensha-data.js     ← 新增 KUIGANG_DAYS 常量 + KUIGANG_INTERPRETATIONS 文案
core/shensha.js          ← 新增 detectKuiGang() + evaluateKuiGang() 函数
core/bazi.js             ← 在 _computeCongGeData(ob) 中调用魁罡检测，注入 ob.kuigang
core/_render_funcs.js    ← 新增 _renderKuiGangCard(ob) 渲染函数，生成 HTML 卡片
web/css/bazi.css         ← 新增 .kuigang-badge / .kuigang-card 样式
```

### 9.2 数据流

```
core/bazi.js (计算)
  → ob.kuigang = {
      hasShensha: true,
      type: '罡',
      dayPillar: '庚辰',
      overlap: { count: 2, ... },
      evaluation: { level: 4, ... },
      taboo: { ... },
      personality: { ... },
      interpretation: { ... }
    }

core/_render_funcs.js (渲染)
  → _renderKuiGangCard(ob.kuigang)
  → HTML 字符串，注入 ob.bzinfo

前端 bazi-ui.js (展示)
  → 从 data.cards[] 中找到魁罡卡片
  → 渲染到神煞区域
```

### 9.3 遵循项目规范

- **共享常量唯一真相源**: 魁罡四日定义放在 `core/shensha-data.js`，不散落多处
- **服务端计算**: 魁罡检测全部在 `core/` 层完成，前端只负责渲染
- **CSS 变量**: 魁罡样式通过 `var(--kuigang-*)` 变量定义，暗色/亮色自动切换
- **五行色统一**: 魁罡金色通过 `variables.css` 中定义 `--color-kuigang: #daa520`

---

## 十、注意事项与实现要点

### 10.1 关键实现要点

1. **查法极其简单** — 魁罡只看日柱，不需要复杂的地支组合计算，但成格/破格判定需要结合身旺衰和财官刑煞
2. **身旺判定复用已有逻辑** — `core/wuxing.js` 中已有身旺判定，直接复用，不要重复实现
3. **叠逢是加分项** — 魁罡叠逢（>=2柱）主大贵，但不是必要条件
4. **性别差异很重要** — 女命魁罡的婚姻风险解读需要特别处理
5. **破格要醒目** — 魁罡破格（见财官刑冲）后果严重，前端需要醒目的警告样式
6. **大运流年区分** — 运逢魁罡与命带魁罡含义不同，需要分别渲染

### 10.2 与天罗地网的区别

| 特性 | 天罗地网 | 魁罡 |
|------|---------|------|
| 查法复杂度 | 中等（地支组合 + 纳音法） | 简单（仅日柱四日） |
| 吉凶属性 | 凶煞 | 吉神（但可破格变凶） |
| 成格条件 | 无（命中即算） | 有（身旺 + 无财官刑煞） |
| 性别差异 | 男忌天罗、女忌地网 | 女命更需注意婚姻 |

---

## 十一、参考文献

1. **《三命通会》卷六·魁罡** — [明] 万民英 著
   - "魁罡四日最为先，叠叠重逢掌大权。"
   - "壬辰庚戌与庚辰，戊戌魁罡四座神。不见财官刑煞并，身行旺地贵无伦。"
   - "庚戌庚辰怕官显，戊戌壬辰畏财连。"
   - "主人性格多聪慧，好杀之心断不偏。"
   - "倘有刑冲兼破坏，一生彻骨受笞鞭。"
2. **《渊海子平》卷五·魁罡诗诀** — [宋] 徐升 编
   - "魁罡重叠是贵人，天元健旺喜临身。"
   - "魁罡性严有操持，运行身旺发福，一见财官祸患立至。"
3. **《星平会海》** — 魁罡遇将星的双重性论述
4. **《壶中子》** — "辰为天罡，戌为河魁，乃阴阳绝灭之地"

---

## 附录A: 魁罡口诀速查

```
【魁罡总诀】
壬辰庚戌与庚辰，戊戌魁罡四座神。
不见财官刑煞并，身行旺地贵无伦。

【叠逢诀】
魁罡四日最为先，叠叠相逢掌大权。
魁罡四柱曰多同，贵气朝来在此中。

【忌讳诀】
庚戌庚辰怕官显，戊戌壬辰畏财连。
主人性格多聪慧，好杀之心断不偏。

【破格诀】
倘有刑冲兼破坏，一生彻骨受笞鞭。
日主独逢冲克重，财官显露祸无穷。

【身旺诀】
魁罡重叠是贵人，天元健旺喜临身。
运行身旺发福至，一见财官祸患临。

【女命诀】
女人有此星，心性过于刚强，
于婚姻有所影响，宜以柔克刚。
```

---

## 附录B: 魁罡四日速查表

| 日柱 | 天干 | 地支 | 魁罡类型 | 藏干 | 纳音 | 忌讳 | 特质关键词 |
|------|------|------|---------|------|------|------|-----------|
| 庚辰 | 庚金 | 辰土 | 天罡 | 戊乙癸 | 白蜡金 | 忌官星(丙丁火) | 刚毅果断、权威过人 |
| 壬辰 | 壬水 | 辰土 | 天罡 | 戊乙癸 | 长流水 | 忌财星(丙丁火) | 深藏不露、内有乾坤 |
| 庚戌 | 庚金 | 戌土 | 河魁 | 戊辛丁 | 钗钏金 | 忌官星(丙丁火) | 果敢魄力、火库爆发 |
| 戊戌 | 戊土 | 戌土 | 河魁 | 戊辛丁 | 平地木 | 忌财星(壬癸水) | 厚重决断、杀气最强 |
