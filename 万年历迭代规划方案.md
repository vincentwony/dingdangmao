# 万年历 / 八字 / 择日 程序梳理与迭代规划方案

> 梳理日期：2026-09-06
> 用途：用户主要用本程序 **择日看期** 与 **算命查八字**，硬性要求——**所有数据计算必须有真实计算依据与来源，不得凭空捏造**。
> 本报告据此对全程序做可信度审计，并给出分阶段的迭代路线图。
> 审计结论基于实际代码阅读（file:line 证据见正文），非泛泛而谈。

---

## 一、程序模块与数据来源现状

| 模块 | 核心实现 | 数据来源 |
|---|---|---|
| 万年历 / 干支 / 节气 | `core/engine/sxwnl-bundle.js`（寿星万年历天文引擎） | 天文历算（VSOP/ELP 级别精度） |
| 择日神煞 | `lunisolar` + `@lunisolar/plugin-thegods` + `core/shensha.js` + `server/routes/calendar.js` 覆盖层 | 协纪辨方书（已做 52 家族差分定正） |
| 宜忌（宜/忌事项） | `server/routes/calendar.js:_getLsrActs` → `lsr.theGods.getActs(1)` | **lunisolar theGods 插件**（第三方，未过覆盖层） |
| 八字四柱 | `core/engine/sxwnl-bundle.js:mingLiBaZi`（3287 行 `jd+=13/24`） | 天文历算 |
| 真太阳时校正 | `server/routes/bazi.js:46-218`（`_setSel2s` + EoT） | 《真太阳时修正技术参数.md》 |
| 喜用神 / 格局 / 从格 | `core/bazi.js`（1400-2960 行） | 子平真诠 / 滴天髓 / 穷通宝鉴 / 三命通会（典籍驱动） |
| 紫微斗数 | `server/routes/ziwei.js` + `web/js/ziwei-ui.js` | 待审计 |

---

## 二、可信度审计（核心：是否可溯源）

### ✅ 坚实可溯源（可放心使用）
1. **四柱干支**：`mingLiBaZi` 调天文引擎，年/月/日/时柱由真实历算得出（`sxwnl-bundle.js:3327-3330`）。
2. **真太阳时校正**：经度修正 `(120-λ)×π/180` + 时差方程 EoT，在路由层落地并修正时柱（`server/routes/bazi.js:46-179`）；依据文档 `真太阳时修正技术参数.md` 存在。
3. **节气时刻**：天文精确时刻（非粗略日期），决定换月/换岁，正确。
4. **神煞 52 家族**：已与《协纪辨方书》逐日差分定正（DIFF=0），含 6 项 value-bug 修复（`server/剩余神煞差分定正报告.md`）。
5. **建除十二神**：月建按节气（`core/shensha.js` 已验证）。
6. **喜用神 / 格局 / 从格**：`core/bazi.js` 以《子平真诠》格局驱动五神体系 + 《穷通宝鉴》调候表，**非魔法数字**；每个判定均引典籍原文（如 1488/1508/1520 行注释）。

### ⚠️ 依据存疑 / 流派差异 / 未充分验证（需处理）
1. **夏令时（DST）缺失** —— 全 live 代码（core/server/web，已排除 Back/build）**无任何 夏令时/DST 处理**，前端也无勾选项。1986-1991 夏出生者未减 1 小时 → **时柱（临界时辰）甚至日柱可能错**。
2. **子时流派固定**：当前用**早子时**（23:00 不起日柱换日，`boundary.test.js:88` 23:00→丁亥、23:30→戊子，时干取自当日日干）。未提供**晚子时（夜子时换日）** 选项，属流派差异未暴露给用户。
3. **宜忌未过验证覆盖层**：`getActs(1)` 直接调插件（`calendar.js:101-110`），**不经过** `_getLsrGods` 覆盖层 → 继承插件已知 bug（天德/月德合/鸣吠对/圣心/益后/续世），且**新增大红砂/小红砂未纳入宜忌判定**。
4. **57 项 NOREF 神煞**：尚未逐条锚定《协纪辨方书》卷九（`剩余神煞差分定正报告.md` 第七节清单）。多为固定数组/日干支列表，可信度待证。
5. **二十八宿吉凶、九星（九宫飞星）**：出处标注缺失，需在代码/文档补全 source。
6. **紫微斗数**：星曜庙旺、四化、南北派依据待专门审计。

### ❌ 实测"凭空捏造"项
- **未发现**。核心风险是「未验证」与「DST 缺失」，**不是编造**。已修 6 项 value-bug 属流派/索引错位，非捏造。
- 唯一结构性隐患：神煞代码注释中 **`source:` 字段为 0 处**，《》书名标注仅 11 处（`shensha.js`），可溯源信息未结构化。

---

## 三、关键缺口（按对你使用的影响排序）

| 优先级 | 缺口 | 影响 |
|---|---|---|
| **P0** | 宜忌未过覆盖层（继承插件 bug + 不含红砂） | 你每天看的「宜/忌」主输出可信度受损 |
| **P0** | 夏令时缺失 | 1986-1991 夏出生者八字可能整柱错 |
| **P1** | 57 NOREF 神煞未锚定协纪卷九 | 部分凶神/神煞可能不准 |
| **P1** | 子时流派不可选 | 23:00 前后出生者日柱/时柱流派争议 |
| **P2** | 二十八宿/九星出处标注、紫微审计 | 透明度与可溯源可见性 |
| **P2** | 神煞 source 字段结构化 | 长期可维护性 |

---

## 四、迭代路线图（分阶段）

### Phase 1 — 可信度收口（择日+八字主路径）· 预计 2-3 周
- **1.1 宜忌覆盖层（P0）**：将已验证神煞（含大红砂/小红砂）以「后置修正」注入宜忌输出，或重算宜忌时调用覆盖层真值。配套 `verify_yi_ji.js` 回归。
- **1.2 夏令时表（P0）**：内置 1986-1991 中国夏令时区间表，自动判定 + 手动勾选覆盖；补 `boundary.test.js` DST 用例。
- **1.3 子时流派开关（P1）**：早子时 / 晚子时（夜子时换日）可切换，结果标注所用流派。

### Phase 2 — 神煞全溯源 · 预计 2-4 周
- **2.1 57 NOREF 逐条锚定**：对照《协纪辨方书》卷九，写 `verify_noref2.js`，DIFF=0 才并入可信集合。
- **2.2 神煞 source 字段**：每条神煞补 `source:'《协纪辨方书》卷X'` 结构化字段（当前 0 处）。

### Phase 3 — 可溯源可见性 + 体验
- **3.1 出处 tooltip**：每个神煞/宜忌项悬浮显示「依据：《协纪辨方书》…」，让"有据可查"可见。
- **3.2 择日报告**：吉神/凶神/宜忌 + 依据 + 流派提示，一页导出。
- **3.3 八字计算明细**：展示真太阳时修正量、是否夏令时、所用子时流派，便于复核。

### Phase 4 — 工程治理
- **4.1 清理冗余**：`Back/`、`build/` 与 live 源码分离，避免旧代码混淆（已发现 Back/ 里有更早的 真太阳时实现，易误读）。
- **4.2 CI 补强**：宜忌回归 + DST 单测 + 子时单测 + 57 神煞锚定。
- **4.3 神煞来源库**：结构化 JSON（名称/规则/出处/流派），供前端与校验共用。

---

## 五、立即可做的优化（Quick Wins，低风险高杠杆）
1. 宜忌覆盖层（最高杠杆，直接提升每日主输出可信度）。
2. 夏令时最小实现（内置表，约 1 天）。
3. 子时开关（早/晚，约半天）。
4. 神煞 `source` 字段标注（边用边补）。

---

## 六、我的建议与待你确认
- **先收口 P0（宜忌覆盖层 + 夏令时）**：这两项直接影响你最常用功能（择日看期 / 查八字）的准确性，且实现可控、可回归。
- 需要你拍板两点：
  1. **晚子时**：是否要「晚子时（夜子时换日）」选项？（涉及 23:00 前后出生的日柱归属）
  2. **宜忌修正方式**：用「后置过滤/标注」还是「重算宜忌」？—— 我倾向后置修正（最小侵入、可回退，与现有覆盖层一致）。

> 确认后我按 Phase 1 顺序落地，每步带回归测试与（如适用）古籍出处注释，遵循你「交互后再修正」的约定。
