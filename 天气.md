# 天气模块渲染升级计划

## 1. 目标
将现有天气模块的渲染层升级为现代移动端可视化卡片模式，包含实时天气、动态指标、五日预报、温度趋势折线图，支持一键刷新模拟数据。

## 2. 组件清单
| 组件区域 | 功能描述 |
|---------|----------|
| 头部 | 城市名称、定位徽章、刷新按钮（带动画反馈） |
| 当前天气 | 大字号温度、天气图标、体感温度、高低温度 |
| 指标卡片 | 湿度（含进度条）、风速、降水概率，带图标 |
| 五日预报 | 水平滚动卡片（毛玻璃效果），显示星期、图标、高低温 |
| 温度趋势 | Canvas 折线图，展示未来5日高温变化曲线 |
| 数据刷新 | 模拟数据生成器，一键刷新所有UI |

## 3. 数据结构

### 当前天气对象
```typescript
interface CurrentWeather {
  currentTemp: number;   // 当前温度
  maxTemp: number;       // 最高温
  minTemp: number;       // 最低温
  feelsLike: number;     // 体感温度
  humidity: number;      // 湿度 0-100
  windSpeed: number;     // 风速 km/h
  pop: number;           // 降水概率 0-100
  condition: string;     // 天气文本
  iconClass: string;     // Weather Icons 类名
}
五日预报数组
typescript
interface ForecastDay {
  weekday: string;       // "今天","明天","周三"...
  highTemp: number;
  lowTemp: number;
  icon: string;          // wi 类名
  condition: string;
}
4. 样式规范
背景：天空蓝渐变 #b8e1fc → #d9f0ff

卡片：毛玻璃 backdrop-filter: blur(14px)，圆角 48px，白色半透明

容器：最大宽度 420px，居中，内边距 24px 22px

字体：无衬线，主色 #1f2e3a

图标库：Weather Icons + Font Awesome 6

5. 核心功能实现
5.1 湿度进度条
html
<div class="humidity-bar">
  <div class="humidity-fill" style="width: 65%;"></div>
</div>
5.2 横向滚动预报
css
.forecast-scroll {
  display: flex;
  gap: 14px;
  overflow-x: auto;
  scrollbar-width: thin;
}
5.3 Canvas 折线图
响应容器宽度，自动重绘

绘制网格线、数据点、温度标签

数据更新时重新调用绘图函数

5.4 模拟数据生成规则
天气类型权重：晴 40%，多云 25%，阴 15%，小雨 17%，雷阵雨 3%

温度基于天气类型波动（晴天 26-32°C，雨天 19-24°C）

湿度与天气关联（雨天 75-92%，晴天 35-55%）

预报温度根据前一天微调（变化 -3~+4°C）

6. 集成步骤
引入依赖（Weather Icons、Font Awesome）

复制 HTML 结构到天气容器

复制 CSS 样式（确保无冲突）

复制 JavaScript（数据生成、UI 更新、Canvas 绘图）

替换模拟数据为真实 API（修改 refreshAllData 函数）

映射真实天气代码到 Weather Icons 类名

测试移动端适配和响应式

7. 测试检查点
不同手机尺寸（375px~428px）显示正常

横向预报可滑动

湿度进度条宽度随数值变化

刷新按钮动画反馈，所有数据更新

Canvas 折线图在窗口旋转时重绘

毛玻璃效果在 iOS 上正常

8. 可选扩展
接入真实天气 API（如 OpenWeatherMap）

添加 24 小时逐小时预报

根据天气动态改变背景渐变