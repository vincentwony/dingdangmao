# Code Review 报告 — 日历月建/节气 + 月视图

> 评审对象：`web/js/calendar-ui.js`（`_renderJqTopBar` / `renderMonth` / `renderDayCell` / `renderEdgeCell` / `selectDay` + 点击委托）
> 评审日期：2026-07-15 ｜ 评审人：资深开发工程师（吴八哥）
> 对照：docs/CR-CHECKLIST.md + docs/CODE-QUALITY.md
> 性质：**实战评审**（走 CR 清单 + 边界交叉验证）

## 一、评审范围与重点
- 顶栏月建/节气逻辑（7/14 已修「月中取值」的方案 A，本轮验证其在边界下仍正确）。
- 月视图网格渲染、跨月边缘格、选中/收起状态机。
- 红 line B5（闰月/跨年/节气交界月建）边界覆盖实证。

## 二、CR 清单对照

### 算法与数值（B4–B6）
- ✅ **B5 边界覆盖 — 实证通过**：以 live API + 复刻 `_renderJqTopBar` 月中取值，对 10 个边界月验证「月建地支 == 节气所属干支月」：
  - 跨年：`2025-12`→子(大雪·冬至)、`2026-01`→丑(小寒·大寒)、`2026-12`→子、`2027-01`→丑 ✅
  - 闰月：`2025-07`(闰六月)→未(小暑·大暑) ✅ —— 农历闰月不污染干支月（节气月），逻辑正确
  - 早交节：`2026-02`→寅(立春·雨水)、`2026-03`→卯、`2026-06`→午、`2026-07`→未、`2026-08`→申 ✅
  - **结果：10/10 通过，0 失败**（脚本 `verify/_cal_month_audit.mjs`）。
- ✅ B4 交叉验证：月建以 `days[].gz.month` 为真值源，与节气（jieQi）逐月自洽；逻辑与 7/14 结论一致。
- ✅ B6 无重复实现：月中取值逻辑仅 `_renderJqTopBar` 一处，未重复。

### 前端交互（状态机）
- ✅ 点击委托（`cal3` click）：`other-month` 跨月格直接 `return`（不可点）✅；已展开日 `.detail-on` 再点 → 移除 `detail-on` + `detail-ui.hide()` + `return`（收起保留高亮）✅；新日 → `selectDay` 重设 `selected`/`detail-on` ✅。
- ✅ `selectDay` 先清除全部 `.selected`/`.detail-on` 再设目标，状态机无泄漏（跨日切换 A 收起 B 展开正确）。

### 前端（B7 令牌化）— 观察项（非阻断）
- ⚠️ `COLOR_GOOD='#c43d3d'` / `COLOR_BAD='#1e1e1e'`（`calendar-ui.js:42-43`）为模块常量，但未引用 神煞墙已建立的 `--ss-good-*` / `--ss-bad-*` 设计令牌（7/14 新增），两套「吉/凶」配色语言不统一（网格红/黑、神煞墙金/朱砂）。
- ⚠️ `renderDayCell` 月相/节气标记用内联硬编码 hex（`#404040`/`#F0A000`/`#888`/`#009000`）。属语义化数据色（朔暗/望金/弦灰/节气绿），但字面上仍是散落 `#xxxxxx`，与红线 10 字面要求有出入。
- **建议（非本次修改）**：将 吉凶/月相/节气 色统一抽到 `variables.css` 命名语义令牌（`--ss-good-*` 复用、`--moon-new`/`--moon-full`/`--moon-half`/`--jq-term`），并在暗色下给 朔(暗点) 提供可读底色，避免暗色不可见。属独立清理任务，不在本评审顺手改（B11 聚焦 + 防暗色回归）。

### 可维护性（B10–B12）
- ✅ B10 可还原：本轮**未改动任何代码**（仅验证 + 文档），无还原风险。
- ✅ B12 注释：`_renderJqTopBar` 含「节气交节必在月初，月中必已入当月节气所属干支月」注释，说明 why ✅。

## 三、结论
- **未发现确证功能性 bug**。月中取值在跨年/闰月/早交节边界下 10/10 正确；交互状态机（选中/收起/跨月格禁用）正确。
- B7 为**建议项**（令牌化清理），留作独立任务，不阻断合并。
- 可合并。

## 四、教学要点（对团队）
- 「修一次 + 边界验证固化」的复利：7/14 修的 月中取值，本轮用跨年+闰月 10 例重新印证仍 0 偏差——印证红线 8「历法边界必须覆盖」应配可重复验证脚本（`verify/_cal_month_audit.mjs` 即固化资产）。
- 语义化数据色 vs 主题色：红 line 10 针对「主题色」，月相/节气这类承载含义的数据色是否强制令牌化，团队可定一个明确口径（建议：一律令牌化，但令牌分「主题」与「语义」两组，语义令牌在暗色下也保证可读）。
