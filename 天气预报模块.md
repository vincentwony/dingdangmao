# 天地玄黄 · 天气预报功能集成指令

## 1. 项目背景与目标
- **当前项目**：天地玄黄日历（基于原生 HTML/CSS/JS 的单页应用）。
- **当前架构**：无后端，纯前端静态页面，使用 `lunisolar` 处理农历，`Tabler Icons` 处理图标。
- **任务目标**：在现有界面中集成“实时天气预报”功能。
- **核心要求**：
  - **零依赖**：不引入 Vue/React 或 jQuery，仅使用原生 JavaScript (ES6+)。
  - **风格统一**：UI 必须融入现有的“暖黄宣纸 + 墨金朱砂”古风设计。
  - **数据源**：使用和风天气 (QWeather) 免费版 API。

## 2. 技术实现规范

### 2.1 API 配置
- **服务商**：和风天气 (QWeather) 开发者免费版。
- **接口地址**：`https://devapi.qweather.com/v7/weather/now`
- **请求方式**：`GET` (Fetch API)
- **参数**：`location` (城市名/经纬度), `key` (API Key), `lang=zh`, `unit=c`

### 2.2 代码结构约束
- **模块化**：在现有的 `<script>` 标签内，新增一个全局对象 `WeatherModule` 来封装所有逻辑，避免污染全局命名空间。
- **初始化时机**：在 `DOMContentLoaded` 事件中调用 `WeatherModule.init()`。
- **容错处理**：
  - 如果 API 请求失败，显示“--”或默认文本，不要报错阻断页面渲染。
  - 优先读取页面中现有的城市选择器（`#Sel2`）的值；如果获取不到，默认使用“宜昌”。

### 2.3 UI/UX 设计规范
- **位置**：
  - **简略版**：在顶部工具栏 (`.bar`) 的时间显示旁，增加一个 `<span>` 显示图标和温度。
  - **详细版**（可选）：在 `#infoCards` 区域增加一个折叠卡片。
- **配色**：
  - 背景：使用 `rgba(196, 30, 10, 0.1)` (朱砂红微透) 或保持透明。
  - 文字：使用 `var(--text-primary)` 或 `#5D4037` (深褐色)。
- **图标**：必须使用项目中已引入的 `Tabler Icons` (例如 `<i class="ti ti-sun"></i>`)。

## 3. 详细开发步骤 (AI 执行指南)

请按以下步骤修改 `index.html` 文件：

### 步骤 1：修改 HTML 结构
在 `id="Clock1"` (时间显示) 的父级容器内，紧邻其后插入以下 HTML 代码：
```html
<!-- 天气简略显示区 -->
<span id="weather-badge" class="inline-value" style="margin-left: 15px; display: inline-flex; align-items: center; gap: 5px; font-size: 13px; color: var(--text-primary);">
    <i id="weather-icon" class="ti ti-cloud"></i>
    <span id="weather-text">--</span>
    <span id="weather-temp">--°C</span>
</span>
没问题，这就为你生成一份专为 AI 编程助手（如 Claude, Cursor, Copilot 等）设计的 CLAUDE.md 指令文件。
这份文件不仅包含了需求，还包含了代码约束、UI 规范和实现逻辑。当你把它放在项目根目录下，AI 就能完全理解你的意图，直接帮你写出能跑的代码。
你可以直接复制下面的内容保存为 CLAUDE.md：
markdown

编辑



# 天地玄黄 · 天气预报功能集成指令

## 1. 项目背景与目标
- **当前项目**：天地玄黄日历（基于原生 HTML/CSS/JS 的单页应用）。
- **当前架构**：无后端，纯前端静态页面，使用 `lunisolar` 处理农历，`Tabler Icons` 处理图标。
- **任务目标**：在现有界面中集成“实时天气预报”功能。
- **核心要求**：
  - **零依赖**：不引入 Vue/React 或 jQuery，仅使用原生 JavaScript (ES6+)。
  - **风格统一**：UI 必须融入现有的“暖黄宣纸 + 墨金朱砂”古风设计。
  - **数据源**：使用和风天气 (QWeather) 免费版 API。

## 2. 技术实现规范

### 2.1 API 配置
- **服务商**：和风天气 (QWeather) 开发者免费版。
- **接口地址**：`https://devapi.qweather.com/v7/weather/now`
- **请求方式**：`GET` (Fetch API)
- **参数**：`location` (城市名/经纬度), `key` (API Key), `lang=zh`, `unit=c`

### 2.2 代码结构约束
- **模块化**：在现有的 `<script>` 标签内，新增一个全局对象 `WeatherModule` 来封装所有逻辑，避免污染全局命名空间。
- **初始化时机**：在 `DOMContentLoaded` 事件中调用 `WeatherModule.init()`。
- **容错处理**：
  - 如果 API 请求失败，显示“--”或默认文本，不要报错阻断页面渲染。
  - 优先读取页面中现有的城市选择器（`#Sel2`）的值；如果获取不到，默认使用“宜昌”。

### 2.3 UI/UX 设计规范
- **位置**：
  - **简略版**：在顶部工具栏 (`.bar`) 的时间显示旁，增加一个 `<span>` 显示图标和温度。
  - **详细版**（可选）：在 `#infoCards` 区域增加一个折叠卡片。
- **配色**：
  - 背景：使用 `rgba(196, 30, 10, 0.1)` (朱砂红微透) 或保持透明。
  - 文字：使用 `var(--text-primary)` 或 `#5D4037` (深褐色)。
- **图标**：必须使用项目中已引入的 `Tabler Icons` (例如 `<i class="ti ti-sun"></i>`)。

## 3. 详细开发步骤 (AI 执行指南)

请按以下步骤修改 `index.html` 文件：

### 步骤 1：修改 HTML 结构
在 `id="Clock1"` (时间显示) 的父级容器内，紧邻其后插入以下 HTML 代码：
```html
<!-- 天气简略显示区 -->
<span id="weather-badge" class="inline-value" style="margin-left: 15px; display: inline-flex; align-items: center; gap: 5px; font-size: 13px; color: var(--text-primary);">
    <i id="weather-icon" class="ti ti-cloud"></i>
    <span id="weather-text">--</span>
    <span id="weather-temp">--°C</span>
</span>
步骤 2：编写 JavaScript 逻辑
在页面底部的 <script> 标签中，在 window.showToast 函数之后，添加以下 WeatherModule 代码：
// ================== 天气模块 (WeatherModule) ==================
const WeatherModule = {
    config: {
        apiKey: 'YOUR_QWEATHER_API_KEY', // 提示用户替换此处
        defaultCity: '宜昌',
    },
    
    // 初始化
    init: function() {
        const city = this.getCityFromUI() || this.config.defaultCity;
        this.fetchWeather(city);
    },

    // 获取 UI 中的城市
    getCityFromUI: function() {
        const sel2 = document.getElementById('Sel2');
        return sel2 ? sel2.options[sel2.selectedIndex].text : null;
    },

    // 请求数据
    fetchWeather: function(location) {
        const url = `https://devapi.qweather.com/v7/weather/now?location=${location}&key=${this.config.apiKey}&lang=zh&unit=c`;
        
        fetch(url)
            .then(res => res.json())
            .then(data => {
                if (data.code === '200') this.render(data.now);
            })
            .catch(err => console.warn('天气获取失败:', err));
    },

    // 渲染 UI
    render: function(data) {
        document.getElementById('weather-text').textContent = data.text;
        document.getElementById('weather-temp').textContent = data.temp + '°C';
        
        // 简单的图标映射逻辑
        const iconMap = { '晴': 'ti-sun', '多云': 'ti-cloud', '阴': 'ti-cloud-rain', '雨': 'ti-rain' };
        const iconClass = iconMap[data.text] || 'ti-cloud';
        const iconEl = document.getElementById('weather-icon');
        iconEl.className = `ti ${iconClass}`;
    }
};
步骤 3：绑定事件
在 document.addEventListener('DOMContentLoaded', ...) 内部，添加：
if (typeof WeatherModule !== 'undefined') {
    WeatherModule.init();
}
4. 注意事项
API Key 安全：在生成代码时，请保留 YOUR_QWEATHER_API_KEY 占位符，并添加注释提醒用户去和风天气官网申请。
跨域问题：提醒用户，如果直接双击打开 HTML 文件 (file:// 协议)，Fetch 请求可能会被浏览器拦截。建议使用 VS Code 的 "Live Server" 插件运行。
图标兼容性：确保映射的图标类名（如 ti-sun）存在于当前引入的 Tabler Icons 版本中。

---
