# 公信万年历 — 商业化授权方案

## 1. 背景

**当前产品形态：**

- **Web 版**：单文件 HTML（~9900 行），纯前端，使用 lunisolar 天文历算库
- **Android 版**：WebView 封装 HTML，打包为 APK（~4MB）
- **核心功能**：黄历日历、八字排盘、年历、地母经、每日记事、吉神凶神等

**商业化目标：** 客户安装后可选择「试用版」或输入序列号激活「完整功能版」。软件作者通过独立的「注册码生成器」根据客户机器码生成序列号。

---

## 2. 授权架构总览

```
┌─────────────────────────┐          ┌─────────────────────────┐
│     客户端（APP）        │          │    作者端（KeyGen）       │
│                         │          │                         │
│  1. 安装 → 生成机器码    │  机器码   │  3. 输入机器码            │
│  2. 显示机器码给客户     │ ──────→ │  4. 生成注册码            │
│  5. 客户输入注册码       │  注册码   │  5. 返回注册码给客户       │
│  6. 本地验签 → 激活      │ ←────── │                         │
└─────────────────────────┘          └─────────────────────────┘
```

**核心原则：**

- **非对称加密**：作者持有私钥（签名），客户端内置公钥（验签）
- **机器码**：基于设备硬件特征生成，不可逆推私钥
- **离线验证**：注册码一旦生成，客户端本地验签，无需联网
- **一机一码**：每个注册码绑定唯一设备，不可跨设备使用

---

## 3. 加密方案选型

### 3.1 推荐：RSA-2048 签名方案

| 环节 | 说明 |
|------|------|
| **密钥对生成** | 作者使用 OpenSSL 生成 RSA-2048 密钥对 |
| **私钥** | 仅作者持有，用于对机器码签名生成注册码 |
| **公钥** | 硬编码在客户端 HTML/APK 中，用于验签 |
| **注册码格式** | `机器码 + 签名` → Base64 编码 → 分组为易读的序列号格式 |

### 3.2 注册码格式设计

```
XXXX-XXXX-XXXX-XXXX-XXXX
│                      │
└─ 共20位，含机器码指纹 + RSA签名（截取）
```

实际传输格式（给客户的）：

```
机器码（客户看到）：
A1B2-C3D4-E5F6-G7H8

注册码（作者返回）：
K9J8-H7G6-F5D4-S3A2-X1Z0
```

---

## 4. 机器码生成算法

### 4.1 Web 版（浏览器环境）

由于浏览器沙箱限制，可获取的信息有限：

| 维度 | 数据源 | 说明 |
|------|--------|------|
| 屏幕指纹 | `screen.width × screen.height × devicePixelRatio` | 屏幕特征 |
| 时区 | `Intl.DateTimeFormat().resolvedOptions().timeZone` | 时区信息 |
| 平台 | `navigator.platform` | 操作系统 |
| 语言 | `navigator.language` | 浏览器语言 |
| Canvas 指纹 | Canvas 2D 渲染差异 | GPU/显卡特征 |
| 随机种子 | 首次安装时生成的 UUID（localStorage 持久化） | 安装实例标识 |

**生成流程：**
1. 收集上述信息 → 拼接为字符串
2. SHA-256 哈希 → 取前 48 bit
3. 转为 8 字符十六进制 → 格式化为 `XXXX-XXXX` 展示

### 4.2 Android 版（WebView + Android API）

通过 JavascriptInterface 暴露更多硬件信息：

| 维度 | 数据源 |
|------|--------|
| Android ID | `Settings.Secure.ANDROID_ID` |
| MAC 地址 | `NetworkInterface`（需处理 Android 12+ 限制） |
| 设备序列号 | `Build.getSerial()` |
| 硬件信息 | `Build.HARDWARE` + `Build.MODEL` + `Build.MANUFACTURER` |

**优先级：** Android 版优先使用 Android ID + 硬件信息；如果 WebView 桥不可用，降级为 Web 版的浏览器指纹方案。

### 4.3 机器码存储

生成后存储于：
- Web 版：`localStorage`（key: `_license_machine_id`）
- Android 版：`SharedPreferences`（key: `license_machine_id`）

一旦生成，终身不变。卸载 APP 后重新安装会生成新的机器码（因为随机种子丢失）。

---

## 5. 客户端验证流程

```
┌──────────────────────────────────────────────────┐
│                  APP 启动                          │
│                     ↓                             │
│         检查 localStorage 是否有激活记录            │
│                ↙              ↘                   │
│            已激活            未激活                 │
│              ↓                ↓                   │
│        正常使用       检查是否首次安装               │
│                      ↙          ↘                 │
│                   首次          非首次              │
│                    ↓              ↓                │
│             生成机器码      显示激活状态              │
│             进入试用模式    提供激活入口              │
│                    ↓              ↓                │
│              ┌─────────────────────┐               │
│              │     试用模式         │               │
│              │  · 功能受限          │               │
│              │  · 显示激活入口      │               │
│              │  · 可随时输入注册码  │               │
│              └─────────────────────┘               │
│                     ↓                             │
│            客户输入注册码                           │
│                ↓                                  │
│       RSA 公钥验签注册码中的签名                     │
│          ↙            ↘                           │
│       验签通过      验签失败                        │
│         ↓             ↓                           │
│   写入 localStorage  提示无效                       │
│   激活完整功能       重新输入                       │
│         ↓                                         │
│   正常使用（完整版）                                │
└──────────────────────────────────────────────────┘
```

---

## 6. 试用版 vs 完整版功能划分

| 功能 | 试用版（7天内） | 试用过期后 | 完整版（已激活） |
|------|:---:|:---:|:---:|
| 当日黄历（日历视图） | ✅ 当月 | ❌ 仅激活页 | ✅ 无限制 |
| 月历翻页 | ❌ 仅当月 | ❌ | ✅ 无限制 |
| 年历视图 | ❌ 仅当年 | ❌ | ✅ 无限制 |
| 八字排盘 | ❌ 禁用 | ❌ | ✅ 完整 |
| 八字保存/批注 | ❌ 禁用 | ❌ | ✅ 完整 |
| 每日记事 | ✅ 1条 | ❌ | ✅ 无限制 |
| 详情查看（吉神凶神等） | ✅ 当月 | ❌ | ✅ 无限制 |
| 地母经 | ✅ | ❌ | ✅ |
| 设置（暗色模式等） | ✅ | ✅ | ✅ |
| 激活入口 | 底部横幅 + 设置页 | 全屏激活页（唯一页面） | 设置页（查看状态） |

> **设计原则：** 试用期让用户看到产品价值（当月完整信息 + 7天倒计时），过期后强制激活。激活门槛低，用户想查其他月份或排八字时自然会想激活。

---

## 7. 作者端工具：注册码生成器（KeyGen）

### 7.1 形态

独立的 HTML 文件（`keygen.html`），作者在本地浏览器打开使用。无需服务器，私钥嵌入在文件中（作者妥善保管此文件）。

### 7.2 功能

```
┌─────────────────────────────────────┐
│         注册码生成器 v1.0            │
│                                     │
│  客户机器码：                        │
│  ┌─────────────────────────────┐    │
│  │ A1B2-C3D4-E5F6-G7H8        │    │
│  └─────────────────────────────┘    │
│                                     │
│  授权类型：○ 终身 ○ 年费（1年）       │
│                                     │
│  [ 生成注册码 ]                      │
│                                     │
│  注册码：                            │
│  ┌─────────────────────────────┐    │
│  │ K9J8-H7G6-F5D4-S3A2-X1Z0   │    │
│  └─────────────────────────────┘    │
│                          [ 复制 ]   │
│                                     │
│  ┌─ 生成记录 ────────────────────┐   │
│  │ 2026-05-24  A1B2.. → K9J8.. │   │
│  │ 2026-05-23  3F7A.. → B2D4.. │   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
```

### 7.3 技术实现

- 使用 Web Crypto API (`SubtleCrypto`) 或 jsencrypt 库进行 RSA 签名
- 私钥以 PEM 格式内嵌于 keygen.html
- 生成记录存储于 localStorage，方便作者回溯

---

## 8. 安全分析

### 8.1 防护层级

| 层级 | 措施 | 防护目标 |
|------|------|----------|
| 1 | 代码混淆/压缩 | 增加逆向难度 |
| 2 | 公钥硬编码 | 无法替换密钥 |
| 3 | RSA-2048 签名 | 无法伪造注册码 |
| 4 | 机器码绑定 | 一机一码，不可复用 |
| 5 | 激活状态存储在 localStorage | 防篡改（配合签名校验） |

### 8.2 已知风险 & 应对

| 风险 | 等级 | 应对 |
|------|------|------|
| 前端代码被逆向，公钥被提取 | 中 | 代码混淆；公钥泄露不影响私钥安全 |
| 用户伪造 localStorage 激活状态 | 中 | 激活状态附带签名，启动时重新验签 |
| 用户修改 JS 跳过验签逻辑 | 中 | WebView 端在 Android 层做二次校验 |
| 私钥泄露（作者端） | 高 | 私钥文件加密存储，物理隔离 |
| 同一注册码在多设备使用 | 低 | 机器码绑定天然防止 |

### 8.3 激活状态防篡改

激活后存储的数据结构：

```js
{
  machineId: "A1B2-C3D4-E5F6-G7H8",
  licenseKey: "K9J8-H7G6-F5D4-S3A2-X1Z0",
  signature: "<RSA签名(机器码+注册码)>",  // 每次启动验签
  type: "lifetime",        // 仅终身授权
  activatedAt: 1717123456,  // 激活时间戳
}
```

启动时：读取 localStorage → 用公钥验签 signature → 通过则正常使用，失败则回退试用模式。

---

## 9. 实施计划

### 阶段一：基础设施

- [ ] 1.1 生成 RSA-2048 密钥对（OpenSSL）
- [ ] 1.2 编写 `init-keygen.js`（一次性脚本：用密码加密私钥，输出加密私钥）
- [ ] 1.3 编写 `keygen.html`（作者端注册码生成器，含密码解锁 + 私钥解密 + RSA 签名 + 生成记录）
- [ ] 1.4 测试密钥对签名/验签完整流程
- [ ] 1.5 将公钥嵌入 `index.html`

### 阶段二：客户端授权模块

- [ ] 2.1 实现机器码生成函数（Web 版浏览器指纹）
- [ ] 2.2 实现 RSA 签名验证函数（Web Crypto API）
- [ ] 2.3 实现激活状态管理（存储/读取/启动验签/防篡改）
- [ ] 2.4 实现 7 天试用期管理（trialStart、剩余天数、时间倒流检测）
- [ ] 2.5 实现试用版功能限制（页面加锁、功能截断）
- [ ] 2.6 编写授权 UI（激活输入框、试用倒计时横幅、过期全屏激活页）
- [ ] 2.7 编写设置页内的授权管理（查看状态、剩余天数、重新激活入口）
- [ ] 2.8 预留联网校验 hook 函数（`onlineVerify`，默认返回 true）

### 阶段三：Android 增强

- [ ] 3.1 实现 JavascriptInterface 暴露硬件信息（AndroidID、Build 信息）
- [ ] 3.2 WebView 端优先使用 Android 硬件信息生成机器码
- [ ] 3.3 更新 APK 构建与测试

### 阶段四：测试与加固

- [ ] 4.1 端到端激活流程测试（安装 → 试用 → 过期 → 激活 → 完整版）
- [ ] 4.2 防篡改测试（清除 localStorage、修改激活状态、修改系统时间）
- [ ] 4.3 HTML + JS 代码压缩混淆
- [ ] 4.4 多设备兼容性测试

---

## 10. 关键技术点

### 10.1 RSA 密钥对生成（OpenSSL）

```bash
# 生成 RSA-2048 私钥
openssl genrsa -out private.pem 2048

# 导出公钥
openssl rsa -in private.pem -pubout -out public.pem

# 查看公钥内容（用于嵌入客户端）
cat public.pem
```

### 10.2 Web Crypto API 验签（客户端）

```js
// 将 PEM 公钥导入为 CryptoKey
async function importPublicKey(pem) { ... }

// 用公钥验签
async function verifySignature(publicKey, data, signature) {
  return crypto.subtle.verify(
    "RSASSA-PKCS1-v1_5",
    publicKey,
    signature,
    new TextEncoder().encode(data)
  );
}
```

### 10.3 签名生成（KeyGen 端）

```js
// 用私钥签名（keygen.html 中）
async function signMachineId(privateKey, machineId, licenseType) {
  const data = machineId + "|" + licenseType + "|" + Date.now();
  const signature = await crypto.subtle.sign(
    "RSASSA-PKCS1-v1_5",
    privateKey,
    new TextEncoder().encode(data)
  );
  return { data, signature: btoa(String.fromCharCode(...new Uint8Array(signature))) };
}
```

---

## 11. 文件清单

实施完成后，项目将新增以下文件：

| 文件 | 说明 |
|------|------|
| `keygen.html` | 作者端注册码生成器（含私钥，需妥善保管，不对外分发） |
| `public.pem` | RSA 公钥（备份用） |
| `private.pem` | RSA 私钥（仅作者持有，加密存储，绝不提交到版本库） |
| `h:/Phone/index.html` | 集成授权模块的主应用（修改已有） |
| `WannianliAndroid/.../LicenseBridge.java` | Android 硬件信息桥接（新增） |

---

## 12. 决策确认（已定案）

| # | 事项 | 决定 |
|---|------|------|
| 1 | 授权类型 | **仅终身授权**，不做年费 |
| 2 | 试用限制 | 功能限制 + **7 天时间限制**，以先到者为准 |
| 3 | 多档位定价 | **不搞多档位**，单一版本 |
| 4 | 联网校验 | **离线优先**，预留后台接口点位，需要时可启用 |
| 5 | 私钥保管 | **加密存储**，keygen.html 本身设访问密码，私钥以加密形态存放 |
| 6 | 发布形式 | **以 APK 为主**，Web 版的授权仅作为降级兼容 |

---

## 13. 试用期 7 天时间限制（补充设计）

### 13.1 时间存储

```js
{
  trialStart: 1717123456,  // 首次安装时间戳（存储在 localStorage）
  trialDays: 7,            // 试用天数
}
```

### 13.2 时间判定逻辑

```
APP 启动
  ↓
检查是否有激活记录 → 有 → 验签 → 激活完整版
  ↓ 无
检查 trialStart 是否存在
  ↓ 不存在               ↓ 存在
首次安装，记录 trialStart   计算 elapsed = now() - trialStart
进入试用模式                 ↓
                      elapsed > 7天？
                    ↙              ↘
                  是               否
           试用过期，强制激活      进入试用模式（功能限制）
           仅显示激活页面          底部显示剩余天数
```

### 13.3 防时间篡改

- 每次启动记录 `lastAccess` 时间戳
- 如果检测到 `now()` < `lastAccess`（时间倒流），标记为可疑，锁定试用
- 同时记录 `realElapsed`（累计使用时长），防止用户反复改系统时间

### 13.4 试用剩余天数显示

在底部标签栏上方显示横幅：

```
┌──────────────────────────────────────┐
│  🔒 试用版 · 剩余 5 天 · [ 激活 ]   │
└──────────────────────────────────────┘
```

---

## 14. 联网校验预留（补充设计）

### 14.1 预留接口点位

在代码中预留以下 hook 函数（默认返回 `true`，即跳过联网校验）：

```js
// 预留：联网校验入口
// 当前版本始终返回 true（离线验签通过即放行）
// 未来可替换为 fetch() 调用远程校验接口
async function onlineVerify(licenseKey, machineId) {
  // TODO: 替换为实际校验接口
  // const resp = await fetch('https://api.example.com/verify', {
  //   method: 'POST',
  //   body: JSON.stringify({ licenseKey, machineId })
  // });
  // return resp.ok;
  return true;
}
```

### 14.2 远程校验接口设计（未来）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/verify` | POST | 校验注册码是否有效、是否被列入黑名单 |
| `/api/activate` | POST | 记录激活事件（用于统计设备数） |

> 当前不实现，仅在代码中埋好调用点位。未来如需启用，只需修改 `onlineVerify` 函数实现即可。

---

## 15. keygen.html 私钥加密保管（补充设计）

### 15.1 方案

keygen.html 本身设访问密码：
- 打开 keygen.html 时，先要求输入解密密码
- 密码正确 → 用 Web Crypto API 解密出私钥 → 可以生成注册码
- 密码错误 → 拒绝进入

私钥以 AES-256-GCM 加密形态存储在 keygen.html 中（不是明文 PEM）。

### 15.2 工作流

```
打开 keygen.html
  ↓
输入解密密码
  ↓
AES-256-GCM 解密内嵌的加密私钥 → 得到 RSA 私钥
  ↓
输入客户机器码 → RSA 签名 → 生成注册码
  ↓
关闭页面后，内存中的私钥即消失
```

### 15.3 初始化脚本

提供一个独立的 `init-keygen.js` 脚本（Node.js），用于：
1. 读取 `private.pem`（RSA 私钥）
2. 提示作者设置一个解密密码
3. 用密码 → PBKDF2 派生 AES 密钥 → 加密私钥
4. 输出加密后的私钥（可直接粘贴到 keygen.html 中）

此脚本仅用一次，用完即销毁。

---

> 以上方案已按决策更新。确认后立即开始阶段一实施。
