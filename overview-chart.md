# 紫微斗数 12 宫命盘完成说明

## 已完成内容
- 按迁移宫参考面板的版式、颜色、字体、字号，生成了完整的 12 宫命盘。
- 使用实际推算数据（1982-5-10 12:20 男，阳历）。

## 关键产物
- `web/ziwei-chart.html`：独立自包含的完整命盘页面，数据内联，换盘只需替换 `DATA`。
- `H:/Phone/ziwei_chart_preview.png`：命盘渲染预览。

## 数据映射
- 顶部星耀：每颗星独立成列，主星红色、辅星（soft）紫色、煞星（tough）蓝色、桃花星（flower）红色、其余杂曜白色；庙旺以灰色显示在第三行。
- 中间：真实小限年龄列表 + 加粗的大限年龄范围。
- 左下角：博士/将前/岁前十二神，三行横排。
- 右下角：十二长生 + 宫干支（干支加粗），竖排。
- 底部：红色宫名；命宫用金色边框标识。

## 布局
- 标准 4×4 命盘方格，按地支定位，中心 2×2 留空。

## 复用方式
- 如需更换命盘：修改 `gen_chart.mjs` 中的出生信息，重新拉取 API 数据并生成；或直接编辑 `ziwei-chart.html` 中的 `DATA` 常量。
