# 紫微斗数 redesign — 文墨天机专业版对齐方案（截图修正版）

> 规划日期：2026-07-18 ｜ 规划人：高级开发工程师（吴八哥）
> 目标：推算逻辑 + UI 参照文墨天机专业版；新增 飞星 / 三合 / 四化 分析法与 天盘 / 地盘 / 人盘 盘别切换。
> 约束：文墨天机为闭源商业 App，无法读取其代码/像素；推算严格按仓库《紫微斗数推算方法_完全版.md》正统规则实现并校验，UI 按截图提供的文墨天机风格还原。

## 一、从截图提取的关键信息

参考图为 **文墨天机专业版（十三周年纪念版）** 默认命盘界面：

1. **盘面方向**：采用 **地支罗盘方位**（子北午南卯东酉西），十二宫沿 4×4 外圈顺时针排布；**命宫不固定左上**，而是按出生月时落在实际地支宫位（截图中命宫在酉位，右侧上格）。
2. **底部 Tab 栏**：`常用功能 | 飞星 | 三合 | 四化`，用于切换视图/分析法。
3. **左侧/上方运限切换**：截图顶部有 `大限 → 流年小限 → 流月 → 流日 → 流时` 的步进箭头；本应用当前左侧面板保留 `天盘/地盘/人盘`（对应本命/大限/流年）。
4. **中央信息区**：姓名、阳男/阴女、五行局、真太阳时、钟表时间、农历、命主/身主/生肖、四柱、节气四化、大限起运表等。
5. **宫位内部**：宫名+天干地支、大限年龄、小限年龄、主星+庙旺利陷、辅星、杂曜、博士十二神、长生十二神、四化角标。
6. **飞星**：宫干四化箭头与自化标记叠加在盘面上。

## 二、已拍板的 4 个决策

| # | 决策点 | 结论 |
|---|---|---|
| 1 | UI 参照程度 | **按文墨天机截图风格还原**（不保证逐像素，但结构、信息层级、配色方向一致） |
| 2 | 计算引擎 | **iztro 作排盘内核**；天文历/真太阳时复用八字已验证链路（不逆向文墨天机） |
| 3 | 三盘定义 | **天盘 = 本命盘**；**地盘 = 大限盘**；**人盘 = 流年盘**（时间递进三层） |
| 4 | 分析法呈现 | **底部 Tab 栏**：`[常用功能 | 飞星 | 三合 | 四化]`；左侧保留 `[天盘 | 地盘 | 人盘]` 盘别切换 |

## 三、现状 → 目标 差异

- 现状前端 `BRANCH_POS` 已是地支罗盘方位，与截图一致，**无需改为命宫固定左上**。
- 现状时间输入为「时辰下拉（0-12）」，需升级为「**时钟时间 HH:MM + 出生地级联 + 真太阳时开关**」（复用八字现有能力）。
- 现状左侧运限 tabs 文本为「本命/大限/流年」，需重命名为 **天盘/地盘/人盘**。
- 现状缺：底部 `[常用功能/飞星/三合/四化]` Tab 栏、方位文字、中央信息区扩展、宫位内辅助信息（大小限年龄、博士/长生十二神、庙旺利陷）、飞星/三合/四化叠加。

## 四、计算层设计（保留 iztro + 复用八字真太阳时）

### 4.1 排盘内核

- 沿用 `server/routes/ziwei.js` 的 iztro 调用；输入由「时辰序号」改为「公历日期 + HH:MM + 出生地经度 + 真太阳时开关」。
- 真太阳时换算：复用八字思路——经度修正 + EoT（Meeus 近似）→ 得到修正后的小时/分钟；若跨子正则日期 ±1；再用 `server/lib/time-util.js` 的 `toShiChenIndex(h,min)` 得时辰序号传 iztro。
- 农历路径仍走 `byLunar`（时辰由真太阳时修正后的时钟换算）。
- 不真太阳时（zty=false）时，直接以 HH:MM 算时辰序号，兼容旧行为。

### 4.2 天 / 地 / 人盘

- **天盘（本命）**：`/astrolabe` 原样（palaces[12]）。
- **地盘（大限）**：`/horoscope` 的 `decadal`，rename 展示为「地盘」。
- **人盘（流年）**：`/horoscope` 的 `yearly`，rename 展示为「人盘」。
- 三盘共用同一套 12 宫渲染，仅星曜集合/四化来源不同。

### 4.3 飞星（宫干四化 + 飞星箭头 + 自化）— 前端计算层（P2）

- 四化表常量（天干 → [禄,权,科,忌] 星名，《完全版》第六章）：
  ```
  甲:廉贞,破军,武曲,太阳  乙:天机,天梁,紫微,太阴  丙:天同,天机,文昌,廉贞
  丁:太阴,天同,天机,巨门  戊:贪狼,太阴,右弼,天机  己:武曲,贪狼,天梁,文曲
  庚:太阳,武曲,太阴,天同  辛:巨门,太阳,文曲,文昌  壬:天梁,紫微,左辅,武曲
  癸:破军,巨门,太阴,贪狼
  ```
- 算法：遍历 12 宫，取各宫 `heavenlyStem` → 查表得禄/权/科/忌对应星名 → 在 natal palaces 中定位该星所在宫 index = 飞入宫；若就在本宫 → 标记「自化」。
- 产出：`flyingStars: [{from, to, type, star, self}]`。
- 渲染：SVG 叠加层在方盘上画弧形箭头，自化用环形标记。

### 4.4 三合（三方四正）— 前端几何层（P2）

- 三方 = 本宫 + (本宫+4) + (本宫+8)；四正 = 对宫（本宫+6）。
- 产出 `sanheLinks`，SVG 连线高亮。

### 4.5 四化（本命四化）— 已具备

- iztro 每颗星 `mutagen` 字段直接高亮 + 角标；「四化」Tab 显示年干四化表卡片。

### 4.6 数据契约（API 变更）

- `POST /api/v1/ziwei/astrolabe`：入参新增 `hour, min, jd, wd, zty`（替换原 `time`；后端内部换算时辰）。
- `POST /api/v1/ziwei/horoscope`：入参同上加 `targetYear`。
- 出参扩展 `meta.clockTime / meta.trueSolarTime / meta.location / meta.fourPillars` 等字段，供中央信息区使用。

## 五、UI 层设计（文墨天机风格方盘）

### 5.1 方盘布局（保留 BRANCH_POS）

```
巳 午 未 申
辰 □□ 酉
卯 □□ 戌
寅 丑 子 亥
```

- 4 列 × 4 行 grid；中央 4 格合并为信息区。
- 宫位按 `palace.earthlyBranch` 落入对应地支格；宫名沿十二宫顺数自然形成。
- 外圈增加方位文字：正南方（上中）、南偏西（右上）、正西方（右中）、北偏西（右下）、正北方（下中）、北偏东（左下）、正东方（左中）、南偏东（左上）。

### 5.2 左侧盘别切换

- 文本改为：`[天盘] [地盘] [人盘]`。
- 天盘 = native；地盘 = decadal；人盘 = yearly。

### 5.3 底部分析法 Tab 栏

- `[常用功能 | 飞星 | 三合 | 四化]`。
- 常用功能：显示完整命盘 + 中央信息卡。
- 飞星/三合/四化：P1 先完成 Tab 壳与基础内容（四化表），P2 叠加飞星箭头与三合连线。

### 5.4 中央信息区

P1 至少包含：
- 姓名 / 性别（阳男/阴女） / 五行局
- 钟表时间 / 真太阳时 / 农历
- 四柱（年柱/月柱/日柱/时柱）
- 命主 / 身主 / 生肖
- 大限起运表（出生后几岁起运、各宫起止年龄）
- 节气四化（节令天干四化）

### 5.5 宫位卡片增强

- 宫名 + 天干地支（如 丙 午）
- 大限年龄范围 + 小限年龄
- 主星 + 庙旺利陷 + 四化角标
- 辅星 / 杂曜
- 博士十二神 / 长生十二神

### 5.6 主题与响应式

- 明暗主题复用现有 `--color-*` token（宣纸底 + 朱砂红 + 鎏金）。
- ≤860px：左栏表单移上方，方盘全宽。

## 六、实施分期（每期过 L0 语法 / L1 启动 安全阀）

### P1 表单升级 + 方位布局 + 底部 Tab 壳 + 中央信息区

- 后端：`ziwei.js` 入参升级（hour/min/jd/wd/zty），真太阳时换算，扩展 meta。
- 前端：`ziwei-ui.js` 表单改 HH:MM + 出生地级联 + 真太阳时开关；左侧运限改名天/地/人盘；外圈方位文字；扩展中央信息卡；底部 Tab 壳。
- `ziwei.css`：方位文字、Tab 栏、信息卡、宫位内部辅助信息样式。
- 验证：node --check + smoke + 浏览器核对。

### P2 飞星 / 三合 / 四化叠加

- 前端：四化表常量、飞星计算、SVG 箭头/自化、三合连线。
- 底部 Tab 三个分析法真正生效。

### P3 视觉打磨

- 配色/字号/间距按文墨天机观感精修；宫位 hover 详情增强；移动端单栏体验。

### P4 校验 + 文档

- 新增 `server/verify_ziwei_feixing.js`：用《完全版》四化表断言飞星结果。
- 扩展 smoke 测试：断言 Tab 渲染、三盘切换。
- `npm run check` 全绿；present 预览。

## 七、风险与对策

| 风险 | 对策 |
|---|---|
| 真太阳时换算错 → 时辰错 → 全盘错 | 复用八字已验证的经度+EoT公式；P1 后用已知命盘抽样核对 |
| 地支方位宫序排错 | 固定 `BRANCH_POS` 常量 + P1 肉眼核对（子宫在北/午宫在南） |
| 底部 Tab 与左侧盘别交互混淆 | 明确语义：左侧换「盘别」，底部换「视图/分析法」 |
| 改动拖垮日历/八字 | 全 `.zw-` 作用域；L0/L1/L1.5 安全阀保护 |

## 八、我替你定的小决策（可随时纠正）

1. **飞星/三合 默认落在天盘（本命）上**；若需叠地/人盘再说。
2. **真太阳时默认关**，开启后需选出生地；与八字一致。
3. **时间输入改为 HH:MM**（不再保留时辰下拉），以便真太阳时精确修正。
4. **iztro 沿用 JS 版**（后端已是 Node + HMAC），零额外成本。
5. **飞星/三合计算放前端**（不新增 API），后端仅保证 palaces 数据完整。
