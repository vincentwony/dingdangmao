# 公信万年历 — 商业化部署落地指南

> 2026-05-29 | 所有方案均不涉及被风控的境外网站

---

## 一、整体架构（纯离线 + 可选后端）

```
客户手机 App                    你的电脑/VPS
┌─────────────┐               ┌──────────────┐
│ index.html  │  ←激活码验证→  │ server-keygen.js │
│ (HMAC离线)  │               │ (Node.js 发码)   │
└─────────────┘               └──────────────┘
                                     ↑
                              你手动输入机器码
                              选择等级 → 生成激活码
                                     ↓
                              微信/QQ 发回给客户
```

激活码格式：`BZ-Y-20270529-A3F7`（短码，客户手动输入很方便）

---

## 二、立即可用：本地发码（零部署）

### 步骤 1：启动本地发码服务

打开终端，进入项目目录：

```bash
cd h:\Phone

# 安装依赖（仅首次）
npm install express cors

# 启动服务
node server-keygen.js
```

看到如下输出即成功：

```
====================================
  公信万年历 · 发码服务已启动
  Web 管理页: http://localhost:3456/admin
  API:        http://localhost:3456/api/generate
  验码:       http://localhost:3456/api/verify
====================================
```

### 步骤 2：用 Web 管理页生成激活码

1. 浏览器打开 `http://localhost:3456/admin`
2. 界面显示：机器码输入框 + 等级下拉框 + 生成按钮
3. 粘贴客户发来的机器码（如 `A3F7-B8C2-D1E9`）
4. 选择对应等级（季卡/年卡/至尊卡）
5. 点击「生成激活码」→ 得到 `BZ-Y-20270529-A3F7`
6. 复制发给客户

### 步骤 3：客户激活

1. 客户打开 App → 点击设置 → 点击「升级会员」
2. 看到定价页 → 选择等级 → 复制机器码发给你
3. 你生成激活码发回
4. 客户在设置中粘贴激活码 → 即时解锁

---

## 三、构建发布版 APK

### 一次性准备环境

```bash
# 设置 JDK 路径
export JAVA_HOME="C:/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot"
export ANDROID_HOME="C:/Users/Administrator/AppData/Local/Android/Sdk"
```

### 每次发布执行

```bash
# 1. 构建 Release HTML（剥离注释、压缩）
node h:/Phone/build-release.js

# 2. 同步到 Android 资源
cp h:/Phone/build/index.release.html h:/Phone/WannianliAndroid/app/src/main/assets/www/index.html

# 3. 构建 APK
"C:/Users/Administrator/.gradle/wrapper/dists/gradle-8.5-bin/5hry6tgzq0wontdz18qo6fdj9/gradle-8.5/bin/gradle" -p h:/Phone/WannianliAndroid assembleRelease

# 4. 签名 APK
powershell -Command "
\$env:JAVA_HOME = 'C:\Program Files\Eclipse Adoptium\jdk-17.0.19.10-hotspot'
\$env:ANDROID_SDK = 'C:\Users\Administrator\AppData\Local\Android\Sdk'
& \"\$env:ANDROID_SDK\build-tools\34.0.0\apksigner\" sign --ks h:\Phone\WannianliAndroid\release.jks --ks-pass pass:12345688 --out h:\Phone\WannianliAndroid\app\build\outputs\apk\release\app-release.apk h:\Phone\WannianliAndroid\app\build\outputs\apk\release\app-release-unsigned.apk
"

# 5. APK 输出位置
# h:\Phone\WannianliAndroid\app\build\outputs\apk\release\app-release.apk
```

---

## 四、生产环境：VPS 部署（国内云服务器）

### 推荐：阿里云/腾讯云轻量应用服务器

配置要求：1核1G即可，CentOS 7+ 或 Ubuntu 20.04+

### 服务器端操作

```bash
# 1. 登录服务器，安装 Node.js
curl -fsSL https://mirrors.aliyun.com/nodejs-release/v20.11.0/node-v20.11.0-linux-x64.tar.xz -o node.tar.xz
tar -xf node.tar.xz
mv node-v20.11.0-linux-x64 /usr/local/node
echo 'export PATH=/usr/local/node/bin:$PATH' >> ~/.bashrc
source ~/.bashrc

# 2. 验证
node --version   # 应显示 v20.11.0

# 3. 上传项目文件（在本地执行，替换 YOUR_SERVER_IP）
scp h:/Phone/server-keygen.js root@YOUR_SERVER_IP:/opt/wannianli/
scp h:/Phone/keygen.html root@YOUR_SERVER_IP:/opt/wannianli/
scp h:/Phone/package.json root@YOUR_SERVER_IP:/opt/wannianli/

# 4. 在服务器上安装依赖
ssh root@YOUR_SERVER_IP
cd /opt/wannianli
npm install express cors

# 5. 设置环境变量（修改默认密码！）
export API_KEY="your-strong-api-key-here"
export PORT=3456

# 6. 启动服务
nohup node server-keygen.js > /var/log/wannianli.log 2>&1 &

# 7. 配置 Nginx 反向代理（可选，加域名+SSL）
# 安装 nginx
apt install nginx -y   # Ubuntu
# 或 yum install nginx -y   # CentOS

# 配置反向代理
cat > /etc/nginx/conf.d/wannianli.conf << 'EOF'
server {
    listen 80;
    server_name your-domain.com;   # 替换为你的域名

    location / {
        proxy_pass http://127.0.0.1:3456;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
EOF

nginx -s reload
```

### 日常使用

1. 浏览器打开 `http://your-server-ip:3456/admin`（或域名）
2. 输入客户机器码 → 选择等级 → 生成 → 发回客户

---

## 五、支付收款流程（当前阶段）

### 推荐流程（零平台依赖）

```
1. 客户看到定价页 → 截图联系你
2. 微信/支付宝个人转账（你的个人收款码）
3. 你确认收款 → 打开发码管理页
4. 输入客户机器码 → 选择对应等级 → 生成激活码
5. 微信/QQ 发回激活码
6. 客户粘贴激活 → 完成
```

### 半自动化（微信机器人 + 发码服务 API）

如果后续需要自动化，可以写一个简单的微信机器人脚本：

```bash
# POST 调用发码 API（你有 API Key）
curl -X POST http://localhost:3456/api/generate \
  -H "Content-Type: application/json" \
  -H "X-API-Key: your-api-key" \
  -d '{"machineId":"A3F7-B8C2-D1E9","tier":"Y"}'

# 返回
{"success":true,"code":"BZ-Y-20260529-A3F7","tier":"年卡","expireDate":"20260529"}
```

---

## 六、测试验证

### 本地自测

1. 用 Chrome 打开 `h:/Phone/index.html`
2. 查看试用横幅 → 点击 → 进入定价页
3. 记下机器码
4. 打开 `http://localhost:3456/admin`
5. 输入机器码，选择「年卡」→ 生成激活码
6. 回到 index.html → 设置 → 粘贴激活码 → 验证
7. 应显示「年卡 · 剩余 365 天」
8. 八字 → 定格/喜用/从格按钮应可点击（年卡解锁）

### 检查清单

| 测试项 | 预期结果 |
|--------|---------|
| 首次打开 | 显示试用版横幅（7天） |
| 点击八字定格按钮 | 弹出定价页（试用版无此功能） |
| 输入无效激活码 | 显示「校验码不匹配」 |
| 输入有效激活码 | 显示「激活成功」 |
| 激活后横幅 | 显示「年卡 · 剩余 365 天」 |
| 激活后八字定格 | 正常显示格局分析 |
| 过期后 | 自动降级为免费版，高级功能锁定 |

---

## 七、文件清单

| 文件 | 用途 | 部署位置 |
|------|------|---------|
| `index.html` | 主应用（含授权验证） | APK 内 / Web 服务器 |
| `server-keygen.js` | 发码服务 | 你的电脑或 VPS |
| `keygen.html` | 离线激活码生成器 | 浏览器直接打开 |
| `worker-deno.js` | Deno Deploy 备用 | 如需要在线发码 |
| `build-release.js` | 构建脚本 | 本地执行 |
| `商业化升级.md` | 功能分级方案 | 参考文档 |

---

## 八、QA

**Q: 客户不联网能用吗？**
A: 能。激活码离线验证（HMAC），不需要联网。首次激活后永久生效。

**Q: 激活码被客户分享怎么办？**
A: 激活码绑定机器码（硬件指纹），换设备无效。至尊卡最多3台设备（后续版本）。

**Q: 怎么防止时间回退破解？**
A: 已内置时钟回退检测，回退超过1分钟自动锁定。

**Q: 旧版 RSA 注册码还能用吗？**
A: 能。系统兼容新旧两种格式。
