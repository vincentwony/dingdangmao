# 2026-07-12 工作记录

## 天德/月德/天赦 推算修正（万年历项目 H:\Phone）

### 已修复的 Bug（三处）
1. **shensha.js `isTiandeDay2`**：原用 `td.length===1` 区分天干/地支，天干"丁"与地支"申"length 同为1 → 永远 true，四仲月地支型天德永远无法匹配。改用 `_GAN_SET` 集合判断。已新增 `isTiandeHeDay2`/`isYuedeHeDay2` 并导出。
2. **lunisolar 天德 `isStem` 逻辑反转**：`s=!(t%3)` → 改为 `s=!!(t%3)`（四仲月子午卯酉 t%3===0 应比地支）。
3. **lunisolar 月德合数组越界+值错**：`[3,1,8,5]` → `[3,1,7,5]`（寅午戌月应取辛=7 而非壬=8）。
   - 9 个 dist 副本（server/node_modules、node_modules、core/node_modules × index.js/index.mjs/index.iife.js）全部已 patch。

### 关键发现：天德 同名碰撞
- lunisolar `getGoodGods()` 中的"天德"是**黄道十二神·天德**（按日支循环，与干支神煞天德同名，是不同神煞）。
- 原 `_getLsrGods` 覆盖逻辑在"干支神煞天德=false"时会 `good.splice` 误删合法的**黄道天德**。
- **修复**：覆盖逻辑对"天德"改为**只增不删**（ADD-only），保留黄道天德；其余四项（天德合/月德/月德合/天赦）仍完整增删（不与黄道同名，安全）。

### 验证
- `server/verify_fix.js`：2026 全年 365 天，黄道天德零误删(0)、神煞天德全补入(36/36)、月德/月德合/天德合/天赦零误判。✅
- 服务已重启（managed node 22.22.2，端口 3000，`[server] listening on port 3000` 正常）。

### 规则文档
- 重写 `H:\Phone\WannianliAndroid\天德月德天赦推算规则.md`：消除 §2.3/§3.2/§7/§9 内部矛盾；明确**四仲月(子午卯酉)天德落地支、比日支、天德合为空**的核心规则。以《协纪辨方书》口诀为唯一基准，总表与 shensha.js 映射完全一致。

### 架构要点（备忘）
- 前端徽章 `isTiande/isYuede/isTianshe` 用 `computed.*`（正确，来自 shensha.js）。
- 详情页神煞列表用 `computed.tiande/tiandeHe/yuede/yuedeHe/tianshe`（正确）。
- `shenSha` 日格列表 = lunisolar good+bad（已覆盖修正）。
- API 需 HMAC 认证（中间件返回 401），本地直连需带认证参数。

## 剩余 ~120 神煞差分定正（续）

### 结论：未再发现「值错误」类 bug
- 提取运行时宇宙：MD（App 展示）实际 137 神煞；YMD（含年神）188 神煞。
- 对 24 项高置信家族逐日差分（月支偏移/三合局/月厌体系/日支集合/王日）：**DIFF = 0**，全部与协纪一致。
- 年神静态校验 12 项全对：劫煞/灾煞/岁煞（三煞）、黄幡、豹尾、灸退、大煞(年)、岁破、岁刑、岁马，外加**飞廉**以《广圣历》原文逐条锚定（子申丑酉寅戌卯巳辰午巳未午寅未卯申辰酉亥戌子亥丑）与 lunisolar 数组完全一致。
- 88 项 NOREF（数组/日干支/阴阳错/黄道）未逐条锚定，建议后续对照协纪卷九逐条做最终差分；预期亦正确。

### 关键教训（再次印证）
从记忆重建协纪参考源极易误判：本次初判"天火/九空/月厌/天狗/飞廉有 bug"，经探针提取 lunisolar 真值 + 协纪原文锚定后，全部确认 lunisolar 正确（天火=中支对冲、九空=墓位对冲、月厌=月厌表、天狗=申月甲戌日 value10、飞廉=逆推四生/墓方）。唯一确证 bug 仍是前序 3 项。

### 交付物
- `server/verify_remaining_final.js`（`npm run test:remaining`，DIFF=0）、`server/剩余神煞差分定正报告.md`、`server/probe_*.js`、`server/remaining_dump.txt`。
- lunisolar `C(...,4,"year"/"month")` 三合局索引序 = `[申子辰, 巳酉丑, 寅午戌, 亥卯未]`（亥卯未/巳酉丑 在 idx3/idx1，与直觉相反）。
- CI 全绿：`test:gods`/`test:override` exit 0；服务主页 `:3000/` HTTP 200。本回合无代码改动（无新 bug 需修），服务保持运行。

---

## 第二批：圣心/益后/续世 值错误（覆盖层修复）

### 新发现 3 项值错误（安装版 dist 月支索引错位）
提取**安装版** `node_modules/@lunisolar/plugin-thegods/dist/index.js` 真实 C 数组（probe_arrays.js）确认，与旧 `remaining_dump.txt` 不同（旧 dump 这三项是错的"正确值"，不可信）：
- **圣心** 实际 `[4,10,11,5,0,6,1,7,2,8,9,3]`；协纪正确 `[4,10,11,5,0,6,1,7,2,8,3,9]`（亥月/子月 卯↔酉 互换）。
- **益后** 实际 `[5,11,0,6,1,7,2,8,9,3,4,10]`；协纪正确 `[5,11,0,6,1,7,2,8,3,9,4,10]`（酉月/戌月 寅↔申 互换）。
- **续世** 实际 `[0,6,1,7,2,8,9,3,4,10,5,11]`；协纪正确 `[6,0,1,7,2,8,3,9,4,10,5,11]`（子月/丑月 与 午月/未月 两组互换；且违背考原「续世=益后+1」）。

### 修复方式（非破坏性覆盖层，沿用项目架构）
- 在 `server/routes/calendar.js` 顶部新增协纪正确数组 `SHENGXIN_REF/YIHOU_REF/XUSHI_REF`（月支下标 0-11 子→亥，值=日支序号）。
- 在 `_getLsrGods` 中由 `computed.monthGZ[1]`（月支）、`computed.dayGZ[1]`（日支）计算布尔真值，对圣心/益后/续世按协纪**全量强制覆盖**（吉神无同名黄道神煞，安全增删）。
- 导出 `module.exports._getLsrGods` 供 CI 直接调用真实函数（非逻辑副本）。

### 验证
- `server/verify_override_noref.js`（`npm run test:override-noref`，新增）：直接 require 真实 `_getLsrGods`，2024-2027 共 4383 神煞实例，圣心/益后/续世 与协纪**零偏差** ✅。
- `server/smoke_test_api.js`：重启服务后真实 API（HMAC 认证）冒烟 6 例（2024-10-12/10-18 圣心、2024-8-7/8-13 益后、2024-1-1/1-13 续世）全部 PASS ✅。
- 服务已重启（taskkill 旧 PID 9808 + 重新 `node index.js`，新 PID 1162，`:3000` listening）。

### 第二批差分总结果
- 新增已锚定一致家族 **28 项**（协纪卷六/卷七：黄道十二神 11 项 + 官日/守日/相日/民日/阳德/阴德/天马/兵禁/土符/大煞(月)/要安/玉宇/金堂/敬安/普护/福生/月空）。累计已验证一致家族 24+28=52 项。
- 新发现值错误 **3 项**（已修复）。
- 剩余未锚定 NOREF **57 项**（三陰、上朔、不將、九坎、了戾、五墓、五虛、伐日、八專、八風、兵吉、兵福、反支、單陰、四廢、四忌、四擊、四窮、四絕、四耗、四離、地囊、地火、大會、天后、天吏、天狗、天罡、孤辰、孤陽、寶日、專日、小會、歲薄、歸忌、氣往亡、河魁、無祿、短星、純陰、純陽、義日、臨日、行狠、製日、解神、觸水龍、逐陣、長星、陰位、陰道沖陽、陰錯、陰陽交破、陰陽俱錯、陰陽擊沖、陽破陰沖、陽錯），建议后续对照协纪卷九逐条锚定。
- 报告已更新为 v2（`server/剩余神煞差分定正报告.md`）。

### 重要架构更正
- 覆盖层实际位于 `server/routes/calendar.js` 的 `_getLsrGods`，**不存在** `server/core/shensha.js`（MEMORY.md 旧记误）。已同步修正 MEMORY.md 与报告 v2。
