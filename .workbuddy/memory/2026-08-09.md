# 2026-08-09 工作日志

## 服务重启
- 需求：重启本地服务。
- 操作：检查端口 3000 无服务，用托管 Node 22.22.2 启动 `H:/Phone/server/index.js`（后台 task `myPS0w`）。
- 验证：`curl` 返回 HTTP 200，`[server] listening on port 3000`，无报错。

## 万年历「今」标识重设计
- 需求：原左上角 9px 暗金小字不高级、不明显，需要重新设计。
- 改动：仅修改 `web/css/calendar.css`（不涉及 JS）：
  - 今天格子金环升级为精致双金环（`--color-gold-bright` 边框 + 内发光 inset shadow）。
  - 「今」字移到右上角，改为 18px 圆形鎏金印戳徽章：
    - 径向渐变亮金→暗金 (`#FCE9B4` → `#E6BC63` → `#B8860B`)
    - 白字 700 字重
    - 高光内边 + 柔光外晕
    - 2.6s 呼吸脉冲光晕动画 (`today-pulse`)
  - 选中今天（金块背景）时徽章反色：白底金字，取消呼吸动画，保持对比。
- 验证：Playwright 打开 `/app.html` 并激活万年历 panel；两种状态样式均正确：
  - selected: 白底 + 金字
  - 未选中: 鎏金底 + 白字
  - 控制台仅有一个无关 404。
- 截图产物：`cal_today_full.png`、`cal_today_selected.png`、`cal_today_badge.png`。

## 万年历「今」标识动效增强
- 需求：上轮重设计后动效仍不明显，要求「带动效」。
- 改动（仅 `web/css/calendar.css`）：
  - `.today-mark` 增加 `today-pop` 弹性入场动画（0.7s，overshoot 曲线 `cubic-bezier(0.34, 1.56, 0.64, 1)`）。
  - 增加 `::before` 能量环 `today-ring`：1.5px 鎏金环从徽章中心向外扩散到 2.2 倍并淡出（2.2s 循环），持续提示「今天」。
  - 呼吸脉冲 `today-pulse` 保留在非选中态，光晕幅度略微增强。
  - 选中今天（金块背景）不再取消动画：保留弹入，并把能量环改为白环（`rgba(255,255,255,0.6)`），在金块背景上更明显。
- 验证：Playwright 读取 computed style：
  - selected 态 today-mark animationName = `today-pop`，ring (`::before`) animationName = `today-ring`。
  - 控制台无报错。
- 截图产物：`cal_today_anim.png`、`cal_today_full.png`。

## 记事功能重新设计（独立页面）
- 问题：原 `note` 路由只是 `__setActivePage('cal-root')` + 弹日历浮层；`PAGE_ROOTS` 无 `note-root`；`open/openForSelected` 入口缺失，记事几乎无法使用、无列表。
- 改动：
  - `web/js/app.js`：`PAGE_ROOTS` 加 `'note-root'`；note 路由改为 `setActivePage('note-root')` + `loadModule('note').renderNotePage()`。
  - `web/js/note-ui.js` 重写：
    - `DailyNotes.entries()` 新增，返回全部非空记事 `[{key,y,m,d,text}]`。
    - 新增 `renderNotePage`：独立记事中心页（标题 + 新建按钮 + 月份筛选 + 列表）。
    - `_renderList`：按日期倒序卡片（日期/周几 + 内容预览 + 编辑/删除），空状态引导，月份筛选。
    - `_openEditor` 升级：加 `<input type=date>`（可记任意日期），保存/删除按输入日期落库。
    - 保留 `_refreshNoteDot`（日历红点联动）、`DailyNotes` 导出、`open/openForSelected` 兼容旧入口。
  - `web/css/base.css`：新增 `.note-page*` / `.note-card*` / `.note-empty*` / `.note-editor-date` / `.note-date-input` 及暗色适配、移动端堆叠。
- 验证（Playwright，block SW）：空状态可见 → 新建(改日期昨天)+再建今天=2条 → 月份筛选2026-08=2 → 编辑标题"编辑记事" → 删除=1 → reload 后仍1（持久化）；无 JS 报错。
- 截图：`note_empty.png`、`note_list.png`、`note_after_reload.png`。

## 首页「天地玄黄 · 日月更替」Hero 重设计
- 需求：原 hero 区仅一个 84px 浮动 webp 图标 + 平铺标题 + 红色圆角 badge，视觉普通、不够高级，要求重新设计。
- 改动：
  - `web/app.html`：`.home-hero` 内部替换为「日月合璧徽记 + 主次标题 + 朱砂印章」结构。
    - 徽记：纯 SVG，银白月牙（左）+ 鎏金太阳（右），使用 SVG mask 画月牙，不依赖背景色。
    - 星轨：绕徽记一圈虚线金环 + 8 颗金色星点，52s 缓慢自转 (`homeOrbit`)。
    - 呼吸：日月整体 5.2s 轻微缩放呼吸 (`homeBreath`)。
    - 标题：主标题「天地玄黄」大字号鎏金渐变（`text-heading`→`color-gold`，background-clip text），副标题「日月更替」金色字距展开。
    - 印章：「八卦 / 掌决」朱砂方印，0.6s 弹入落印动画 (`homeSealIn`)。
  - `web/css/layout.css`：
    - 删除旧的 `.home-brand-icon`、`.homeFloat`、`.home-badge`。
    - 新增 `.home-emblem`、`.home-orbit`、`.home-sunmoon`、`.home-title-main`、`.home-title-sub`、`.home-seal` 及动画关键帧。
    - 加 `prefers-reduced-motion` 媒体查询关闭动画。
- 验证：Playwright 访问 `/app.html`（SPA 入口）：
  - `.home-emblem` 可见、`.home-sunmoon svg` 存在。
  - 主标题文本「天地玄黄」、副标题文本「日月更替」、印章文本「八卦掌决」均正确。
  - `orbitAnim=homeOrbit`，`breathAnim=homeBreath`。
  - 无 JS 报错（仅无关 404）。
- 截图产物：`home_hero.png`。
