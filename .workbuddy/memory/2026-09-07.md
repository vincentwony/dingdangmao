# 2026-09-07 工作日志

## Phase 2.2 — 神煞协纪来源标注（20 个差异神煞，已完成）

> 延续 Phase 2.1：为「今日神煞」卡片中 20 个与《协纪辨方书》有流派/立成差异的神煞，补协纪出处 + 差异说明，供 tooltip / 「流派差异」标记展示。**仅展示，不改吉凶判定、不改 goodGods/badGods。**

### 后端（server/routes/calendar.js）
- 新增 `GODS_SOURCE` 常量：键=lunisolar 实际输出名（繁体，已用 `probe_godnames.js` 校准），值 `{xiejì,def,diff,isDiff:true}`。
  数据全部来自 Phase 2.1 逐条原文锚定（verify_noref2.js + Phase2.1_锚定报告.md），无捏造。
- 新增 `buildGodsMeta(good,bad)`：仅返回当日命中的来源神煞元数据，不泄漏非来源名。
- `/calendar/day` 在 `goodGods/badGods` 后注入 `computed.godsMeta`。导出 `_buildGodsMeta`/`_GODS_SOURCE` 供 CI。

### 前端（web/js/detail-ui.js + calendar.css）
- `buildSsGroup` 增 `meta` 入参：命中时渲染 `title`(原生tooltip)+`data-tip`(自定义浮层)+`has-source` 类+`ss-tag-diff` 小标记（⚑）。
- `renderShensha` 传 `dat.godsMeta`；`_renderJishenBanner` 为吉神名加 `title`。
- CSS：`.has-source` 光标 help、`.ss-tag-diff` 金色小标记、`.has-source[data-tip]:hover::after` 浮层（原生 title 兜底）。

### 验证（全绿）
- `npm run test:godsmeta`（verify_godsmeta.js）：20 键名精确命中 lunisolar 输出、字段齐全、不泄漏 → 20/20 ✅
- `npm run test:godsmeta-ui`（verify_detail_godsmeta.js）：执行 detail-ui.js 真实 buildSsGroup，确认渲染 title/data-tip/has-source/ss-tag-diff、非来源不泄漏 ✅
- 端到端 `probe_godsmeta_live.js`：HMAC POST /calendar/day (2026-01-04) → godsMeta 命中 兵吉、天后，含完整 xiejì/def/diff/isDiff ✅
- 回归 verify_override_4 / verify_override_noref / test:noref2 均 EXIT=0（无副作用）

### 20 个差异神煞（键名=实际输出）
九坎 了戾 地囊 地火 大會 孤辰 孤陽 專日 小會 氣往亡 短星 義日 行狠 陰道沖陽 陰錯 陰陽俱錯 兵吉 臨日 天后 天吏

### 产物 / 文档
- 脚本：verify_godsmeta.js（CI）、verify_detail_godsmeta.js（CI）、probe_godnames.js（键名校准）、probe_godsmeta_live.js（端到端）。
- 设计文档：server/Phase2.2_神煞来源标注方案.md。
- package.json 注册 test:godsmeta / test:godsmeta-ui。

### 已知边界
- 仅「今日神煞」卡片展示；月视图网格不展示来源（与既有设计一致）。
- 若 lunisolar 升级改了某神煞繁体输出名，GODS_SOURCE 对应键静默失效；CI `test:godsmeta` 的「键名命中真实宇宙」检查会捕获。
- 扩展点：把 14 个覆盖层神煞（天德等）协纪出处也录入 GODS_SOURCE（isDiff:false），即可展示「已按协纪修正」说明。

## Phase 3.1 — 择日助手（选日报告，已完成）

> 延续 Phase 2.2：在已定正真实黄历数据之上，新增「择日助手」——给定事项+月份，基于当月每日真实宜忌+吉神/凶煞评分排序，给候选吉日与可解释理由。**仅筛选/排序/解释，不改吉凶判定。**

### 后端（server/routes/calendar.js）
- `CHOOSE_EVENTS`：10 类事项（jiahun/ruZhai/kaiShi/dongTu/chuXing/anZang/qiFu/dingMeng/qiuYi/qianYue），每类含 label/core/yi/prefer/avoid/avoidJianchu/ref；映射取自《协纪辨方书·义例》《选择宗镜》《玉匣记》通用通则。
- `CHOOSE_UNIVERSAL_BAD = ['月破','歲破','四離','四絕']` + 小红砂 + 杨公忌 → 通用诸事不宜硬排除（直接判忌、入 avoidDays）。
- `_dayBundle(y,m,d)`：复用 `computeDayFromLunar`+`_getLsrGods`+`_getLsrActs` 取全量真实包（区别于 `_buildDayItem` 的 `slice(0,6)`）。
- `_scoreDay(b,ev)`：matchedYi(+12·min3) / preferHit(+5) / avoidHit(−9) / 建除忌(−10) / conflictJi(−40) → 吉(score≥24)/平(≥8)/忌；reasons[] 真实依据。
- `_chooseDays(eventKey,y,m)`：遍历当月按 吉>平>忌 + score 降序排序；返回 {event,label,ref,y,m,total,days,avoidDays,sources}。
- `POST /api/v1/calendar/choose`：校验 event/y(1900–2100)/m(1–12)，非法 400。导出 _chooseDays/_CHOOSE_EVENTS/_dayBundle/_scoreDay/_CHOOSE_UNIVERSAL_BAD。

### 前端（web/js/choose-ui.js + calendar.css）
- `calendar-ui.js` 末尾懒加载 `import('./choose-ui.js')` 调 `init()`（独立模块避免循环依赖）；在 `#calHeaderBar` 注入「择日」按钮 + `#cal-detail-inline` 后注入 `#choose-panel`。
- 面板：事项 select(10 项 key 与后端一致) + 年/月 select + 查询/收起；`API.post('/calendar/choose')` → `_renderResultsHtml`。
- `_dayCardHtml`/`_renderResultsHtml` 为纯函数（可测）：候选区过滤忌/诸事不宜、`details` 折叠诸事不宜日、出处；「查看日课」→ `selectDay(y,m,d)` 跳转并定位当日详情。
- CSS：`.choose-day-card.grade-吉|平|忌`（绿/amber/红）含 `body.dark` 覆盖。

### 验证（全绿，无回归）
- `npm run test:choose`（verify_choose.js）：25/25 ✅
- `npm run test:choose-ui`（verify_choose_ui.js）：12/12 ✅
- `verify_choose_live.js`：HTTP 200，嫁娶·31天·17诸事不宜·榜首 2026-10-23 吉（宜嫁娶；吉神 天喜、三合、不將、月恩）✅
- 回归：`test:override`/`test:override-noref`(0偏差)/`test:noref2`(NEEDS-SOURCE=0)/`test:godsmeta`(20/20)/`test:godsmeta-ui` 均 EXIT=0（无副作用）
- 设计文档：`server/Phase3.1_择日助手方案.md`。

### 已知边界
- 评分权重为择日**通则**加权近似，非某流派精确"成格"判定；神煞名已用 probe_godnames.js 校准繁体。
- 未启动：Phase 3.2（八字明细/择日八字联动）、Phase 4（清理 Back/build、CI 并入主流水线）。

## Phase 3.2 — 择日报告与八字联动（本命合参，已完成）

> 在 Phase 3.1 候选卡补**八字三柱** + 新增**本命合参**联动（干支冲合刑害）。仅展示+同等级内软排序，不改吉凶定级。

### 八字三柱（数据真实性已探针确认）
- `_dayBundle` 已返回 `gz.year/month/day`；探针确认 `computeDayFromLunar` 年月柱**按节气分界**（2026-02-03 乙巳→02-05 丙午；立夏 05-04 壬辰→05-06 癸巳），即真八字年/月柱，可作日课干支展示。
- 候选卡 `_dayCardHtml` 改显 `_bazi3col(gz)` = 「年 月 日 干支」。示例 2026-10-23 = 丙午年 戊戌月 庚午日。

### 本命合参（确定性干支规则）
- 数据来自保存命盘 `localStorage: bazi_archives`（`web/js/archive-ui.js`，记录含 `pillars:{year,month,day,hour}`），取**日柱**为本命日元。面板：下拉选命盘 + 手动填日柱；`_normZhi` 取地支单字。
- 关系（`ZHI_ARR` 序号）：六冲(隔6)/六合/三合局(申子辰[8,0,4]·亥卯未[11,3,7]·寅午戌[2,6,10]·巳酉丑[5,9,1])/三刑(寅巳申·丑戌未·子卯)/六害。
- ⚠️ **坑（已修）**：三合数组初版误写 `[[2,0,4],[5,3,7],…]`（寅子辰/巳卯未），CI 当场捕获「申与子同局」失败，修正为 `[8,0,4]/[11,3,7]`。**三合数组务必按 ZHI_ARR 序号核对，勿凭记忆。**
- `_dayUserRelation(dayZhi,userZhi)` 返回标签（冲/刑/害=bad，合/三合=good）；`_applyBenming` 标注+软权重(合+6/冲−8/刑害−4)，**仅同 grade 内**重排，grade 不变。卡片渲 `.cdc-bm-good/bad`，meta 提示不改吉凶定级。

### 验证（全绿）
- `npm run test:choose-ui`（verify_choose_ui.js）：22/22（原12+新10）✅
- `npm run test:choose-e2e`（check_choose_ui.js，新增 Playwright）：5/5 ✅（预置甲子命盘，三柱卡+冲合标注+无报错）
- `npm run test:choose`（verify_choose.js）25/25、`verify_choose_live.js` HTTP 200（后端未改）
- 回归：`test:override`/`test:override-noref`(0偏差)/`test:noref2`(NEEDS-SOURCE=0)/`test:godsmeta`(20/20)/`test:godsmeta-ui` EXIT=0
- 设计文档：`server/Phase3.2_择日报告与八字联动方案.md`；package.json 注册 `test:choose-e2e`。

### 已知边界
- 本命合参以日柱(日元)为基准，未纳入年柱(年命)冲合；时柱未参与（择日按日，三柱=日课非四柱）。
- 未启动：Phase 4（清理 Back/build、CI 补强并入主流水线）。

## Phase 4 — CI 补强并入主流水线（CI 部分已完成；清理待确认）

> 把分散 verify_*/check_* 并入 `npm test` 主流水线；清理遗留目录（不可逆，仅扫描出清单，待用户确认）。

### CI 聚合（server/package.json）
- 新增 `test`：串联 17 步 fail-fast = override→override-noref→noref2→godsmeta→godsmeta-ui→choose→choose-ui→choose-e2e→dst→bazi-dst→yiji→zishi→hongsha→hongsha-ui→guansha→gods→remaining。
- 新增 `test:legacy`：老阶段 bazi/ziwei 独立脚本（daymaster/congge/p2/hmac-fallback/insecure/ziwei-override/ziwei-patterns），按需跑不阻塞主流。
- 补登遗漏红砂脚本：`test:hongsha`=verify_hongsha_impl.js（9/9）、`test:hongsha-ui`=check_hongsha_ui.js（11/11）。

### 主流水线当场捕获并修复的真实问题
**A. 红砂旧文案「红砂日」未彻底替换（真 bug）**
- `routes/calendar.js:485` 宜忌后置注入原写 `'红砂日（小红砂·忌百事）'`，而前端角标已是「小红砂（凶）」。详情页 `bad[]` 仍含旧词。
- 修复：`calendar.js:485`→`'小红砂（凶·忌百事）'`；同步 `verify_yi_ji.js:22` 断言。
- **关键坑**：改 `server/` 需重启。首次 `taskkill //PID 13336 //F` 返回 exit 23 未真杀，端口 3000 仍被旧进程占、新进程 EADDRINUSE 退出 → 一直跑旧代码致红砂检查反复失败。改用 `PowerShell Stop-Process -Id 13336 -Force` 强杀重启（新 PID 390），探针确认 `contains 红砂日?===false`。**教训：重启后务必 netstat/探针校验端口归属进程，勿只看 health 401。**

**B. choose-e2e 偶发 404 控制台噪声（flaky，非 bug）**
- `check_choose_ui.js` console 监听未过滤 `Failed to load resource` 噪声；隔离复现全流程 0 错误 0 4xx → 浏览器资源抖动。对齐 `check_hongsha_ui.js` 过滤（保留 `pageerror` 真实 JS 异常）。

### 结果
`npm test` 17/17 步骤 0 失败，全绿。

### 清理（已执行：移入隔离区，可恢复）
- 用户确认「全部删除含 Back/」。因 Back/(42M)/.build/(1.9M) 不在 git、删除不可恢复，**未用 rm**，整体移入 `H:/Phone/.trash_phase4/`（≈55M）：server/_shots+_footer_shots(548K)·build/(11M,git 跟踪 98 文件转 D)·.build/·Back/。
- 原始路径已清空；这些目录均不被 server 静态服务，运行时无影响（已验证 health 401）。数据仍可还原（build/ 还可 git restore）。
- `core/`（gongxin-core 依赖，已跟踪）不纳入。根目录 `.md/.png/.mjs` 调试产物超范围，未动，建议人工复核。
- 如需永久清除隔离区，须二次确认后清空 `.trash_phase4/`。
- 设计文档：`server/Phase4_CI补强与清理方案.md`（已更新清理为「已执行」）。

---

## 收口（20:22 结束）

- Phase 2.2 → 3.1 → 3.2 → 4 全链路已完成并收口。Phase 4 任务 #203/#204/#205 均标记 completed。
- 今日最终状态：`npm test` 17/17 全绿；遗留目录已隔离（可恢复，运行时无影响）；两处真实回归（红砂旧文案、choose-e2e 噪声）已修复。
- 下一步可选（未启动）：① 永久清空 `.trash_phase4/`；② 把 14 个覆盖层神煞协纪出处也录入 GODS_SOURCE（isDiff:false）展示「已按协纪修正」；③ Phase 3.2 本命合参扩展纳入年柱冲合。
