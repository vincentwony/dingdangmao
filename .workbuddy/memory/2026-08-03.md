# 2026-08-03 紫微斗数全方面校验（倪师天纪方法论）

## 任务背景
用户要求：用倪师（ni-haixia-perspective 技能，天纪「假设→验证→结果」治学方法论）对本程序紫微斗数做全方面修复/验证/重写，并随时互动。

## 已坐实的结论
- **计算引擎 iztro 排盘核心正确**：命身宫、五行局、紫微/天府落宫、主星两系、年干四化（三端=后端MUTAGEN_MAP/前端/iztro 一致，全书北派庚干天同忌）、飞星（前端自研依赖 MUTAGEN_MAP 不依赖 iztro 空壳 fliesTo）、流年（农历太岁法，先前误判已翻案）、庙旺利陷（抽查紫微得地/太阴丑庙/太阳丑不/天同亥庙/破军戌旺/七杀午旺/廉贞申庙/巨门卯庙 全中标准，不需覆盖）。
- **真 bug 已修**：`routes/ziwei.js` 覆盖层 `import('./ziwei_baseline.mjs')` 路径错（文件在 server/ 非 routes/）→ 改为 `'../ziwei_baseline.mjs'`，活接口 500 已修复。
- **流派分歧已覆盖（传统派/倪师天纪 覆盖 iztro 派）**：大限（阳男阴女顺/阴男阳女逆）、长生十二宫（依日干五行、阳顺阴逆）、小限（年支性别起宫表 + 男逆女顺）。iztro 原用另一自洽流派（大限仅阳女顺、长生依五行局），非算错。
- **基准模块 `server/ziwei_baseline.mjs`** 已建（不依赖 iztro 的独立规则实现）；修正了小限起宫表两处抄写错（子年女/戌年男：子→午）。
- 验证脚本：`verify_ziwei_override.mjs`（8样本+文档明例 129/129 PASS，纳入 CI `test:ziwei-override`）、`verify_ziwei_live.mjs`（活接口确认覆盖层生效）、`probe_ziwei_brightness.mjs`。

## 唯一实打实功能缺口
**格局判定从未实现**：iztro `plugins=[]` 无格局；前端 ziwei-ui.js 仅在 AI prompt 文案提"命宫格局"，无判定逻辑。文档第八章（873-1031行）有完整规则：甲级吉格22 + 乙级吉格8 + 甲级煞格12 + 乙级煞格6 ≈48格，每格列构成条件+主断。注意文档1295行注明格局以中州派为主、兼采南派（与四化用全书北派略有流派交错，但格局判定主看星曜组合，基本流派无关）。

## 待办
- 与用户对齐格局判定实现范围（全48格 vs 聚焦甲级 vs 其他）后动手。
- 若做：建 `server/ziwei_patterns.mjs` 纯函数判定器 + 接后端 `/astrolabe` + 前端渲染；验证脚本补 CI。
- 服务现由本会话后台启动（node index.js @ :3000，监听0.0.0.0）；此前旧进程 PID 17448 已 taskkill 释放端口后重启。

## 紫微斗数 UI 版式改造（2026-08-03 后续）
用户要求：宫位星耀完整显示、文字版式参照图例、方位文字显示、不遮挡。
- 改造 `web/js/ziwei-ui.js`：重写 `_renderPalace`，采用图例式横排星耀块（主星+辅星+亮度/四化一行，杂曜一行），长生十二神放左栏竖排，大限范围+小限年龄放右栏，底部显示博士/将前/岁前十二神；每个外侧宫格外侧显示方位文字（正南/南偏西/正西 等）。
- 改造 `web/css/ziwei.css`：命盘容器 `max-width` 560→720px，宫格 gap 增大，新增 `.zw-palace-azimuth` / `.zw-palace-core` / `.zw-star-block` 等样式，移除旧的全局 `zw-dir-label` 方位标签（改由单宫渲染，更贴合图例）。
- 验证：`node --check` 通过；Playwright(msedge headless) 对 1982-5-10 12:20 男命自动填表排盘截图，确认 12 宫星耀、方位文字完整显示、无明显遮挡。
- 截图产物：`H:/Phone/ziwei_preview_1982-5-10.png`。

## 紫微 UI 微调（2026-08-03 晚间）
用户反馈三处：字号合适、方位文字加深、中间信息卡「大限起运」列表简化。
- `web/css/ziwei.css`：`.zw-palace-azimuth` 颜色由 `var(--text-muted)`/`opacity:0.9` 改为 `var(--text-secondary, #3D3732)`/`opacity:1` 并加 `font-weight:600`，方位文字更清晰。
- `web/css/ziwei.css` + `web/js/ziwei-ui.js`：中间信息卡「大限起运」列表由 2 列 12 行改为紧凑 3 列网格，每个单元格去「宫」字后缀、去「岁」字，格式如「命 4–13」；CSS 新增 `.zw-info-dx-cell { white-space: nowrap; }`。
- 验证：`node --check` 通过；静态服务返回最新 CSS/JS；Playwright 重新截图，方位文字明显加深、大限列表紧凑显示。

## 紫微 UI 修正：移除宫位右侧岁数数字（2026-08-03 晚间）
用户反馈：宫位右侧"全是数字"，问是不是岁数，要求不要这些、要完整星耀名称。
- 根因：`web/js/ziwei-ui.js` 的 `_renderPalace` 在宫位右栏渲染了 `p.decadal.range`（大限起止岁数，如 `4~13`）和 `p.ages`（小限年龄，每个宫约 9~10 个岁数竖排），导致视觉上"全是数字"，并把星耀挤到中间很窄。
- 处理：从 `_renderPalace` 中删除整个右侧年龄列（大限范围 + 小限年龄）；同步把 `web/css/ziwei.css` 的 `.zw-palace-core` 由 `grid-template-columns: 1em 1fr 2.4em` 改为 `1em 1fr`，宫位主体只剩「左长生十二神 | 中星耀」两列，星耀占满整格完整显示。
- 说明：大限起运信息仍保留在中间信息卡底部的「大限起运」列表（如 田宅 94–103 / 官禄 84–93），小限年龄从 UI 移除（按用户要求"不要这些"）。
- 验证：`node --check web/js/ziwei-ui.js` 通过；DOM 文本诊断确认 12 宫右侧数字列全为空字符串、中栏星耀名称完整；Playwright 重新截图确认无数字列、星耀名称完整可读。
- 截图产物：`H:/Phone/ziwei_preview_1982-5-10.png`。

## 传统方盘集成收尾（2026-08-03 本轮）
将定稿的传统方盘单宫样式集成进项目 `web/js/ziwei-ui.js`（`_renderPalace` 重写为：顶部星耀三行/中央小限大限/左下博士将前岁前/右下长生干支/底部红宫名）与 `web/css/ziwei.css`（`.zw-palace-*` + `.zw-c-*` 配色），保留全部原功能（运限/飞星/三合/四化视图、信息卡、点击交互、明暗主题）。
- **修复一处类名前缀 bug**：`_starColorClass` 返回裸 `c-red/c-purple/c-blue`，而 CSS 仅定义 `.zw-c-red/.zw-c-purple/.zw-c-blue`，导致主星两行（名1/名2）不显色（只有 r3 庙旺行用了 `zw-c-gray` 正确）。已改为返回 `zw-c-red` 等带前缀名，与 CSS 对齐。
- 验证：Playwright 重新跑 `screenshot_ziwei.mjs`，DOM 抽样确认 `<span class="zw-palace-cell zw-c-red" data-star="天机">天</span>`；chart card 554×554、canvas/grid 504×504 正常，palaceCount=36（12宫×3视图）。截图存 `H:/Phone/ziwei_preview_1982-5-10.png`（模型不可读图，需用户在浏览器目视确认视觉）。
