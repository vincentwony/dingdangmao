# 2026-07-18 紫微模块代码 BUG 全面审计与修复

## 审计结论
运行时字段迁移**完整无遗漏**（活接口 JSON 逐字段核对）：brightness/ages/jiangqian12/suiqian12/decadal.range/meta.* 全齐；前后端 time-util 镜像一致。先前 brightness 半截 bug 已根治。

## 修复的 5 处
1. **真太阳时跨日修正符号反了（真实逻辑 bug）** `server/lib/true-solar.js`：旧 `ztyOff>720` 判据符号错，东经+偏移跨午夜误减日、西部早间误加日、跨年进位错。改为 `dayDelta=floor(total/86400)` 连续秒数拆日。单元验证 +1/-1/跨年 全过。
2. **星曜序列化半截模式（根因防护）** `server/routes/ziwei.js`：major/minor/adjective 三条数组各写内联映射、漏 scope，与 serializeStar 不一致（同上次 brightness bug 同源）。统一 `.map(serializeStar)`。活接口复测无回归（adjectiveStars 现带 mutagen/type/scope/brightness）。
3. **showDetail 选择器错误** `web/js/ziwei-ui.js`：`getElementById('zw-detail')` 错（zw-detail 是 class）→ 改查 `.zw-view-panel.active .zw-detail`。
4. **time-util 红线分叉** `web/js/ziwei-ui.js`：删本地 `_parseClockTime`，改 `import { parseClockTime } from './time-util.js'`（守单一真源红线）。
5. **SVG 脆弱耦合加固** `web/js/ziwei-ui.js`：palaces[idx] 依赖「数组位置===宫index」隐性假设 → 新增 palaceByIndex 按 index 显式查找。

## 验证
node --check 后端(CommonJS)+前端(ESM .mjs 形态) 通过；真太阳时 applyTrueSolar 单元用例通过；重启 :3000 后活接口字段审计无回归。
**服务已切回 nodemon 热重载**（H:/Phone/server/run-node.bat 配置）；中途曾误用 plain `node index.js`，已纠正。

## 提示项（未改，非 bug）
- 前后端 MUTAGEN_MAP 重复（年干四化走后端 meta.yearMutagen 权威值；宫干四化飞星走前端表）。当前一致，建议加交叉注释+开发期断言防漂移。
- 报告文件：`H:/Phone/ziwei-audit-report.md`。

# 2026-07-18 服务常驻 + 热重载 + 登录自启

## 用户诉求
每次打开项目都要手动重启服务,想要更简便/自动的方式。最终选「全都要」:常驻(关终端不杀) + 改代码热重载 + 登录自启。

## 落地文件
- `H:/Phone/start-server.ps1` — 核心脚本,支持 `start|stop|restart|status`;用 **CIM `Win32_Process.Create`** 脱离终端启动(比 Start-Process 可靠)。
- `H:/Phone/start.bat` — 手动一键启动(双击/一句命令)。
- `H:/Phone/server/run-node.bat` — nodemon 包装:`node nodemon.js --ignore "core/**" ... index.js`,日志重定向到 `H:/Phone/.cal-run/`。
- 登录自启:`C:\Users\Administrator\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\start-cal-server.bat`(调 powershell 起服务,最小化窗口)。

## 关键坑(可复用经验)
1. **git bash 下 `nohup node &` 不可靠** —— 父 shell 退出会带走进程,导致"每次打开要重启"。改用 CIM 脱离。
2. **PowerShell 5.1 `Start-Process` 因 `Path`/`PATH` 大小写冲突抛异常** —— 用 .NET `SetEnvironmentVariable` 修复,或直接用 CIM 绕过。
3. **`node --watch` 在本项目自重启崩溃** —— 引擎加载触碰监听目录文件触发无限重启。改用 **nodemon**。
4. **nodemon 不读配置文件**(`.nodemon.json` 带点错名;即使正确名也不显示 ignoring 行)—— 直接命令行 `--ignore` 最稳。
5. **致命**:run-node.bat 日志重定向到 `server/` 内 → nodemon 监听日志写入 → 自重启 → 端口冲突 crash。必须把日志/pid 放 **server 目录之外**(`H:/Phone/.cal-run/`)。
6. **app 在无控制台(hidden)上下文运行几秒后崩溃** —— 用 `-WindowStyle Minimized`(有控制台)而非 Hidden;且 `start`/`Start-Process`/`WScript.Shell` 被沙箱安全策略拦截,Startup bat 用同步 `powershell -WindowStyle Minimized -File`(start 脚本 health 通过即返回,nodemon 已脱离常驻)。
7. **任务计划(Session 0)触发 app 也 crash** —— 改用 Startup 文件夹方案(交互用户上下文,等同手动双击,已验证稳定)。

## 验证结果(全绿)
- 常驻:关掉启动 shell 后 netstat 仍见 3000 监听 ✅
- 热重载:touch 源码 → 日志 `restarting due to changes` → 重新 listening ✅
- 登录自启:手动运行 Startup bat 从零拉起,10秒稳定无 crash ✅

---

## 紫微斗数 vanilla tab 视觉重排（同日追加）
- 用户反馈当前圆形罗盘"错乱"，要求参照截图的 4×3 方格命盘格式。
- 已按参考图重排：`web/js/ziwei-ui.js` 的 `renderChart` 改为 4×3 grid，地支到网格位置固定映射（巳午未申/辰卯戌酉/寅丑子亥）；命盘信息卡放到网格上方，不覆盖宫位，确保 12 宫内容完整。
- 每宫格渲染：宫名+干支、大限年龄、流年 ages、主星（亮度/四化）、辅星/杂曜/煞星、运限模式下叠加运曜/流曜。
- `web/css/ziwei.css` 重写为方格命盘样式，保留表单/运限/AI/详情/悬停样式。
- 安全阀：L0 21 文件、L1 启动冒烟、L1.5 紫微 31 断言全绿；前端文件已热重载，服务 health 200。

## 紫微 tab 显示日历 bug 修复（13:45 用户报障）
- **症状**：打开紫微斗数 tab，页面显示的是万年历（日历信息），而非命盘。
- **根因**：`web/js/ziwei-ui.js` 顶部在 4×3 重写时只保留了 `_lsSet`，**丢了 `_lsGet`**。`show()` → `populateYearOptions()` 直接调用未定义的 `_lsGet('zw_year',1990)` 抛 `ReferenceError` → 冒泡出 `show()` → 被 `app.js` 的 `loadModule('ziwei').then(...).catch()` 捕获 → 执行 `showCalendarPage()`（日历）。`prefillFromBazi` 因有 try/catch 兜着没暴露，但 `populateYearOptions` 无守卫，一上来就崩。
- **修复**：补回 `function _lsGet(k, d)`（与 `_lsSet` 相邻）。
- **诊断方法（可复用）**：用 jsdom 写 `repro_ziwei.mjs`，import 真实 `api.js` 后把 `API.post` mock 成返回命盘，再调 `show()`，直接看到抛错栈。比纯读代码快。
- **防复发**：L1.5 烟雾测试新增 4 项 `show()` 全链路断言（渲染 4×3 命盘 + 信息卡 + 日历区隐藏 + 12 宫），此前 L1.5 只测 renderChart/buildFormHTML/showDetail，**从没测过 `show()` 链路**，才漏掉此 bug。
- 教训：任何 `show()`/入口函数若同步抛错，会被路由 `.catch` 静默回退，现象是"显示别的 tab 内容"。测试必须覆盖入口函数本身。

## 紫微命盘布局修正：4×3 → 4×4 方盘（14:23 用户指正）
- **用户指正**：命盘应是 **4×4**（标准紫微斗数 方盘），不是我之前做的 4×3（缺了中间一行，导致"错乱"）。
- **权威排法**：12 地支绕成一圈（子北·午南·卯东·酉西），中间 2×2 留空放元信息。具体 POS_GRID：
  ```
  巳 午 未 申
  辰 □ □ 酉
  卯 □ □ 戌
  寅 丑 子 亥
  ```
- **改动**（`web/js/ziwei-ui.js` + `web/css/ziwei.css`）：
  - `POS_GRID`/`BRANCH_POS` 改为 4×4（中心两格为 null）。
  - `renderChart` 改为真实 CSS Grid（`display:grid; grid-template:repeat(4,1fr)/repeat(4,1fr)`），12 宫用显式 `grid-row/grid-column` 落环位；信息卡 `.zw-info-card` 用 `grid-row:2/4;grid-column:2/4` 跨占中央 2×2。
  - `renderPalace(p,i,scope,mutMap,scopeStars,pos)` 新增 `pos` 参数发坐标；删除 `.zw-chart-row` 包裹（不再需要）。
  - CSS：`.zw-chart-grid` 由 flex-column 改为 grid 4×4；`.zw-info-card` 改为居中 flex（去掉 margin auto）。
  - `scripts/smoke-ziwei.mjs`：断言更新为 4×4（12 宫、信息卡跨中央 2×2、`zw-palace-empty` 为 0）。
- **验证**：L0(21)✅ L1(启动冒烟)✅ L1.5(35 断言)✅；服务 health 200，前端文件已热重载（grep 确认含 `null, null` 与 `repeat(4,1fr)`）。
- **教训**：用户提供的参照图是 4×4 方盘，我之前误判成 4×3。命盘类视觉务必先确认网格维度（3×4/4×4）再动手。
- **待确认**：宫名绕环方向（顺/逆）与参照图是否完全一致需用户提供确切生辰输入才能逐字对拍；当前按 iztro 返回的 earthlyBranch 落位（iztro 引擎权威）。

## 服务掉线导致 localhost:3000 打不开（14:34 用户报障）
- **症状**：浏览器打不开 `http://localhost:3000/app.html#ziwei`，完全连不上。
- **根因**：后端服务进程又掉了（老毛病），curl 全 `000`，ps 无 node 进程。与前端无关。
- **修复**：`powershell -File H:/Phone/start-server.ps1 start` 重启，health 200。
- **验证**：node fetch 实测 app.html(15805B)/ziwei-ui.js(24523B)/ziwei.css(14088B) 均 200；L1.5 紫微 35 断言全绿（含 `show()` 未回退日历）。
- **注**：curl `-w %{size_download}` 对 chunked 传输误报 0B，以 node fetch 的 `t.length` 为准。若再次打不开，先 `start-server.ps1 status` 看进程是否存活。

## 紫微 vanilla 改造全量回退到 iframe 老版（14:56 用户要求"取消所有修改，回到老版本"）
- **澄清范围（关键）**：工作区除紫微外还有大量未提交改动（calendar.css+649、detail-ui+448、bazi-ui+369、core/shensha.js 神煞修正+75、routes/calendar.js+75 等约3500行）。先用 AskUserQuestion 确认只回退紫微，保留其它。用户选"仅回退紫微"。
- **回退动作**：
  - `git restore --source=HEAD --worktree -- web/app.html web/js/app.js ziwei/index.html` → 回到 iframe 嵌 React 老版（app.html 的 `#ziwei-root` 覆盖层 + iframe，app.js 的 `iframe.src='/ziwei/...'`）。
  - `server/index.js`：删 `const ziweiRouter=require('./routes/ziwei.js')` + `app.use('/api/v1/ziwei',ziweiRouter)`；`/api/interpret` 还原成 HEAD 的 503 占位版（老版 React 兼容那个）。
  - 删新增文件：`web/js/ziwei-ui.js`、`web/css/ziwei.css`、`server/routes/ziwei.js`、`scripts/smoke-ziwei.mjs`、`docs/CR-REPORT-ziwei.md`。
  - **保留**：`/ziwei` 静态托管 + SPA 回退 + `/astrolabe` 重定向（老版 iframe 加载 React 用）、`/api/interpret` 路由本身（仅还原实现）。
- **服务恢复踩坑（重要教训）**：
  1. 改完文件后第一次用 `restart` → nodemon 在 Stop 阶段杀进程 + 启动竞态，误判 "app crashed - waiting"（实际 node index.js 无 stderr 干净退出）。日志 `.server.err` 空，`.server.log` 末尾 "app crashed" 是**旧残留 nodemon** 写的。
  2. **误杀服务**：残留两个 nodemon 父进程（13800/6052），我误以为 6052 是 waiting 残留去 `taskkill /PID 6052 /T /F`，结果把**活跃 nodemon 6052 及其子进程 20272(node index.js, 真正监听3000)** 一起杀了，服务全挂(000)。教训：**taskkill 残留 nodemon 前必须先确认父子关系**——真正监听端口的 `node index.js` 是活跃 nodemon 的子进程，不是 waiting 那个。
  3. 正确做法：先 `taskkill` 掉所有 waiting 残留 nodemon（无 `node index.js` 子进程的），再用 `start`（不是 restart）干净拉起。这次 `start` 稳定 health 200。
- **验证**：`/ziwei/` 200、`/ziwei/assets/main-edba3d82.js` 200、`/app.html` iframe 覆盖层1次/vanilla-root 0次、`/api/v1/ziwei/astrolabe`→401(已移除)、`/api/v1/health` 200；其它改动(core/shensha.js、routes/calendar|bazi.js、bazi-ui|calendar-ui|detail-ui.js)仍 M 保留。
- **结论**：紫微已完全回到 iframe 嵌 `H:/Phone/ziwei/` React SPA 的老版；原 vanilla 改造（P1-P4）痕迹清除。

## 紫微 vanilla 4×4 版全量重建（16:56 用户要求"回到上一步"= vanilla 版）
- **背景**：上一步"取消所有修改"把未提交的 vanilla 改造一并回退删除了（vanilla 文件从未 git 提交，git/回收站均不可恢复）。用户引用最初指令"参照...改用手写 vanilla JS + tab 切换"明确要的是 **vanilla 4×4 版**（非 iframe 老版）。本次基于对话记忆 + iztro 实测探针全量重建。
- **重建文件（已落地并验证）**：
  - `server/routes/ziwei.js`（新建）：`POST /api/v1/ziwei/astrolabe`（动态 import iztro，序列化 12 宫+元信息五行局/命主/身主）、`POST /api/v1/ziwei/horoscope`（astrolabe.horoscope 序列化 decadal/yearly/age；**age.stars 为 N/A 需防护**）。挂 `/api/v1` 下自动 HMAC 鉴权。
  - `server/index.js`：加回 `ziweiRouter` 挂载 + `/api/interpret` 恢复 SSE 流式代理 deepseek（**注意：/api/interpret 在 hmacAuth 之前、无 /v1 前缀，前端直接用 `fetch('/api/interpret')` 不签名**）。
  - `web/js/ziwei-ui.js`（新建，ES module）：4×4 方盘（BRANCH_POS 对齐子北午南卯东酉西）、`show/buildFormHTML/renderChart/showDetail`、运限（本命/大限/流年+目标年）、AI 面板（buildAiPrompt/_runAi/_streamInterpret，key 存 localStorage `zw_ai_key`）。**务必保留 `_lsGet`**（之前丢它导致 show() 抛错被路由 .catch 回退日历——已用 L1.5 `show()` 全链路断言防复发）。
  - `web/css/ziwei.css`（新建，4×4 grid + 朱砂金 premium 风，全 .zw- 作用域复用 variables.css token）。
  - `web/app.html`：iframe 覆盖层 → `<div id="ziwei-vanilla-root">` + 加 ziwei.css link。
  - `web/js/app.js`：`_moduleLoaders.ziwei` + `Router.register('ziwei')` 走 `loadModule('ziwei').then(mod=>mod.show()).catch(showCalendarPage)` + showCalendarPage 隐藏 ziwei-vanilla-root。
  - `scripts/smoke-ziwei.mjs`（新建，L1.5 16 断言全绿）。
- **iztro 实测关键结构（探针确认，可作后续参考）**：astrolabe.palaces 12 宫（index/name/earthlyBranch/heavenlyStem/isBodyPalace/decadal/ages/majorStars[].mutagen/...）；`isBodyPalace` 标记**身宫**（非命宫）；horoscope.decadal/yearly 各含 `index`(运限命宫 palaces 索引)+`mutagen[4]`（禄权科忌序）+`stars`(12 宫数组，每宫运曜/流曜数组)；`age.stars` 为 undefined。
- **踩坑（本次新增，可复用）**：
  1. `global.navigator` 在 Node 22 是只读 getter，jsdom smoke 里不可 `global.navigator=...`（但 ziwei-ui 不用全局 navigator，删该行即可）。
  2. **并行 Edit 同一文件竞态**：同一 message 里对 app.html 发两个 Edit（iframe→vanilla-root + 加 ziwei.css link），结果第二个（ziwei.css link）**看似成功实际未写入**，首轮验证 ziwei.css link=0、4×4 样式丢失。改用单独精确锚点（calendar.css+bazi.css 上下文）重做才生效。**教训：同一文件多次 Edit 不要并行，逐一执行并验证**。
  3. `show()` 内误留未定义调用 `_renderScopeControls()`（实际绑定在 `_bindForm` 里），导致 show() 抛错中断渲染；删除该调用后正常。
- **验证**：L1.5 smoke 16/16 全绿（show() 不崩、12 宫、信息卡跨中央 2×2、运限切换、点击详情）；后端 /api/v1/ziwei/astrolabe + /horoscope 实测 200；前端 app.html/ziwei-ui.js/ziwei.css HTTP 200；老 React 版 `/ziwei/` 仍 200（"原应用保持不变"承诺保持）。探针 `server/probe_iztro*.mjs` 已删，`server/test_ziwei_api.js` 保留作后端测试。

## 日历详情神煞吉凶分类错误（17:0x 用户截图报障）
- **症状**：用户截图显示「今日吉神」里混入「月害」「復日」等凶煞；用户说「信息不是丢失就是格式错误」。
- **根因**：`server/routes/calendar.js` 的 `_getLsrGods` 覆盖层在 `_truth` 循环中，把需要「按协纪全量覆盖」的 13 个神煞统一按「成立即吉神」处理。其中 `月刑/月害/重日/復日` 本是凶煞，却错误地 `good.push` + `bad.splice` 移除，导致它们出现在 `goodGods` 里；`天德/天德合/月德/月德合/天赦/天恩/天願/聖心/益後/續世` 才是吉神。
- **触发背景**：2026-07-14 `detail-ui.js` 取消 `GOOD_ALL/BAD_ALL` 白名单过滤后，这些原本被过滤掉的修复神煞全部暴露，后端错误分类才显现。
- **修复**：在 `_truth` 循环前明确定义 `GOOD_GODS` 与 `BAD_GODS` 两套数组，分别处理：吉神成立→补入 good 并从 bad 移除；凶煞成立→补入 bad 并从 good 移除；不成立→从两边都移除。天德仍保留「只增不删」特殊处理（避免误删黄道十二神·天德）。
- **验证**：
  - 本地 `_getLsrGods(2026,7,13)` 实测：good=`金堂,兵吉,解神,四相,製日`，bad=`小耗,九坎,歸忌,五虛,天刑,月害,復日`（月害/復日正确归到凶煞）。
  - 2026-07-08 重日→bad；2026-07-14 月刑/復日→bad；2026-07-25 月害→bad；均符合协纪凶煞定位。
  - 重启服务（start-server.ps1 restart 因 health 误判，改用 stop + 后台 `node index.js`）后 `POST /api/v1/calendar/day` 返回结构正确。
- **教训**：覆盖层处理「是否成立」与「吉凶归属」是两个维度，不能统一按「成立=吉神」处理；前端取消白名单后更要审计后端分类逻辑。


- **症状**：点日历格无详情渲染；页脚格式丢失（裸奔）。
- **关键结论（git diff 自证）**：两个 bug 都与紫微 vanilla 重建**无关**——我对 app.html/app.js 的改动仅限 ziwei 的 link/挂载点/路由，从没碰 `#cal-root` 与页脚。
- **Bug1 根因（点格无详情）**：`web/js/detail-ui.js` 的 `show()` 第一步 `document.getElementById('cal-detail-inline')`（即 `_host()`），容器不存在则 `if(!host) return` 静默退出。而 `#cal-detail-inline` **从未加入 app.html**（HEAD 提交版也没有，全仓 grep 仅 detail-ui.js 自身引用、无任何创建代码）。即 2026-07-14「详情改内联渲染」只重写了 detail-ui.js + 在 calendar.css 加好 `.cal-detail-inline` 样式，**唯独漏加容器**——早于本会话的断头迁移。
- **Bug2 根因（页脚格式丢失）**：`web/css/layout.css`（未提交改动，**非我改**）把页脚样式整段重写——删掉所有旧 `.foot-*` 规则，换成 `.foot-bar`/`.foot-brand`/`.foot-src`/`.foot-pop` 等新类名；但 `app.html` 页脚 HTML 仍用旧类名（`.foot`/`.foot-tab`/`.foot-body`/`.foot-inner`/`.foot-line`）。旧 HTML 无对应 CSS、新 CSS 也无对应 HTML → 页脚裸奔。这是更早一次「页脚重设计」只改 CSS、没把 HTML 同步迁移的半成品（grep 确认新类名无任何 JS/HTML 引用，删之无副作用）。
- **修复（用户选「详情+页脚都修，页脚还原旧CSS」）**：
  - `web/app.html`：`#cal-root` 内、`cal-grid-wrap` 之后补 `<div id="cal-detail-inline"></div>`（calendar.css 样式已就绪，加容器即可渲染）。
  - `web/css/layout.css`：用 Python 脚本精准替换——把当前新页脚块（`/* ---- 固定页脚（常驻细条 + 源流浮层） ---- */` 起到 `body.dark .foot-pop {...}`）还原成 HEAD 原始 `.foot-*` 规则。只还原页脚块，保留同文件其它改动（顶部导航、首页视图等——git diff stat 仍 179 insertions/47 deletions 即证）。
- **验证**：app.html `cal-detail-inline` 计数=1；layout.css 200；新类名（`.foot-bar` 等）已从 diff 的 `+` 行消失、旧 `.foot-tab` 回归；top-nav 改动仍在 diff。服务 health 200。
- **教训**：① 内联渲染改造须「JS 期望的容器 + CSS + HTML 容器」三者齐备，少一个即静默无渲染（detail-ui.show() 的 `if(!host) return` 不报错，极难发现）。② CSS 重构若只改样式不改 HTML，会出现「类名对不上」的裸奔，grep 新类名确认无引用再删最稳。③ 用户报障先 `git diff` 自证改动范围，避免把历史半成品的锅背自己头上。

## 节气顶栏格式丢失 + "今日值神"缺失（18:0x 用户报障）
- **症状**：用户报「每个页面顶栏原本显示『节气 乙未月 本月节气 小暑·大暑』和『今日值神...』带格式，现在格式丢失、今日值神不显示」。
- **节气顶栏根因（已修）**：`web/app.html` 第 65 行 `<div id="jqTopBar" style="font-size:12px;line-height:1.5;"></div>` **只挂内联 font-size，漏挂 `jq-topbar` 类**。而 `web/css/calendar.css`（886-920 行）的样式全是**后代选择器** `.jq-topbar .jq-branch`/`.jq-term`/`.jq-tag` 等，要求祖先有 `jq-topbar` 类才生效。祖先无类 → 全部不生效 → 顶栏裸文字。git HEAD 里**根本没有** `_renderJqTopBar` 函数和 `jq-topbar` 样式（grep 空），说明节气顶栏整套是**未提交工作改动**，且从一开始 #jqTopBar 就漏挂类。与紫微重建无关（我对 app.html 只加 ziwei 挂载点 + cal-detail-inline）。
- **修复**：`web/app.html` 第 65 行改为 `<div id="jqTopBar" class="jq-topbar"></div>`（移除冗余内联 font-size，交还 class 控制；`--text-xs` 等变量均在 variables.css 定义）。calendar-ui.js 的 `_renderJqTopBar` 生成的 `jq-tag/jq-branch/jq-label/jq-term/jq-sep` 现全部被 `.jq-topbar` 后代规则命中。
- **"今日值神"根因（用户选先不做）**：全仓（含 git HEAD/stash/reflog）**从未有任何把「今日值神」渲染进顶栏的代码**。仅两处出现：① `calendar.css:973` 一条**预留注释**（`「今日值神 / 今日九星」门控 + 古籍释义折叠块`，下面没有任何实现规则）；② `detail-ui.js` 日详情卡片的「建除值神·九星值日」。属「规划过但没落地」，非被改没。AskUserQuestion 确认：Q1 选「先不做，只修节气」、Q2 选「每个页面都显示」。
- **"每页都显示"架构事实**：`#jqTopBar` 是 `.main` 的兄弟节点（在 cal-root/bazi-root/ziwei-vanilla-root 之外），**页面切换永不隐藏它**；`calendar-ui.js` 的 `init` 走 `loadAndRender().then()` 异步渲染并调 `_renderJqTopBar` 填充它。故顶栏本就每页可见（深链直跳时最多几百毫秒异步空窗，加载完即出现）。无需额外改——用户选"每页显示"已天然满足。
- **验证**：curl app.html 确认第 65 行已是 `class="jq-topbar"`；calendar.css 200。刷新日历页顶栏恢复金/分隔样式；其它页顶栏同样显示（共用元素）。
- **教训**：CSS 用后代选择器（`.parent .child`）时，必须确认 HTML 祖先元素**确实挂了该父 class**，否则样式整体失效却无任何报错，极易误判为"内容丢失"。

## 顶栏重设计为汉堡抽屉（18:23 用户要求"重新设计标题栏，适配移动端"）
- **要求**：用户发截图（顶栏显示 八卦掌决版 badge + 7 个导航项），要求重新设计标题栏并适配移动端。AskUserQuestion 选定「汉堡菜单」方案。
- **改动文件**：
  - `web/app.html`：顶栏 HTML 重构。品牌改为 `<a class="top-nav-brand">`（含 `.brand-mark` 金底图标方块 + `.brand-text` 标题/副标题 + `.badge` 红底金字「八卦掌决版」）；导航 `.top-nav-tabs` 内 7 个 `tab-item`（id 保留 tabHome/tabCalendar/tabNianli/tabBazi/tabZiwei/tabNote/tabSettings，原 tabHome 是 `<a href="/">` 整页刷新，改为 `<button>`）；新增 `.nav-toggle`（三横汉堡，含 `.nav-toggle-bar`×3，aria-expanded/aria-controls）；顶栏**外**独立放 `.nav-scrim`（遮罩）+ `.nav-drawer`（role=dialog/aria-modal，`drawer-item`×7 带 tabler 图标 + data-nav）。
  - `web/css/layout.css`：
    - 品牌 `.top-nav-brand` 改为自身即链接（去内部 `<a>` 嵌套），加 `.brand-mark`/.brand-text 样式。
    - 删旧的「≤640px 横向滚动」响应式块，替换为：`.nav-toggle`（桌面 display:none）/`.nav-toggle-bar`（三横动画成 X，aria-expanded=true 时旋转）/`.nav-scrim`（fixed 遮罩 + blur，opacity/visibility 过渡）/`.nav-drawer`（右侧滑入，translateX(100%)→0，cubic-bezier(0.16,1,0.3,1) 0.32s，is-open 态）/`.nav-drawer-head`/`.nav-drawer-nav`/`.drawer-item`（hover 金底、is-active 金左边框 inset + 左缩进）/`.nav-drawer-foot` 徽章。
    - 响应式断点改为 **≤760px**：隐藏 `.top-nav-tabs`、显示 `.nav-toggle`、品牌隐藏副标题/分隔/badge。
  - `web/js/app.js`：
    - `initNavigation` 给 `#tabHome` 加 `Router.navigate('home')`（原无监听，靠 `<a href="/">` 整页刷新）。
    - `initRoutes` 注册 `home` 路由（`_highlightFab('tabHome')` + `showCalendarPage()`）。
    - 新增 `initDrawer()`：汉堡点击开关抽屉；遮罩/关闭按钮/Esc 关闭；打开锁 body overflow、focus 首项；`drawer-item` 点击 `Router.navigate(data-nav)` + 关闭；`State.on('route:changed')` 同步抽屉项 `is-active` 高亮。在 boot 的 `initNavigation()` 后调用 `initDrawer()`。
- **设计约束/细节**：抽屉放在 `<header>` 外，避免被紫微全屏 `display:none` 顶栏误伤（汉堡按钮在 header 内，紫微页隐藏后抽屉也用不到，自洽）；全部复用 variables.css token（--color-cinnabar-deep/--color-gold-bright/--bg-card/--border-gold/--radius-*/--space-*/--shadow-xl/--backdrop-blur/--z-overlay/--z-panel）；触摸目标 ≥44px；尊重 `prefers-reduced-motion`（variables.css 已把过渡压到近 0）；ARIA dialog + focus 管理。
- **验证**：`node --check app.js` OK；app.html/layout.css 200；grep 确认 nav-drawer/nav-toggle/drawer-item(×7) 均在 HTML 中。桌面端仍平铺 7 标签；≤760px 折叠为汉堡，点击右侧滑出抽屉；设置项走 `data-nav=settings`→`Router.navigate('settings')`→toggleSettingsPanel（与原桌面 tabSettings 内联行为一致）。

## 「首页」误显示万年历（18:5x 用户报障，紫微/抽屉重建的连带引入）
- **症状**：点「首页」(tabHome) 显示的是万年历页面。
- **根因**：上一轮做汉堡抽屉时，我把 `tabHome` 从 `<a href="/">`（整页刷新回日历）改成 `Router.navigate('home')`，并新注册 `home` 路由——但路由处理函数里错写成 `_highlightFab('tabHome'); showCalendarPage();`。即「首页」路由直接渲染了万年历，`Router.navigate('home')` == 显示 cal-root。
- **历史背景**：原 HEAD 里 `tabHome` 是 `<a href="/">`、且**根本没有 `home` 路由**；`web/css/layout.css:374` 的 `.home-root` 整套首页样式（hero 图标/标题/分隔线/四个入口卡 `.home-entry`/日期选择器 `.home-picker`）早已写好但**从未有对应 HTML/JS**——又一处「规划好没落地」的半截功能。所以"首页"本应是独立落地页，而非万年历。
- **修复**：
  - `web/app.html`：`.main` 内、日历区之前插入 `<div id="home-root" class="home-root" style="display:none;">`——含 `.home-hero`（.home-brand-icon 用 web/img/home.webp + 标题"天地玄黄·日月更替" + tagline + 红底 badge）、`.home-divider`、4 个 `.home-entry`（data-nav=calendar/bazi/ziwei/note，各带 tabler 图标+标题+描述+箭头）、`.home-picker`（公元年/月/日三 select #homeY/#homeM/#homeD + 「查看日课」按钮 #homeGo）。
  - `web/js/app.js`：① `home` 路由改为 `_highlightFab('tabHome'); showHomePage();`；② 新增 `showHomePage()`（隐藏 cal-root/bazi-root/ziwei-root/ziwei-vanilla-root/detail-root、显示 home-root、`State.emit('page:changed','home')`）；③ 新增 `initHome()`（绑定 4 个 `.home-entry` 点击→`Router.navigate(data-nav)`；填充 #homeY(1900-2100)/#homeM(1-12)/#homeD(按年月算天数，月/年 change 时重算)，#homeGo 点击→`Router.navigate('calendar')`+`State.emit('home:goto-date',{y,m,d})`，复用 calendar-ui 既有 `home:goto-date` 监听定位并展开当日详情）；④ boot 的 `initDrawer()` 后调用 `initHome()`。
  - **行为变化**：应用落地页从「万年历」变为「首页」——符合用户"首页≠万年历"的预期；万年历仍由「年历」tab 进入。
- **验证**：`node --check app.js` OK；app.html/layout.css 200；curl 确认 `home-root`(1)、`home-entry`(×4)、`id="homeGo"`(1) 均已写入；app.js 中 `showHomePage` 在 home 路由引用(165) 且定义(242)。
- **教训**：新增路由时处理函数必须渲染该路由应有内容，切勿复制粘贴别的页面函数；「孤儿 CSS」(如 .home-root) 往往是未落地功能，遇到导航概念不一致时优先 grep 是否存在对应 HTML/JS。
- **待演示**：预览面板是桌面宽度，汉堡需浏览器设备模拟（≤760px）或真机查看。

## 「首页在每个页面都显示」(19:0x 用户报障 — 上一轮首页落地的遗漏)
- **症状**：从首页点任意 tab（年历/八字/紫微/节气/记事），首页内容仍叠在当前页上。
- **根因**：上一轮把 `#home-root` 作为新页面根加进 `.main`，`showHomePage()` 正确地把其它根都藏了，但**所有"其它页面"的切换函数反过来都没藏 home-root**——home-root 是后加的，老函数不知道它的存在。逐一确认漏藏点：`showCalendarPage`(app.js) / `showZiweiPage` iframe(app.js) / `ziwei-ui.js show()` / `bazi-ui.js show()` / `nianli-ui.js show()` / `note` 路由 handler，共 6 处。本质=经典的「新增兄弟根，忘了更新所有兄弟的隐藏清单」。
- **修复（集中式，用户选方案 A + 保持首页落地页）**：
  - `web/js/app.js`：新增唯一权威 `setActivePage(targetId)`（+ 暴露 `window.__setActivePage`），内部 `PAGE_ROOTS=['home-root','cal-root','bazi-root','ziwei-root','ziwei-vanilla-root','detail-root']` 先全 hidden 再只显示目标根。`showCalendarPage`/`showHomePage`/`showZiweiPage` 改为调 `setActivePage`；`nianli`/`bazi`/`ziwei` 路由 handler 在 `loadModule` 前**立即** `window.__setActivePage(对应根)`（消除异步加载空窗闪现 + 模块加载失败也不卡首页）；`note` 路由 handler 调 `window.__setActivePage('cal-root')`（记事是日历覆盖层）。
  - `web/js/ziwei-ui.js`：`show()` 改 `window.__setActivePage('ziwei-vanilla-root')`（删原 `['cal-root','bazi-root']` 手动隐藏）。
  - `web/js/bazi-ui.js`：`show()` 改 `window.__setActivePage('bazi-root')`（删原 cal/ziwei 手动隐藏）。
  - `web/js/nianli-ui.js`：`show()` 改 `window.__setActivePage('cal-root')`（删原 cal/bazi/ziwei/detail 手动隐藏，保留 detail-root.classList.remove('show') 复位）。
- **验证**：4 文件 `node --check`（app.js CJS / 三个模块 `--input-type=module`）全 OK；资源 200；grep 确认 7 处切换路径全部 `__setActivePage`/`setActivePage` 调用；残留 home-root/cal-root 引用仅注释、PAGE_ROOTS 定义、initHome 绑定查找。
- **教训（铁律）**：本项目「页面根」是兄弟节点平铺在 `.main`，切页靠 `display` 互斥。**任何新增页面根，必须在 `PAGE_ROOTS` 一处登记**，绝不在各 show 函数里手写兄弟隐藏清单——否则必漏。这套 `setActivePage` 现在是唯一权威。

## 节气页残留详情 + 设置页删 10 项标签开关 (19:2x 用户报障)
- **节气页残留详情**：`nianli-ui.js show()` 复用 `cal-root`（年历渲染进 `#Cal3`），而内联日详情 `#cal-detail-inline` 是 `cal-root` 子节点。万年历点过某天后 `detail-ui.js` 把内容塞进 `#cal-detail-inline` 并加 `.show`；切到节气时 `setActivePage('cal-root')` 显示整个 cal-root，`#Cal3` 被年历替换但 `#cal-detail-inline` 旧详情未清 → 残留在年历下方。旧 `_closeDetailIfOpen()` 只关模态 `detail-root` 浮层，从没碰内联详情。
  - 修复：`nianli-ui.js show()` 切节气时显式 reset `#cal-detail-inline`（remove `.show` + `aria-hidden` + `innerHTML=''`）；`app.js _closeDetailIfOpen()` 一并复位内联详情（堵「日历→首页→返回日历」残留）。
- **设置页删 10 项标签开关**：10 项是「无禄日/月倒家杀/年倒家杀/金神七煞/五富日/四离四绝/天德/月德/天赦/杨公忌」badge 开关（`app.html` settingsPanel 旧 lines 192-261）。核实这些开关是**失效死代码**——calendar-ui.js 按 `dayData` 无条件渲染 badge，全仓无任何消费 `settings:changed` 的逻辑，故删开关后 badge 本就常显（满足"保持常显"）。
  - 修复：`app.html` 删 10 个 `<div class="st-row">` badge 块；内联脚本同步删 10 条 `initToggles` 的 `getElementById(...).checked`（否则元素消失后抛错、整个设置面板崩）与 10 条 `change` case；`refreshCalendar` 的 data 对象清空为 `{}`。**保留字号/暗色/授权三行**（暗色开关 `togDarkMode` 仍由内联脚本 + settings-ui.js 共用，正常）。
- **验证**：`node --check` app.js(CJS)/nianli-ui.js(ESM) OK；curl 确认 10 个 badge id 残留=0、设置面板保留 3 行、togDarkMode 仍在、app.html/app.js/nianli-ui.js 均 200。
- **教训**：内联日详情 `#cal-detail-inline` 属于 cal-root 子树，凡复用 cal-root 的页面（年历）切走时都要 reset，不能只依赖模态浮层的关闭逻辑。删除带 id 的开关 UI 时，必须同步清理脚本里对该 id 的 `getElementById`，否则运行时抛错。

## 紫微斗数文墨天机风格 P1 开工（20:11 用户发截图并要求按图开工）
- **关键修正**：文墨天机截图默认盘面是**地支罗盘方位**（命宫随实际地支落位，子北午南），不是「命宫固定左上」。当前 `BRANCH_POS` 本就按地支落位，所以 P1 保留现有方位布局，重点做表单升级 + 方位文字 + 中央信息区 + 底部 Tab 壳。
- **已拍板**：地支罗盘方位布局；底部 Tab `[常用功能/飞星/三合/四化]` + 左侧 `[天盘/地盘/人盘]`；P1 先做表单/方位/Tab 壳/中央信息区，飞星/三合/四化叠加放 P2。
- **改动文件**：
  - `server/lib/true-solar.js`（新建）：真太阳时换算（经度修正 + Meeus EoT），供 ziwei 复用。
  - `server/routes/ziwei.js`：入参升级 `hour/min/jd/wd/zty`（兼容旧 `time` 序号）；真太阳时修正后算时辰序号；扩展 meta（clockTime/trueSolarTime/location/fourPillars/yearMutagen）；序列化 palace 新增 `jiangqian12/suiqian12/ages`。
  - `web/js/region-cascader.js`：支持可选 `prefix`，避免紫微/八字同时存在时 `rcProvince/rcRegion/Jd_input/Wd_input` ID 冲突；`_rcSelectByRegion` 支持带 prefix 选中。
  - `web/js/ziwei-ui.js`：表单改 HH:MM + 地区级联（prefix `zw-`）+ 真太阳时开关 + 最近城市快捷；左侧运限改名为天/地/人盘；底部 Tab 栏；中央信息卡扩展（四柱/命主身主/真太阳时/大限起运表/年干四化）；宫位内增加大小限年龄、博士/长生/将前/岁前辅助信息。
  - `web/css/ziwei.css`：新增地区级联/真太阳时开关/最近城市/底部 Tab/方位文字/信息卡扩展/宫位辅助信息/四化 Tab 等样式。
- **验证**：`node --check` 通过 `true-solar.js`、`ziwei.js`、`ziwei-ui.js`；后端 `/ziwei/astrolabe` + `/ziwei/horoscope` 新旧参数均 200；真太阳时开关生效（北京示例：clock 19:54 → true 19:33，offset -1215s）；静态资源 200；预览面板已打开 `#ziwei`。
- **待 P2**：飞星/三合 SVG 叠加、四化视图高亮、宫位庙旺利陷、时间步进控件。

## 紫微斗数 P2：飞星/三合/四化叠加（21:01 用户「P2继续」）
- **后端**：`server/routes/ziwei.js` 的 `serializeStar` 及 `serializePalace` 内联映射新增 `brightness`（庙旺利陷，iztro 星体自带字段，值如 庙/旺/得/利/平/陷）；重启服务。坑：serializeStar 改了但 palace 的 major/minor 用的是内联映射，漏带 brightness，需两处都加。
- **前端** `web/js/ziwei-ui.js` 重写关键部分：
  - 视图面板 `common/feixing/sanhe` 各自内嵌 `zw-chart-canvas`（grid + `svg.zw-overlay` 绝对叠放 viewBox 0..100）+ 方位文字 + 图例；`renderChart()` 改为 `renderChartInto` ×3（共用 12 宫渲染）。
  - **飞星**：`computeFlyingStars` 取每宫 `heavenlyStem`(宫干)→ 查 `MUTAGEN_MAP` 得禄权科忌星名 → 由 `computeStarToPalace`(星名→宫index，主星优先) 定位飞入宫；同宫=自化。SVG 弧形箭头（向外弯曲少压字）+ 自化环；`zw-fly-chip` 禄权科忌筛选（默认全显，点选隐藏）。
  - **三合**：`PALACE_SEQ`(命1…父12) 算三方四正 = 本宫+4/+8 三合 + +6 对宫，再映射回地支落位画连线；**全部淡线作底图 + 选中宫朱砂高亮**（默认命宫），点宫位切换高亮。注意：不能用 palace 数组 index（+4/+8），必须用宫序，因命宫不固定在 index0。
  - **四化 Tab**(`_renderSiHua`)：增强显示四化星所在宫位（`zw-sihua-loc`）。
  - **庙旺利陷**：星名后加 `sub.zw-bright`（庙旺得利绿/陷红）。
- **CSS** `web/css/ziwei.css`：`.zw-chart-canvas/.zw-overlay` 叠放； palaces z2 / overlay z3 / info-card z4（箭头压宫位、信息卡压箭头，中央干净）；`zw-fly-chip/legend-note/zw-bright/zw-sihua-loc` 样式。
- **验证**：`node --check` 通过；资产 200；用真实 API 数据跑飞星/三合逻辑——飞星 48 条(3 自化)抽样 `戊宫化禄=贪狼→夫妻(未)`、`化权=太阴→子女(午)` 正确；命宫三方四正=财帛(巳)+官禄(丑)+迁移(卯) 正确；12 宫全部映射地支无遗漏。
- **P2 未做（留 P3）**：时间步进控件（流月/流日/流时）、文墨天机视觉精修、移动端单栏细化。
- **铁律**：飞星几何用宫干→星名→星所在宫；三合几何必须用宫序(命1…父12)而非数组 index；SVG 叠加层与 grid 共用 `.zw-chart-canvas` 容器以对齐坐标。

## 紫微 P3（最终渲染落地，2026-07-18 晚）
用户确认三决策：①飞星默认只画「命宫飞化+大限飞化」(+自化环)；②四化 A/B/C/D 标在星曜旁；③时间步进留 P4。
- **飞星**：`state.flyScope='ming-da'|'all'`；`drawFlyingOverlay` 经 `flyingLinksFiltered` 过滤（命宫 index + `getDaIdx()`=`horoscope.decadal.index`）；顶部加 `.zw-fly-scope-btn` [命宫+大限(默认)/全部飞化]。
- **四化**：四化视图面板改为「盘面图 + 表格」(`_chartCardHTML('sihua')` + `#zw-sihua-card`)；年干四化星在星曜名旁标 A/B/C/D(`zw-sihua-letter`)；表格字母行可点击，`state.sihuaFilter` 联动盘面 `.zw-dim` 变暗聚焦。
- **三合**：`drawSanheOverlay` 改为虚线(`stroke-dasharray`)，移除全宫淡线底图，仅高亮选中宫三方四正。
- **宫位 5 层**：顶行宫名+干支、年龄行(大限+小限)、主体星曜、神煞分区(左长生/右博士·将前·岁前 `.zw-extras-left/right`)。
- **中央信息区**：补身宫名、真太阳时偏移(较钟表±分)、年干四化双栏标注(`.zw-info-sihua-label`)。
- **`_calc` 始终加载 horoscope**（取大限命宫 index 供飞星用）。
- **验证(真实数据 1982-05-10 12:20 男)**：命宫+大限飞化=8条(1自化) vs 全48条；壬年四化 A天梁→疾厄/B紫微→官禄/C左辅→财帛/D武曲→命宫，与古籍清单一致。资产 200，node --check 通过。
- **P4 待做**：顶部时间步进(大限/流年/流月/流日/流时 ←→)、文墨天机视觉精修(朱砂/石青/金箔配色)、移动端单列/横屏、逐项对比验证页。

## 紫微 P3 后 bugfix（2026-07-18 晚）
用户反馈：① 飞星盘点击某宫，巨门/天机/太阴/天同等目标星不高亮；② 文字排版应"有的竖版有的横版"。
- **bug①根因**：`renderChartInto` 与 `drawFlyingOverlay` 飞星分支先 `flyingLinksFiltered`(只留命宫+大限) 再 `filter(from===flyPalace)`；点非命宫/大限宫时链接被第一步清空 → `flyTargets` 空 → 不高亮。
- **修复**：`state.flyPalace!=null` 时直接用 `comp.links.filter(from===flyPalace)`，不受 ming-da 范围限制；默认(`flyPalace=null`)仍用命宫+大限高亮。点击任意宫都能高亮该宫宫干四化飞出的四星曜+画箭头。
- **排版②**：宫位星曜原全 `flex-wrap` 横排。改为 `.zw-stars:not(.zw-adj)` 竖列右对齐(主/辅/运星=竖版) + `.zw-adj` 杂曜横排(横版)，宫名/干支/年龄/神煞本就横排 → 文墨天机式竖横混排。
- **验证(真实数据)**：点击官禄/迁移/疾厄/财帛/子女/兄弟/命宫/福德，巨门/天机/太阴/天同均可正确成为飞化目标高亮并定位(仆役/田宅/兄弟宫)；node --check 通过；资产 200；纯前端静态改动刷新即生效。


## 生成扫码即装 PWA（23:xx）
- 用户要求"生成扫码即装的 PWA"。前端 `api.js` BASE=`/api/v1`（同源 HMAC 签名），强依赖后端，故 PWA 必须同源带后端 → 采用**局域网同源**方案（手机同 WiFi 扫码访问 `http://<LAN_IP>:3000/app.html`），非纯静态部署。
- LAN IP = **192.168.1.7**，扫码地址 `http://192.168.1.7:3000/app.html`。
- 补齐的 PWA 三件套（此前 `app.html` 已引用 `/manifest.webmanifest` 但文件缺失，典型半截迁移）：
  - `web/manifest.webmanifest`：name/short_name/start_url=/app.html/scope=/display=standalone/theme=#3C2415，icons 含 **PNG 192+512(any)+512(maskable)** + SVG 兜底（Chrome/Android 安装硬性要求 PNG，纯 SVG 部分 WebView 拒装）。
  - `web/sw.js`：APP_SHELL 预缓存 + 导航 network-first 回退 /app.html + 同源静态 SWR + `/api/v1` network-only（HMAC 不缓存）。
  - `web/icons/`：纯 Python 无依赖生成 PNG 图标（gen_icons.py，金箔徽记）+ 原有 SVG。
- server/index.js：`/manifest.webmanifest`(application/manifest+json) 与 `/sw.js`(text/javascript + `Cache-Control: no-cache` + `Service-Worker-Allowed: /`) 路由必须在 `express.static` 之前（否则被静态中间件覆盖、SW 头丢失）。
- 前端注入：`app.html`/`index.html` 加 `apple-touch-icon` **必须 PNG**（iOS 不支持 SVG apple-touch-icon，否则回退截图）；`theme-color` 必须字面量（不能写 `var()`，浏览器忽略）。两文件均注册 `/sw.js`。
- 二维码：`segno` 经**绕过代理直连**（`unset http_proxy` + `--proxy ""`）安装成功；生成 `web/pwa-qrcode.png`（深墨/米白高对比 444×444）。
- 验证：manifest JSON 合法且 INSTALLABLE_OK；sw.js 语法 OK；所有图标/QR 200；SW 头正确；nodemon 热重载无回归。
- ⚠️ **已知限制（非 bug）**：局域网 HTTP 下，iOS Safari「添加到主屏幕」可用；Android Chrome 自动安装横幅需 **HTTPS**（HTTP 仅能菜单「添加到主屏幕」作快捷方式）。如需真·安装提示，需公网隧道(如 cloudflared)+证书，留作后续可选项。

# 2026-07-18 紫微格局判定（patterns）前端渲染落地

## 背景
后端 `server/ziwei_patterns.mjs`（传统派/倪师天纪纯函数判定器，47 格：甲级吉22+乙级吉7+甲级煞12+乙级煞5+日月反背）已在 `/api/v1/ziwei/astrolabe` 的 `data.patterns` 返回，活接口验证 3 项（铃贪格甲/英星入庙甲/辅弼拱主乙）。前端此前未渲染该字段。

## 改动（web/ 静态，刷新即生效）
- `web/js/ziwei-ui.js`：
  - state 新增 `patternFocus`（格局高亮宫名集合）。
  - `buildShellHTML` 右侧栏新增 `_patternsBlockHTML()`（格局判定卡 `#zw-patterns-card`）。
  - 新增 `_renderPatterns()`：读 `state.astrolabe.patterns`，吉格在前煞格在后，渲染名字/等级/说明/相关宫；`_markActivePattern()` 标当前高亮项。
  - 点击格局 → 若其 `palaces` 含真实宫名则高亮该格局相关宫（非相关宫 `zw-dim-pattern` 变暗）并自动切「常用功能」视图；再次点击同项取消；手动点宫位取消格局高亮。
  - `_calc` 排盘后调 `_renderPatterns()`；`state.patternFocus` 重排时清空。
  - `buildAiPrompt` 追加「格局判定」段，AI 解读可引用 detected 格局。
- `web/css/ziwei.css`：新增 `.zw-patterns-card/.zw-pattern(-ji/-sha/-active)/.zw-pattern-cat/.zw-pattern-desc/.zw-pattern-pal` 及暗色适配；`.zw-palace.zw-dim-pattern` 变暗态。

## 验证
- `node --check web/js/ziwei-ui.js` 通过；DOM 桩动态 import 模块无顶层错误、导出完好。
- 活接口 `verify_ziwei_patterns_live.mjs` 仍返回 3 项 patterns（后端未动）。
- 静态服务 `:3000` 取回 `ziwei-ui.js`(含 `_renderPatterns`)、`ziwei.css`(含 `zw-patterns-card`)、`app.html` 均 200。
- ⚠️ 纯前端交互（点击格局高亮联动）未在真实浏览器跑过，仅做了语法/DOM桩/静态服务三层验证；如需可再用 browser-use 做端到端点击验证。
