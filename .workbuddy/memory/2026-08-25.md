# 2026-08-25 工作日志

## 页眉重设计 · 新一轮整合效果图

- 之前已出 A/B/C/D/E/F/G1/G2/G3/G4 共 14 版孤立品牌区，用户均不满意。
- 经 AskUserQuestion 确认：用户真正需要看**完整页眉 + 右侧导航标签**在真实页面里的整合效果；图形气质接受「罗盘/星轨、玉璧/古钱、竹简/金石」。
- 本次交付：
  - `web/_preview_header_full.html`：复用真实 `layout.css`，在同一真实页面语境下并排展示 H1（罗盘星轨）、H2（玉璧古钱）、H3（竹简金石）三套完整页眉，各带右侧 7 个导航标签和模拟内容条。
  - `server/shot_header_full.js`：用 Playwright 截取真实当前页眉基线 `real_header.png` 与整合预览 `header_full_preview.png`。
- 未改任何应用文件，待用户确认方案后再动手。

## 落地 E 版（八卦徽标）到正式文件
- 用户确认"用文案E" → 采用 E（八卦徽标：三爻，上下阳爻、中阴爻）作为页眉品牌标识。
- 改动 `web/app.html`：顶部 `.brand-mark` 与移动端 `.nav-drawer-brand` 的原 `ti-calendar-time` 图标替换为内联 SVG 三爻卦象（fill #D4A853，嵌进原金调圆角芯片）。
- 改动 `web/css/layout.css`：新增 `.top-nav-brand .brand-mark svg`（22px）与 `.nav-drawer-brand svg`（22px, flex-shrink:0）尺寸规则。
- 验证：curl 200；`ti-calendar-time` 在 app.html 已归零；`server/shot_header_E.js` 截真实页眉确认渲染（`header_E_real.png` / `header_E_brand.png`）。
- 备注：用户用词"文案E"偏指"方案E"，已按视觉 E 落地；若其本意是改品牌文字（天地玄黄/副标/徽章文案），需进一步确认。
