# 2026-09-09 工作日志

## Phase 3.2.3 — 本命合参扩展：年命纳音（五维度，已完成）

> 用户选「年命纳音扩展」。在 Phase 3.2.2 五维度(地支 日/年/课年) 基础上，纳入标准六十甲子纳音表，新增年命纳音生克维度（日纳/年纳）。

### 改动（web/js/choose-ui.js，纯前端）
- 新增 `NAYIN`（60 项标准经典纳音表，非捏造，校验 60 项齐全 + 抽样 丙午水/庚午土/甲子金）、`WX_SHENG`/`WX_KE`、`_nayinRelation(dNayin,yNayin,ctx)`：仅标 日课克本命(纳音克,凶,-10)/日课生本命(纳音生,吉,+3)/同五行(比和,+2)；本命克/生日课(我克/我生) 中性不标。
- `_applyBenming`：第 3 参由「年命地支」改为「年命完整干支」(取[1]做地支、查 NAYIN 做纳音)；新增 `relDayN`(日纳)/`relYrN`(年纳)；权重统一 `WMAP={三合6,合6,生3,比和2,冲-8,刑-4,害-4,克-10}`。
- `_dayCardHtml`：`ctxTxt` 补 `日纳→日课纳音`/`年纳→年命纳音`；`_renderResultsHtml` 提示补「含日课年柱冲合与年命纳音生克」；`_query` 传完整年命干支 `userYearPillar`。

### CI
- `verify_choose_ui.js`：沙箱导出加 `_nayinRelation`/`NAYIN`；新增纳音表 60 项+抽样、纳音克/生/比和/中性、五维度合并(戊辰上浮居首/庚午 纳音克+冲 下沉/不改 grade)。现 22 项全绿。
- `check_choose_ui.js`（Playwright 6/6）：预置 丙午(水)；2026-10 候选日课年柱=丙午(水) → 年纳比和，断言 `/纳音比和/`。
- ⚠️ 两个测试预期初写反（木生水应为水生木；水→木 是日课生本命应标吉非中性）→ 修正为 金生水 + 中性用 本命生日课(木→火)/本命克日课(金→木)。
- 完整 `npm test` 17/17 全绿。server 因隔夜退出重启 PID 869（仅前端改动本不需重启，e2e 需运行 server）。

### 文档
- `server/Phase3.2.3_本命合参扩展(年命纳音)方案.md`。任务 #213 completed。

### 设计取舍
- 纳音严格按标准六十甲子表（非捏造）；生克仅「日课对本命」生/克/比和 标，中性（本命克/生 日课）不标。
- 与地支维度一致：仅软排序、不改 grade（纳音克 凶 仅 −10 下沉，不硬排除为「忌」）。
- 本命合参现共五维度：地支(日支vs年命/年支vs年命) + 纳音(日柱纳音vs年命纳音/年柱纳音vs年命纳音)，均仅参考。

## Phase 3.2.4 — 本命合参扩展：纳音双参照（年命 + 日元，七维度，已完成）

> 用户点名开放项②「候选日年柱纳音 vs 日元纳音等更细维度」。纳音参照从「仅年命」扩为「年命 + 日元」双参照，与地支三维(日/年/课年) 对称。

### 改动（web/js/choose-ui.js，纯前端）
- `_applyBenming` 增**第四参 `userDayPillar`**（本命日柱完整干支，算 `userDayNayin`）；新增 `relDayN2`(ctx=日纳元: 日柱纳音vs日元纳音)/`relYrN2`(ctx=年纳元: 年柱纳音vs日元纳音)。现**七维度**：地支(日/年/课年) + 纳音(日纳/年纳/日纳元/年纳元)。
- `_query`：解析保留 `userDayPillar`（校验 `NAYIN[值]` 合法），传第四参；提示补「年命与日元纳音生克」。
- `_dayCardHtml`：ctx 文案映射加 `日纳元→日柱纳音·日元`/`年纳元→年柱纳音·日元`。

### CI
- `verify_choose_ui.js`：扩七维度断言（纳音双参照 克/生/比和/中性 + `_applyBenming` 七维度合并标注、不改 grade）。现 **26 项全绿**。
- `check_choose_ui.js`（Playwright 6/6）：种子 甲子|丙午；七维度实际渲染(本命标签=17)、无控制台/页面报错。
- 完整 `npm test` **17/17 全绿**。server 重启 PID 1282（仅前端改动，e2e 需运行 server）。

### 设计文档
- `server/Phase3.2.4_本命合参扩展(纳音双参照_日元).md`。任务 #214/#215 completed。

### 设计取舍
- 纳音双参照 = 年命(根基) + 日元(自身)，对称地支三维参照；信息更完整。
- 仍仅软排序不改 grade（纳音克 −10 下沉不硬排除）；中性态(本命克/生日课)不标。
- 地支三维已是「vs 日元/vs 年命」，本次仅把纳音参照从单一年命补齐为双参照。

## Phase 3.3 — 择日助手升级为「独立页面」（已完成）

> 用户要求：点击「择日」单独跳出一页 + 有返回通道。对接确认：返回通道=页面内返回按钮+浏览器后退+ESC；入口=日历头部按钮+首页快捷入口+抽屉导航；默认月份=跟随日历当前查看月份；URL 纯 `#choose` 不深链。

### 改动
- `web/app.html`：cal-root 后新增 `<div id="choose-root" style="display:none;">`；首页 `home-entry data-nav=choose`；抽屉 `drawer-item data-nav=choose`。
- `web/js/app.js`：`PAGE_ROOTS` 增 `choose-root`；`_moduleLoaders` 增 `choose`；`initRoutes` 注册 `choose` 路由（`__setActivePage('choose-root')` + `loadModule('choose').show()`，`_highlightFab('tabCalendar')`）。
- `web/js/choose-ui.js`：去掉内联 `togglePanel/ensurePanel`；`init()` 择日按钮改 `Router.navigate('choose')`；新增 `show()`(渲染整页到 choose-root + 返回栏 + 控件 + 结果、ESC 监听、`_last` 状态复原)/`back()`(navigate calendar)/`_onEsc`/`_restoreControls`；`_pageHtml` 替代 `_panelHtml`（含「返回」按钮栏）；`_query` 记录 `_last`；「查看日课」→`navigate('calendar')`+`selectDay`(setTimeout 30ms)；导出加 `show,back`。
- `web/css/calendar.css`：`.choose-panel`→`.choose-page` 整页布局（去 `max-height` 滚动）；新增 `.choose-page/.choose-page-bar/.choose-back-btn` 样式 + 暗色覆盖。

### 关键约定
- 复用既有 hash 路由 + `PAGE_ROOTS` 页面模型（与 note/ziwei 同款）；返回通道三条全部基于 `Router.navigate('calendar')` 或 hashchange。
- 默认月份读日历 `Cal_y`/`Cal_m`（calendar-ui 维护），实现「跟随日历当前月份」。
- `_last` 模块级状态：重新进入择日页直接复原上次查询结果与控件，不重查。
- ESC 监听幂等：仅在 `Router.current()==='choose'` 时触发返回。

### 测试
- `check_choose_ui.js`（Playwright 10/10）：跳 `#choose`、返回按钮回 `#calendar`、查看日课跳转并展开详情、七维度渲染、`无控制台报错`。
- 完整 `npm test` **17/17 全绿**（前端静态改动，e2e 用运行中的 server PID 4252）。

## 首页图标重设计（已完成）

> 用户要求：首页「万年历」「择日助手」「八字排盘」「紫微斗数」「每日记事」5 个入口图标要有辨识度，并给文案。

### 改动
- `web/app.html`：把 5 个 `home-entry-icon` 中的通用 Tabler 字体图标替换为**自定义 SVG 图标**：
  - 万年历 = 日历页 + 月牙（点出农历/阴阳合历）
  - 择日助手 = 罗盘 + 天池指针（朱砂红 `#C0473B`）
  - 八字排盘 = 四柱碑 + 干支短线
  - 紫微斗数 = 命盘圆 + 紫微星芒 + 十二宫刻度
  - 每日记事 = 便签页 + 完成勾
- `web/css/layout.css`：补 `home-ic-choose` 背景色（原缺失导致择日图标接近不可见）；新增 `.home-entry-icon svg { width:27px; height:27px; }`。

### 设计统一原则
- 白线描 + 彩色方块，与首页卡片配色一致；统一 24×24 viewBox、1.8px 描边、圆角端点。
- 每个图标用 2-3 个记忆点形成独特剪影：万年历（月牙）、择日（罗盘）、八字（四柱）、紫微（星盘）、记事（勾选）。

### 验证
- Playwright 截图 `server/_shots/home_icons.png`：5 图标全部渲染为 SVG，无控制台/页面报错，暗色主题下清晰可辨。

## 部署：Render.com 免费云（进行中）

> 用户要求：把万年历网页部署到免费服务器，手机/电脑都能看。选定 Render.com（从 GitHub 自动部署）。

### 已做
- `server/package.json` 增 `engines.node: ">=18"`。
- 仓库根新增 `render.yaml`（Render Blueprint）：`type:web, runtime:node, rootDir:server, plan:free, buildCommand:npm install, startCommand:npm start, healthCheckPath:/`。
- 本地提交 `8922052`（deploy: 增加 render.yaml 与 engines）。
- 确认 `core/ web/ server/` 均已被 git 跟踪 → Render 构建能拿到完整代码。
- 确认 HMAC 密钥是代码内置公开默认值 → 公网匿名访问无需改代码。
- 确认 `/` 已是 200 着陆页(web/index.html)，部署入口即域名根，无需改路由。

### 阻塞
- 本机 ssh-agent 的 key 无权访问 `github.com:vincentwony/dingdangmao.git`（Permission denied）。**需用户自行 `git push origin main`**（用其有权限的 key），再在 Render 后台 New→Blueprint 选本仓库部署。
- 注意：`runtime: node` 若 Render 不识别可改 `env: node`。

### 免费档注意
- Render free 计划 15 分钟无访问自动休眠，首次打开冷启动数秒。
