# 2026-07-14 工作日志

## 网页神煞展示策略调整（用户决策：显示全部神煞）
- 背景提问：用户问"所有的更改在网页中是否都要完全呈现"。经排查数据链路，发现日详情"今日神煞"卡片原本只渲染 GOOD_ALL/BAD_ALL 白名单（约 40 个），导致本回合修复的 圣心/益後/續世（及 天恩/天願/重日/復日/鳴吠對）虽后端算对但网页不可见。
- 用户选择"显示全部神煞"。改动 `web/js/detail-ui.js` 的 `renderShensha()`：去掉对 GOOD_ALL/BAD_ALL 的遍历过滤，直接渲染 `dat.goodGods`/`dat.badGods` 全部。GOOD_ALL/BAD_ALL 保留为参考清单并加注释。
- 数据链路：app.js 动态 import detail-ui.js → show() 调 POST /calendar/day → 返回 goodGods/badGods（含覆盖层修正值）→ 全量渲染。
- 入口页 web/app.html，web/ 由 server/index.js 静态服务，刷新即生效，无需打包。
- 月视图网格仍只显示建除/节气/节日，不显示神煞列表。
- 验证：重启服务 PID 1416（端口 3000）；POST /calendar/day 在 2024-1-13 含 續世、2024-8-7 含 益後、2024-10-18 含 聖心，2024-10-12 正确不含 聖心。
- 注：`goodGods`/`badGods` 为 MD 模式（不含年神）；年神（太岁/岁破/飞廉等）本就未进该卡片，与本回合修复无关。

## 日历页面 UI 升级（Phase 1+2：月历网格 + 神煞墙）
- 用户诉求：重新迭代升级美化日历页面。经分析确认方向：**深化古典金(OWSPACE) × 全区域升级 × 神煞吉凶配色+分组折叠**，先做月历网格+神煞墙。
- **月历网格**：`calendar-ui.js` `renderMonth/renderDayCell/renderEdgeCell/renderStaticCalendar` 由 `<table>` 改为 CSS Grid（`#Cal3 .cal-weekhead` + `#Cal3 .cal-body`，7 列 `repeat(7,1fr)` gap）。单元格 `.cal-cell` 改为 flex 纵向卡片（阳历数字图/农历/干支/建除/徽章/朱砂印）。新增：
  - **「今天」高亮锚点**：`.cal-cell.today` 金环(inset box-shadow `var(--color-today)`) + `.today-mark` "今" 字，与选中金块区分。
  - **月份切换箭头**：`#btnPrevMonth`/`#btnNextMonth`（ti-chevron）绑定 `changeMonth(±1)`，已在 app.html 头部加按钮。
  - 模块级 `_today` 记录真实今天。
- **神煞墙**：`detail-ui.js` 新增 `buildSsGroup()` + 全局 `window._ssToggleMore()`。吉神=金(`good-tag`)、凶煞=朱砂(`bad-tag`)，改用 `variables.css` 新增令牌 `--ss-good-*`/`--ss-bad-*` **去渐变、提对比**（符合零渐变原则）。单组 >12 个时默认折叠 `max-height` + 「展开全部 N 位」按钮。
- tokens：`variables.css` 增 `--color-today`/`--ss-good-*`/`--ss-bad-*`（明/暗双主题）。
- 验证：`node --check` 通过；无残留 `<table>/<td>`；server(PID 3944) 静态服务已含新代码（curl 命中 cal-weekhead/_ssToggleMore/color-today/btnPrevMonth）。
- 未做（本轮范围外，待 Phase 4）：页脚/公告栏重排（节气顶栏 #jqTopBar、吉神公告栏 #jishenBanner、固定页脚 .foot 仍在 Phase 1+2 之外）。

## 日历页面 UI 升级（Phase 3：顶部导航）
- 用户诉求：继续 Phase 3 顶部导航改造。方向沿用「深化古典金」。
- **语义统一**：原 `tabHome` 是 `<a href="/">`（整页刷新、无高亮），与其余 6 个 `<button>`（走 `Router.navigate`）混用。改为 `<button>`，并新增独立 **home 路由**（`app.js` 注册 `home` → showCalendarPage + 高亮 tabHome；`router.js` 初始 `navigate('home')` 且 hash 兜底改为 `home`）。默认落地项由"年历"变为"首页"，7 项全部为 SPA 按钮，无整页刷新。
- **桌面精炼**：`layout.css` 去掉 `.tab-item` 间的 `::after` 分隔竖线；hover 改悬浮药丸底色、active 改"金底色药丸 + 底部金下划线(`::after`)"；品牌标题去内联样式、CSS 化（`.brand-title`/`.brand-sep`/`.brand-sub`），更优雅。7 项各加 Tabler 图标（ti-home/calendar/sun/directions/stars/notebook/settings），**桌面隐藏图标、仅文字**，不增高度。
- **移动端横滑 chip（≤640px）**：`.top-nav-tabs` 改 `overflow-x:auto` 横向滚动容器 + `scroll-snap`，每标签 `flex:0 0 auto` 胶囊 chip（圆角 + 边框）、图标显示、active 用胶囊高亮替代下划线；品牌精简为"天地玄黄"（隐藏 日月更替/图标/badge）。`dom-helpers.js` 的 `_highlightFab` 在移动端对激活项 `scrollIntoView({inline:'center'})` 自动滚入视野。
- 删除 `layout.css` 末尾已失效的 `.top-nav-head` 响应式死代码。
- 验证：`node --check` app.js/router.js/dom-helpers.js 通过；server(PID 3944) 静态服务已含新内容（curl 命中 tab-icon×7 / scroll-snap-type / tab-item.tab-active::after / Router.register('home'）。静态文件改完刷新即生效，无需重启/打包。
- **重要架构事实**：顶部导航是 SPA，`Router`(hash 路由) 驱动；`_highlightFab` 管理激活态；`--top-nav-height` 由 JS 实时测量（改导航高度不会撑乱内容间距）。各 UI 模块只监听 `page:changed==='calendar'`，而 `showCalendarPage()` 不论 home/calendar 路由都 emit 它，故新增 home 路由安全。

## 日历页面 UI 升级（Phase 4：页脚与公告栏）
- 用户诉求：进入 Phase 4，范围经确认 = **精修页脚 + 让两个遗留空壳公告栏真正生效**（原 #jqTopBar/#jishenBanner 全站无填充代码，永久空显）。
- **节气顶栏 #jqTopBar（生效）**：`calendar-ui.js` 新增 `_renderJqTopBar(data)`，在 `renderMonth` 末尾调用。数据源 = 月历 `days[].jieQi`（节气名）+ `days[0].gz.month[1]`（月建）。展示「『午月』 本月节气 小暑·大暑」式金色信息条。CSS `.jq-topbar` 加入 `position:sticky; top:var(--top-nav-height)` 吸附于导航下方（设计令牌化，去内联样式）。
- **吉神公告栏 #jishenBanner（生效）**：`detail-ui.js` 新增 `_renderJishenBanner(dat)`，在 `show()` 取到 `/calendar/day` 成功后填充，显示「今日吉神」+ goodGods 前 16 位胶囊（用 `--ss-good-*` 令牌），超量「等 N 位」；`hide()` 时清空隐藏。呼应本回合修复的 圣心/益後/續世 等神煞（goodGods 含之）。CSS `.jishen-banner` 设计令牌化、`.show` 入场动画。
- **固定页脚 .foot 精修**：`app.html` 重排为 品牌行(命理推断+朱砂版徽) + 金色细线 + 推演范围/历算来源/指导·校对·设计 三行；`footTab` 由 `<div>` 改 `<button>`（加 aria-expanded 同步）；`layout.css` 重写页脚区块（古典金层级、字号/间距令牌化、折叠态过渡保留）。
- 全部改动静态文件，刷新即生效。验证：`node --check` 三 JS 通过；curl 命中 jq-topbar/jishen-banner/_renderJqTopBar/_renderJishenBanner；HMAC 实测 API：day.goodGods 有值（例 天德合 月德合 四相 月刑 復日）、month.jieQi=小暑 大暑 + 月建午，数据源确认可用。
- 注意：吉神公告栏仅在「打开某日详情」时显示（初始静默高亮今天不触发详情 fetch，故首屏不显示，点选日期后生效）——符合"让公告栏生效"诉求。

## 修复：节气顶栏 #jqTopBar 遮挡日期选择交互
- 用户反馈：日历页"午月 本月节气 小暑·大暑"信息条挡住日期选择交互。
- 根因：Phase 4 给 `.jq-topbar` 加了 `position:sticky; top:var(--top-nav-height); z-index:var(--z-sticky)`。`.wrap` 是 `overflow:hidden` 且靠 `margin-top:var(--top-nav-height)` 避开 fixed 导航；jqTopBar 吸顶后钉在导航下方，滚动时盖住其下方的 `cal-header-bar`（年/月 `<select>` + 上/下月箭头，即切换月份的控件）及顶部日期格，导致选日期交互被挡。
- 修复：`web/css/calendar.css` 的 `.jq-topbar` 由 `position:sticky` 改为 `position:static`（去掉 `top`/`z-index`）。信息条回归正常文档流、位于 cal-header-bar 上方，永不遮挡控件。视觉样式（金色令牌、分隔符）保留不变。
- 验证：curl 命中 `position: static`（calendar.css:1021），且全 css 仅剩 line 563 一处 `position:sticky`（与 jq-topbar 无关）。静态服务刷新即生效，无需重启。

## 详情改造：模态"钤印浮层" → 内联渲染在日历下方（用户诉求，全推荐方案）
- 用户诉求：选中某天的详情在**日历底部内联渲染**，去掉"钤印查询"（印章开关 + 全屏底滑/侧滑模态浮层，"太土也不方便"）。经三问确认全采推荐方案：**内联嵌入日历正下方 + 点格直接展开(删印章) + 首屏默认展示今天**。
- **app.html**：`#cal-root` 内 `#FTLN` 之后新增 `<div id="cal-detail-inline" class="cal-detail-inline" aria-live="polite">`；删除废弃的 `#detail-root`（旧模态占位，模态本就自建 overlay 挂 body，占位从未使用）。
- **detail-ui.js（重写 show/hide/toggle/init）**：`show(y,m,d)` 改为渲染进 `#cal-detail-inline`（`.cal-detail-inner` 包裹 `renderDetail` 全部卡片），去掉遮罩/`body.overflow` 锁滚动/拖拽关闭/ESC/`_updateSealState`/自建 overlay。加 `_currentKey` 防过期响应覆盖；新增 `isOpen()` 并导出。`renderDetail` 及所有卡片渲染函数**原样保留**。`_renderJishenBanner`（Phase 4 吉神公告栏）保留，show 填充/hide 清空。
- **calendar-ui.js**：`renderDayCell` 删除 `.cal-detail-toggle` 印章按钮；点击委托去掉印章分支——点格 = `selectDay`（高亮+内联展开），**再点已展开的当天(`.detail-on`)= 收起**；`selectDay(y,m,d,silent)` 非静默时给 cell 加 `.detail-on` 并 `detail.show()`+emit，静默时 `detail.hide()`；删除 `_toggleDetailForDay`。init 首屏改为 `selectDay(今天)`(非静默)→**默认展示今天详情**；`home:goto-date`/`page:changed`/`_onSettingsChanged` 均在 `loadAndRender().then` 后 `selectDay` 恢复选中并展开详情（page:changed/settings 仅当选中日在当前月时）。
- **calendar.css**：新增 `.cal-detail-inline`（默认 `display:none`，`.show` 显示 + `calDetailIn` 淡入下滑动画）、`.cal-detail-inner` 内衬、顶部金色装饰细线(`::before`)、`.cal-detail-inline .detail-date-head{position:static}`（内联下取消模态吸顶）、loading/error 态、暗色适配。旧印章 CSS(`.cal-detail-toggle/.seal-char`) 留存但已无对应 DOM(无害)。
- 卡片入场动画 `.dp-reveal-item`（`dpFadeUp ... both`）为纯 CSS 自动播放、不依赖任何观察器/模态，内联后照常淡入并停终态，无隐患。
- `app.js` 的 `_closeDetailIfOpen()` 仅作用于已不存在的 `.detail-panel-overlay`（现为无害空操作）；内联详情随 `#cal-root` 页面整体显隐，切到紫微/设置无冲突，未改。
- 验证：`node --check` detail-ui.js/calendar-ui.js 通过；curl：app.html 含 cal-detail-inline(1)/detail-root(0)、calendar-ui.js cal-detail-toggle(0)/_toggleDetailForDay(0)、detail-ui.js cal-detail-inner(1)/detail-panel-overlay(0)、calendar.css cal-detail-inline(5)。静态刷新即生效。

## 三卡沉浸式重构（今日九星 / 时辰值神 / 今日神煞）
- 用户诉求：三张卡片"渲染样式太普通"。经 AskUserQuestion 确认：沉浸式重构(方位盘/时钟盘) + 九星每格补吉凶徽标含义 + 统一"焦点+网格"语言。
- **时辰值神 → SVG 环形12时辰时钟盘**（`detail-ui.js renderShichen`）：viewBox 0 0 200 200，cx=cy=100，外R=94/内r=54。新增 helper `_clockXY(cx,cy,r,deg)`(deg-90 转弧度极坐标)、`_annularSector(cx,cy,R,r,startDeg,endDeg)`(M..A..L..A..Z 环形扇区 path)。12 扇区每 30°(start=i*30-15/end=i*30+15)，`.clk-sec`+`clk-good`(黄道yellow)/`clk-bad`(黑道)/`clk-cur`(当前)。当前时辰 `curSC = Math.floor(((getHours()+1)%24)/2)`(子时跨零)，指针 `.clk-pointer`+`.clk-hub` 指向 curSC。中央 `.clk-center` 显示 此刻/branch时/god/黄道·吉|黑道·凶/时间范围(cur.full.split(/[：:]/)[1])。图例 `.shichen-legend`。
- **今日九星 → 焦点区 + 洛书九宫盘**（`renderNineStar`）：顶部 `.ns-hero`(ns-hero-badge 吉/凶 + name + tag + desc=meaning)；下方 `.ninestar-grid` 3×3，每格 `.ninestar-item` 含 `.ns-badge`(absolute 右上圆徽 吉/凶) + `.ns-name` + `.ns-brief`(新增常量 `NINE_STAR_SHORT[idx 0-8]`) + `.ns-label`。
- **今日神煞 → 焦点总览 + 信息徽章 + 标签墙**（`renderShensha`）：`.ss-hero`(goodN 吉神护佑 / 中间建除 jcGood?黄道:黑道 / badN 凶煞值日)；`.ss-info-badges` 用新增 helper `_ssBadge(icon,label,val)` 生成 4 徽章(ti-flame五行=dayNaYin/ti-building-castle建除+吉凶/ti-swords冲煞=chong·sha/ti-star廿八宿=xiuFull)；保留 `buildSsGroup` 标签墙。
- CSS（`calendar.css`，插于 `.cosmic-ss-group.bad-group` dark 之后、"特殊日警示"之前）：新增 `.ns-hero*`/`.ns-badge`/`.ns-brief`/`.ns-label`、`.shichen-clock-wrap`/`.shichen-clock`/`.clk-*`/`.shichen-legend`、`.ss-hero*`/`.ss-info-badges`/`.ss-badge*` 全套 + body.dark 覆盖。统一"焦点区 Hero + 细节网格"视觉语言。
- 未改后端：数据字段已满足（HMAC 实探确认 nineStar{name,good,meaning,idx} / shichen[12]{branch,god,koujue,yellow,full} / goodGods/badGods / jianchu·dayNaYin·chong·sha·xiuFull 齐备）。
- 验证：`node --check`(以 .mjs)通过；curl 命中 detail-ui.js 的 shichen-clock/ns-hero/ss-hero/_annularSector/NINE_STAR_SHORT、calendar.css 的 shichen-clock-wrap/clk-sec/clk-pointer/ss-info-badges/ss-badge/ns-hero-badge。静态刷新即生效。
- 坑：`node --check` 对含 `import` 的文件须用 `.mjs` 扩展名（否则 "Cannot use import statement outside a module"）。HMAC 签名串 = `mid+ts+nonce+url` 无分隔拼接、body 用 `{y,m,d}`（参考 `server/smoke_test_api.js`），非换行分隔/非 {year,month,day}。

## 修复：节气顶栏月建与节气错位（方案 A：月中取值）
- 用户反馈："未月 本月节气 立秋·处暑"——月建与当月节气不匹配。诊断：原 `_renderJqTopBar` 取该公历月第 1 天的干支月（days[0].gz.month[1]），而节气扫描整月；首日月建总比"当月交节后月建"落后一位（如 8月1日仍属未月，但立秋后已入申月），导致顶栏"未月 立秋·处暑"错误。
- 修复（`calendar-ui.js _renderJqTopBar`）：月建改取**当月月中(15日附近)**的干支月——节气交节必在月初，月中必已入当月节气所属干支月。实现：过滤当月日(curDays = !other && d && gz.month)，取 d 最接近 15 的那天 midDay，再取 midDay.gz.month.charAt(1)。
- 验证：HMAC 实探 2026 全年 12 月，新逻辑输出月建与节气所属干支月 **12/12 一致**（1丑2寅3卯4辰5巳6午7未8申9酉10戌11亥12子），0 失败。7月→未月 小暑·大暑、8月→申月 立秋·处暑 均正确。`node --check`(calendar-ui.js, .mjs) 通过；curl 命中上线(curDays/midDay/Math.abs(d.d-15))。
- 注意：此为前端展示口径修复，与后端神煞推算无关；年神/月建推算本身无误，仅顶栏取数锚点错位。

## 月标注区分改造（方案一）：顶栏全称+节气标、详情头拆双色标
- 用户确认"未月/乙未月"易混淆，选定方案一：用「节气(金)/农历(灰)」双色身份标签区分三种"月"，顶栏改显示完整月建全称"乙未月"（与详情对齐，消除"未"vs"乙未"字面差）。
- 顶栏（calendar-ui.js _renderJqTopBar）：月建变量由 `mbChar`(只取地支 charAt(1)="未") 改为 `mbFull`(完整 gz.month="乙未")；渲染加 `<span class="jq-tag jq-tag-jq">节气</span>` 前缀。现显示「节气 乙未月 本月节气 小暑·大暑」。
- 详情头（detail-ui.js renderDetail 行2）：原 `乙未月【五月·沙中金】` 把节气月(乙未月)与农历月(五月)塞同一【】，易混。改造拆开：节气月加 `<span class="ddh-tag ddh-tag-jq">节气</span>`、农历月加 `<span class="ddh-tag ddh-tag-nl">农历</span>`（var _lm 定义前移）。现显示「丙午年【天河水】· 节气 乙未月【沙中金】 · 农历 五月 · 己丑日【初一·路旁土】」。
- CSS（calendar.css）：新增 `.jq-topbar .jq-tag`(金色描边标) + `.ddh-tag`/`.ddh-tag-jq`(金)/`.ddh-tag-nl`(灰) 标签样式，复用 --color-gold/--text-muted/--border-light token，明暗自动适配。
- 验证：node --check 两 JS 通过；curl 命中 jq-tag-jq/mbFull、detail-ui 命中 ddh-tag-jq/ddh-tag-nl、calendar.css 三处；HMAC 实探 7月 顶栏将显示完整月建"乙未"（原错"未"）。
- 附注：calendar-ui.js 第499行 `midDay.d` 经 Grep 确证为正确拼写（之前 Read 显示 `m.idDay` 系 Read 渲染视觉误判，前端无 bug）。

## 八字排盘分析页 计算逻辑审计（逐项·隔离·可还原）
- 用户诉求：重新规划分析"八字排盘分析"页全部计算逻辑正确性，一项一项验证、实时交互、改前备份、崩了可还原。确认方案：3库交叉验证(推荐) + 汇报后备份再修(推荐) + 重点测边界(推荐)。
- 架构：web/js/bazi-ui.js(4板块渲染) → server/routes/bazi.js(API:HMAC `/api/v1/bazi`, 双引擎A/B) → 计算内核 gongxin-core=`H:/Phone/core`(lunar.js四柱/胎元/命宫/神煞; bazi.js旺衰/格局/用神/从格/十神; wuxing.js五行分; shensha.js神煞)。4板块：核心命盘(四柱·大运·流年)/格局分析(日主·格局·用神)/五行十神(生克·制化·干支)/命理参考(胎元·神煞·称骨)。
- 备份：`H:/Phone/Back/bazi_audit_20260714/` 已存 12 个候选文件时间戳副本(.bak)。改前再单独备份。
- 交叉验证harness：`H:/Phone/verify/_probe_bazi.mjs`(四柱) / `_probe_dayun.mjs`(大运)。参考库正确用法：
  - lunar-javascript：`Solar.fromYmdHms(y,m,d,h,min,0).getLunar().getEightChar()` → `ec.getYear()/getMonth()/getDay()/getTime()`（注意非 getYearInGanZhi）。
  - mystilight-8char：require **`mystilight-8char/src/index.js`**（package.main=`dist/index.js` 缺失会加载失败！）→ `getCurrentEightCharJSON({year,month,day,hour,minute,sect:2,gender})`，四柱取 `r.pillars.{year,month,day,hour}.value`，大运 `r.dayunArr[i].ganZhi`+`startYear`，流年 `dayunArr[i].liunianArr`。
  - lunisolar+char8ex：`require('lunisolar/plugins/char8ex')` 后 `lsr.char8.char8ex('乾'/'坤')`——本次 `char8ex is not a function`，插件未成功加载，暂未用作基准。
- **八字排盘审计（核心命盘/格局/十神/命理参考）— 已完成核验**：
  - **时柱午夜 Bug（已修复并验证）**：`server/routes/bazi.js:66` `parseInt(body.h,10)||12` 在 h=0 时把午夜当正午。改为 `isNaN(parseInt(body.h,10))?12:parseInt(body.h,10)`（保留0）。需**重启 node 服务**才生效（routes 是启动时加载模块）。验证：2000-01-01 00:30→壬子、12:30→戊午、1984-08-20 00:15→戊子，与 lunar-javascript 逐柱一致；非午夜/大运不变。
  - **晚子时(23:xx)换日＝正确，非 bug**：8/8 例项目日柱=真实次日日柱（含跨月/跨年/闰月边界 1995-12-31→1996-01-01、2000-02-29→03-01），遵循"晚子时换日"传统（子平主流）；lunar-javascript 用"不换日"另一派才显得不同。**不改**。
  - **十神主星显示 Bug（新发现，仅显示层）**：`core/engine/sxwnl-bundle.js` 的 `sshen(b3,b4)` 算**时柱主星**在 20/20 抽样命中 4 例错误（2010-10-10 戊午时 偏财应正官、2005-03-03 甲午时 正印应偏印、1960-12-12 甲戌时 正印应比肩、1997-02-02 己卯时 正官应偏财）；年/月/日主星正确。但**格局/用神/五行力量走 `core/bazi.js` 自有 `_dmGetRelFull(dayStem,otherStem)`（经核验正确）**，不碰引擎 sshen → 分析计算不受污染。结论：仅"专业细盘表"时柱主星标签错，低危。修复需改显示层或引擎，待用户定。
  - **胎元 12/12 正确**（与 lunar-javascript 全一致）；**命宫/身宫 3/12 与 lunar 不同**（1969/1992/2010），胎元却全对 → 符合"命宫/身宫算法流派差异"特征，非随机错，需第二参考源复核。
  - **五行力量**：`wuxingPct` 12/12 求和=100，正确。
  - **定格/用神/从格**：启发式，内部一致；`_dmGetRelFull` 正确 → 定格月令本气一致性 4/12 直连、8/12 为从格/透干（预期）。无确定性 bug。
- 交叉验证 harness 存于 `H:/Phone/verify/_xval.mjs`(综合)、`_verify_hourfix.mjs`(时柱修复回归)、`_verify_latezi.mjs`(晚子时)、`_test_hourstar.mjs`(时主星20例)、`_probe_*.mjs`(探查)。参考库：lunar-javascript(Solar.fromYmdHms().getLunar().getEightChar()，胎元 getTaiYuan/命宫 getMingGong/身宫 getShenGong/十神 get*ShiShenGan)。
- 服务重启：PID 原 3944 已停，现 `C:/Users/Administrator/.workbuddy/binaries/node/versions/22.22.2/node.exe index.js` 后台跑于 `H:/Phone/server`（端口3000）。改 routes/server 代码须重启才生效；改 web/ 静态文件刷新即生效。

## 八字排盘审计·三点复核收尾（用户要求"三个点都要干"）
逐项、隔离、可还原。备份 `H:/Phone/Back/bazi_audit_20260714/` 完好。本轮**未改动任何代码**（仅验证）。
- **点1 时主星显示bug（推翻误报，不修复）**：之前 `_test_hourstar.mjs` 报告的"时主星20%错误"是**假阳性**——该脚本解析错源字段，且误读 `b4=(D-1)*12+90000000+SC` 编码（b4%10 与时干无直接关系）。改用"直接解析专业细盘HTML主星行 + lunar-javascript 7例交叉"：年/月/时三柱主星 **7/7 全一致（0失败）**。时主星实际完全正确，无需修复。注意：`Lunar.sshen` 内部用 `t%10` 取天干、四柱主星（含时）走 `sz.gss[3]=sshen(b3,b4)`（sxwnl-bundle.js:3339），实现正确。
- **点2 命宫/身宫/胎元（第三方复核）**：胎元 12/12 正确（标准算法"月干进一、月支进三" + lunar-javascript 双验全一致）；命宫/身宫与 lunar-javascript 有 3/12 差异（1969/1992/2010），经分析属"真太阳时修正 + 流派起法（卯/酉中介）"灰色地带，非确定性bug，**不改**（改了可能偏离主流子平法）。
- **点3 神煞/称骨/干支关系（机制验证）**：①神煞按四柱各柱独立查（年/月/日/时柱各自标神煞），命名规范、归属合理（天德/月德在月柱、天医/国印在日柱、天乙按柱干查）——核心神煞（天乙/驿马/文昌/桃花/华盖/将星/羊刃）与标准算法一致；②干支关系正确且完整（含六合/三合/六冲/六害/三会/半合/相刑/自刑），与我自算核心关系（六合/三合/六害）完全一致，且更详尽；③称骨总重=各部分求和（1990例 9+9+10+8=36钱=3两6钱 ✓算术自洽），袁天罡歌诀格式标准。全部机制正确。
- **关键教训**：交叉验证务必用"权威解析路径"（如直接解析渲染后的专业细盘表格主星行），勿依赖自写可能含阴阳/编码误判的对照函数；`b4` 编码 `(D-1)*12+90000000+SC` 的 `%10` 不等于时干，勿据此误判时柱。
- 本轮新增验证脚本：`_extract_star.mjs`(主星行提取)、`_verify_star_final.mjs`(主星7例交叉)、`_point2_final.mjs`(命宫/胎元三方)、`_probe_shensha.mjs`/`_point3_shensha_check.mjs`(神煞)、`_point3_rest.mjs`(干支关系+称骨)。

## 八字排盘输入页 UI 重设计（古典卷轴·命主档案式）
- 用户诉求：按确认设计稿重设计"八字排盘分析"输入页。方向：古典卷轴风格 + 个人档案式布局 + 快捷优先交互（AskUserQuestion 选定）。
- 备份：`H:/Phone/Back/bazi_ui_redesign_20260714/`(bazi-ui.js + bazi.css 时间戳 .bak)。
- CSS（`web/css/bazi.css` 末尾追加"古典卷轴·命主档案式"区块）：`.bazi-scroll`(卷轴外框 + 上下 `.bazi-scroll-rod` 卷轴杆 + 宣纸底 #faf7f0) / `.bazi-form-title`(居中大字) / `.bazi-profile-card`(命主档案卡) / `.bazi-avatar`(未字头像圈) / `.bazi-name-input` / `.bazi-profile-actions` / `.bazi-mode-pills`+`.bazi-mode-pill`(A·滴天髓/B·子平，取代旧 `.engine-toggle`/`.engine-opt`) / `.bazi-profile-card .bazi-sex-btn.active`(印章红) / `.bazi-time-hint`+`.bazi-now-link`(使用当前时间) / `.bazi-shichen-grid`+`.bazi-shichen-cell`(十二时辰点击切换) / `.bazi-recent`+`.bazi-recent-chip`(最近城市) / `.bazi-submit-row .bazi-action-go`(印章红) / 暗色模式 `body.dark` 覆盖 + ≤480px 时辰6列。
- JS（`web/js/bazi-ui.js`）：重写 `buildFormHTML()` 结构 = 卷轴外框 → 命主档案卡(头像+姓名+性别+模式pills+保存/档案) → 出生时间(公历/农历+年/月/日/时间+当前时辰提示+使用当前时间+十二时辰网格) → 出生地点(regionCascaderContainer + 最近城市chips) → 开始排盘(印章红)。新增 `_SHICHEN`/`_SHICHEN_TIME`/`_hourToShichenIdx()`/`_syncShichenFromTime()`。引擎切换选择器 `.engine-opt`→`.bazi-mode-pill`（bindFormEvents + renderResult + doSave 三处同步）。bindFormEvents 末尾新增：时辰网格 click→设时间并高亮、time input change 同步时辰、使用当前时间→填今天年月日时、最近城市 chips→`window._rcSelectByRegion(prov,region)`、初始 `_syncShichenFromTime()`。
- 保留逻辑：性别/历法tab/闰月/年份/保存/档案/cascader/引擎即时切换均不变；去掉旧 `.bazi-header` 标题栏（并入档案卡）。
- 验证：`node --check`(.mjs) 通过；server(端口3000) 静态服务已含新代码（curl 命中 bazi-scroll/bazi-profile-card/bazi-shichen-cell/bazi-recent-chip/bazi-mode-pill）。改 web/ 静态文件刷新即生效，无需重启。

## 页脚重设计：去折叠 + 细条常驻 + 源流浮层（用户诉求"点击隐藏太土"）
- 用户诉求：重设计公用页脚渲染方案，原 `.foot-tab` 折叠箭头 + `max-height` 折叠"点击隐藏"方式太土。经 AskUserQuestion 确认方向：细条常驻 + 轻点「源流」淡出浮层；署名用书画落款式。
- 备份：`H:/Phone/Back/foot_redesign_20260714/`(app.html + layout.css + app.js 时间戳 .bak)。
- app.html：删除 `foot-tab` 折叠按钮与 `foot-body` 折叠容器；改为 `.foot-bar` 三栏(`.foot-brand`命理推断 + 朱砂`.foot-badge`八卦掌决版 / `.foot-range`岁次 前4712—公元9999 / `.foot-src`「源流」按钮) + `.foot-pop` 源流浮层(`.foot-pop-hd`朱砂`.foot-seal`"源" + `.foot-pop-title`历算源流 / 两行`.foot-pop-line`来源 / `.foot-pop-rule`金线 / `.foot-pop-credit`书画落款"指导 王贵柱 · 校对 芹芹 · 设计 梓踊")。
- layout.css：重写页脚样式块（原 161-269 折叠逻辑全删）。`.foot` 改 `font-family:var(--font-display)`（注意项目无 `--font-serif`，国风字族用 `--font-display`）；`.foot-bar` 描金上边线 + 宣纸底 + flex 三栏；`.foot-src` 描金按钮(hover / `aria-expanded=true` 态)；`.foot-pop` `position:absolute; bottom:calc(100% + 8px); translateX(-50%) translateY(8px)` → `.open` 时 `translateY(0)` 上滑淡入(opacity 0→1, 220ms)；暗色 `body.dark` 覆盖。变量(--border-gold/--color-cinnabar-deep/--color-gold-bright/--font-display 等)均已在 variables.css 定义。
- app.js initFooter：移除折叠 toggle；改为 `.foot-src` click 切换 `.foot-pop.open`(openPop/closePop，closePop 用 240ms 延时 hidden 让动画播完) + document click 外部点击关闭 + ESC 关闭。
- index.html 的 `.footer` 为旧落地页静态页脚（无折叠），不在本次范围，未改。
- 验证：app.js `node --check`(.mjs) 通过；curl 确认 app.html 含 footSrc/footPop、layout.css 含 foot-bar/foot-pop.open、app.js 含 openPop，且 footTab 旧引用计数=0（已彻底移除）。静态刷新即生效，无需重启。

## 微调：八字排盘输入页默认头像由"未"改为"?"
- 用户反馈：命主档案卡默认头像显示"未"，易被误解为地支"未月/未羊"，不知道什么意思。
- 修改：
  - `web/js/bazi-ui.js` `buildFormHTML`：默认头像字由"未"改为"?"，表示未填写姓名；姓名输入框 `value` 由"未知"改为空字符串，placeholder 显示"姓名"更自然。
  - `bindFormEvents` 末尾增加姓名输入同步头像：监听 `#Name_input` 的 `input` 事件，输入后头像自动显示姓名第一个字（`v.charAt(0)`），清空或仅空格时恢复"?"。用 `_boundAvatar` 守卫避免重复绑定。
- 验证：`node --check`(.mjs) 通过；curl 命中 baziAvatar default=?>、Name_input value=""、`_boundAvatar`/`_updateAvatar` 存在；保存/档案仍用 `value || '未知'` 兜底，不影响原逻辑。静态刷新即生效，无需重启。

## 紫微斗数页 国风统一（A+B+C 全深）
- 用户诉求：紫微排盘与其它页面（日历/八字国风）完全不搭，规划并落地统一方案。经诊断 + AskUserQuestion 确认方向：**全深（壳层A+浮层B+紫微内部C）+ 国风标题条与进入过渡 + 命盘固定暗金国风**。
- 诊断关键事实：紫微是 `H:/Phone/ziwei/` 下**独立打包 SPA（无组件源码，仅 index.html+assets 产物）**，通过 `web/app.html` 的 `#ziwei-root` iframe 全屏覆盖主应用（server/index.js `app.use('/ziwei', express.static(ziwei))`）。之前已做一轮浅国风化：配色变量重映射、`.iztro-palace` 命盘宫格描金边、罗盘楷体、`.ant-btn-primary` 金渐变；但残留：①iframe 进出生硬无过渡 ②返回胶囊 bug（已修但仍是悬浮小胶囊非标题条）③AI浮窗/宫位详情浮层是赛博深蓝紫 `#12122a` ④`.ant-card`/`.form-container` 深青 `rgba(14,24,24,.7)` 底。
- 改造文件（全部静态，刷新即生效，无需重启/打包）：
  - **A 壳层（主应用侧）**：
    - `web/app.html` `#ziwei-root` 结构重做：加 `#ziwei-bar`(国风标题条：‹返回 + 紫微斗数 + 印章"掌") + `#ziwei-loading`(国风 loading：朱砂"紫"印章摆动 + "正在展开命盘…") + iframe `top:50px`。
    - `web/css/layout.css` 末尾追加：#ziwei-root 默认 `display:none;opacity:0;translateY(14px)` → `.open` 触发 `.28s` 上滑淡入；#ziwei-bar 50px 高金线标题条；#ziwei-loading 居中 loading；#ziwei-iframe `top:50px`。明暗自动（`--bg-page`/`--color-gold` 等）。
    - `web/js/app.js`：`showZiweiPage` 加 `void offsetWidth` reflow + `classList.add('open')` 进入过渡 + 显示 loading + 绑定 `#ziwei-back`→`closeZiweiPage`；iframe `onload` 隐藏 loading；`closeZiweiPage` 移除 `.open` + `setTimeout(280)` 后 `display:none` 退出过渡。
  - **B 浮层国风化（注入 `ziwei/index.html` 的 `#guoxin-unify` style）**：`#ai-floating`/`#ai-head-bar`/`#ai-toggle`/`#ai-*`(赛博紫 #12122a) → 暖金 #1f1b15+描金边；`#palace-detail` rgba(18,18,34) → rgba(31,27,21,.96)；`#sf-panel`(body inline 赛博紫) 用 `!important` 覆盖 inline → 暖金；星名 `.pd-star`/`.pd-name` 暖金。
  - **C 紫微内部深度国风化（同段 `#guoxin-unify`）**：`.ant-card`/`.form-container` 深青→暗金描边；全局楷体字族（`body/.site-menu/.ant-*`/标题/按钮/输入）；`header{top:50px!important}` 把紫微自身 fixed header(64px) 下移到主应用标题条下方；`#root{padding-top:114px!important}` 内容补偿防遮；`.iztro-palace` 描金边保持一致。
  - **返回胶囊**：`ziwei/index.html` 的 iframe 内 `#back-to-calendar` 在 `inIframe` 时 `display:none`（主应用标题条已接管返回；非 iframe 直接访问 /ziwei/ 仍保留原链接返回）。
- 备份：`H:/Phone/Back/ziwei_unify_20260714/`(app.html + app.js + ziwei/index.html + ziwei/assets/main-d2ee087f.css 时间戳 .bak)，可一键还原。
- 验证：`app.js node --check`(.mjs) 通过；curl 命中 app.html 的 ziwei-bar/ziwei-loading、layout.css 的 #ziwei-root.open/.ziwei-bar-title、app.js 的 ziwei-back/ziwei-loading/280ms 退出、ziwei/index.html 的 guoxin-unify/header top:50px/#root padding-top:114px/sf-panel 暖金/back 隐藏。全部线上生效。
- 注：紫微内部命盘 `.iztro-palace` 此前已描金边、罗盘已楷体，C 层未重复改；命盘固定暗金（未做主应用↔iframe 主题同步，属可选增强）。
- 预览：`http://localhost:3000/app.html`（点"紫微"tab 进入，看标题条+进入过渡+暗金命盘+暖金浮层）。

## 撤销：紫微斗数页 国风统一（A+B+C 全深）
- 用户 23:51 要求"撤销"。已用 `H:/Phone/Back/ziwei_unify_20260714/` 备份还原全部改动：
  - `web/app.html`、`web/js/app.js`、`ziwei/index.html` 由 .bak 直接 cp 还原（命中验证：app.html ziwei-bar=0、app.js 280ms退出=0、ziwei/index.html guoxin-unify=0、#sf-panel暖金覆盖=0）。
  - **layout.css 备份缺失**（`Back/` 目录无 `layout.css.20260714.bak`，备份时该 cp 未落盘）：改用 Edit 手动删除此前在 layout.css 末尾插入的紫微壳层样式块（#ziwei-root/#ziwei-bar/#ziwei-back/#ziwei-loading/#ziwei-iframe 约 56 行），恢复为备份前结构（紧接 `body.dark .home-picker-sel` 后直接到「响应式」注释）。curl 确认 ziwei CSS 残留=0。
- 现状：紫微页回到统一前状态（全屏 iframe 覆盖 + 悬浮小胶囊返回 + 赛博紫浮层）；主应用/日历/八字/页脚仍为统一国风。备份目录保留可二次还原。
- 教训：备份脚本若某行 cp 因路径/权限静默未落盘，后续还原会缺文件；下次备份后应立即 `ls` 校验每个预期 .bak 均存在再继续。（点"紫微"tab 进入，看标题条+进入过渡+暗金命盘+暖金浮层）。
