# 2026-07-17

## 紫微斗数统一诊断（diagnose 技能）
- 主应用 `web/`（vanilla JS + tab）与紫微 `ziwei/`（独立 React SPA，Vite 打包，3004 端口）通过全屏 iframe 集成。
- 反复"改紫微破坏原代码"根因：紫微胶水(~75行)活在启动入口 `web/js/app.js`；"统一"改动落在共享关键文件(app.html/app.js/layout.css)且**无自动回归闸门**。
- 推荐方案（未执行，待用户确认）：A) 抽胶水到 `web/js/ziwei-shell.js`；B) 视觉走 token 重映射+postMessage 主题同步(不重写 React)；C) 建反馈回路 L0`node --check`全量→L1 JSDOM boot 冒烟(升级 `web/diag.html`)→L2 Playwright 回归。
- 不建议把 React SPA inline 进主应用当真 tab（撞类名/变量，正是破坏源）。
- 交付物：`H:/Phone/ziwei-unify-diagnosis.md`（诊断+分步方案）。

## 前端安全阀落地（L0+L1，diagnose 第一步）
- 用户确认方向：视觉风格对齐（保留 iframe）+ 先建 L0+L1 安全阀。
- L0：`scripts/check-syntax.js`（全量 node --check web/js），npm `check:syntax`。L1：`scripts/smoke-boot.mjs`（jsdom 启动冒烟：加载 app.html DOM+注入桩+动态 import app.js 真实启动，断言 #cal-root 未致命失败/#Cal3 已渲染/挂载点齐全），npm `check:smoke`。
- `package.json` 的 `check` 链已含 `check:syntax`；jsdom 入 devDependencies（managed node workspace 合规）。
- 已注入故意错误验证两道闸门均拦得住（语法错误 / 删除 #cal-root 挂载点）。
- 后续步骤 A（紫微胶水抽到 ziwei-shell.js）、B（token 固化 + postMessage 主题同步）可在安全阀保护下进行，改完跑 `npm run check:syntax && npm run check:smoke`。
- 注意：Playwright 已装但无浏览器二进制，L2 回归需 `npx playwright install chromium`（沙箱可能受限）；L1 用 jsdom 已足够覆盖"改共享文件破坏启动"类。

## 重启服务 + 改动落地验证（2026-07-17 晚）
- 服务此前未运行（3000/3004 均无监听）。按 diagnose 纪律：先过 L0+L1 闸门（20/20 语法通过、boot 冒烟 #Cal3 已渲染）→ 再起服务。
- 启动：`cd H:/Phone/server && node index.js`（managed node 22.22.2，NODE_PATH=server/node_modules），后台 nohup，PID 监听 0.0.0.0:3000。日志 `server/.restart.log`。
- 验证（HMAC 鉴权 /api/v1/*）：health=200；month(2026-7)=200 返回 31 天、含 nineStar/isTianshe/isTiande 字段；day(2026-7-16)=200 含 goodGods/badGods/shichen。原代码改动（web/ 前端 + server routes + core）已生效且未破坏核心计算。
- 紫微生产由主服务 `/ziwei` 静态挂载提供，无需单独起 3004 dev server。

## 步骤 A 落地：紫微胶水抽离到 web/js/ziwei-shell.js（2026-07-17 晚）
- 新建 `web/js/ziwei-shell.js`（ESM）：原样搬入 `showZiweiPage/closeZiweiPage/_onZiweiEsc`、ziwei:close 消息监听、window 暴露；`showCalendarPage` 经 `registerZiweiShell({showCalendarPage})` 依赖注入（避免循环依赖，行为与原版一致）。
- `web/js/app.js` 改造：删除紫微胶水函数/消息监听/window 暴露（约 80 行），ziwei 路由改为 `import('./ziwei-shell.js')` **懒加载** + `.catch` 回退到日历；移除不再使用的 `time-util` import。
- 关键收益：紫微壳成为独立模块，**即便它有 bug 也拖不垮日历/八字启动**（启动路径不再静态依赖紫微代码）。
- 新增 `scripts/smoke-ziwei.mjs`（L1.5，jsdom 最小 DOM 验证紫微壳显示/关闭/依赖回调/导出完整性，12 断言）。`package.json` 加 `check:smoke-ziwei` 并并入 `check` 链（现含 hmac/syntax/css/zh/boot-smoke/ziwei-smoke）。
- 验证：L0 20/20、L1 启动冒烟、L1.5 紫微冒烟全绿；运行服务 health=200 且实时吐出 ziwei-shell.js / 改后 app.js（前端静态服务，无需重启）。
- 下一步（可选，待用户确认）：步骤 B 视觉对齐（token 重映射固化 + postMessage 主题同步，不重写 React）。

## 步骤 B 落地：紫微视觉对齐（token 固化 + 主题实时同步）（2026-07-17 晚）
- 根因确认：紫微 CSS 消费的变量（--color-brand/bg/bg-panel/text-main/divider/ant-bg/ant-switch-inner/container-shadow/html-bg）正好等于 index.html 那段 remap 映射的变量——remap 已覆盖到位。
- 隐藏 bug 修复：主应用暗色 token 定义在 `body.dark`，紫微用 `:root.dark` 触发，`var(--color-*)` 在 html 层回退到亮色（如 `--color-text-main` 暗色下解析成近黑→暗底看不见字）。已将 `ziwei/index.html` 的 `:root.dark` 与 `body.dark input/select/textarea` 块改为硬编暗色字面量（对齐主应用 body.dark token），作为紫微↔主应用 token 唯一真源。
- 主题同步（postMessage）：主应用 `web/js/ziwei-shell.js` 新增 `_syncZiweiTheme()`（读 body.dark/本地 theme → 写 `pub.ziwei.theme` + `iframe.contentWindow.postMessage({type:'theme',theme})`）；在 `showZiweiPage` 设 src 前写 key（首屏正确）+ iframe load 后 post；`initZiweiShell` 加 MutationObserver 监听 body 主题变化实时同步。紫微 `index.html` 加 message 监听 + 首屏把 `.dark` 同时加到 html 和 body（让紫微 :root.dark 与主应用 body.dark 都生效）。
- 测试：`scripts/smoke-ziwei.mjs` 新增 3 条主题断言（导出 syncZiweiTheme / 暗→pub.ziwei.theme=dark / 亮→light），并入 `npm run check` 链。L0+L1+L1.5 全绿（15 断言）。
- 验证：运行服务 health=200，/ziwei/ 与 /js/ziwei-shell.js 实时含新逻辑（双 .dark / 暗色字面量 / postMessage theme / 监听）。
- 注意：本环境无浏览器自动化（无 Playwright 二进制），紫微 iframe 的"切主题视觉跟随"需真机目检；逻辑+token 已闸门外验证。
- 待办：步骤 C（L2 Playwright 真浏览器回归，需装 chromium 二进制，沙箱可能受限）。
